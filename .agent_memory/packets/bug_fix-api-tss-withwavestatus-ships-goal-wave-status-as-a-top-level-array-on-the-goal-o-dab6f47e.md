---
type: "Bug Fix"
title: "api.ts's withWaveStatus ships goal.wave_status as a TOP-LEVEL array on the goal object ..."
description: "api.ts's withWaveStatus ships goal.wave status as a TOP LEVEL array on the goal object one entry per plan.wave, shape {status: 'merged'|'executing'|'due'|'waiting'|'partial', failed run ids?} , derived by goal.ts's goalW"
resource: "mcp/audit-driven-parity-pass.test.ts"
tags: ["delegated-run", "kage-run:fix-two-app-client-defects-found-by-oper-260821-cdaa"]
timestamp: "2026-08-21T08:32:29.461Z"
x-kage-id: "repo:fix-two-app-client-defects-found-by-oper-260821-cdaa:bug_fix:api-tss-withwavestatus-ships-goal-wave-status-as-a-top-level-array-on-the-goal-o"
x-kage-type: "bug_fix"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/audit-driven-parity-pass.test.ts", "mcp/delegation/app-client.ts", "mcp/fix-two-app-client.test.ts", "mcp/sessions-ui.test.ts"]
---

# api.ts's withWaveStatus ships goal.wave_status as a TOP-LEVEL array on the goal object ...

> api.ts's withWaveStatus ships goal.wave status as a TOP LEVEL array on the goal object one entry per plan.wave, shape…

api.ts's withWaveStatus ships goal.wave_status as a TOP-LEVEL array on the goal object (one entry per plan.wave, shape {status: 'merged'|'executing'|'due'|'waiting'|'partial', failed_run_ids?}), derived by goal.ts's goalWaveStatus — never a field on the wave object itself. app-client.ts's waveGateStatus previously read a wave.status.due boolean that no server code ever produced, meaning the due-wave banner and Dispatch wave button had never rendered for any goal since inception.

Learned while delivering: Fix two app-client defects found by operator dogfooding today, both in mcp/delegation/app-client.ts (+styles if needed). DEFECT 1 — the goal overlay never renders the due-wave banner or the Dispatch wave button: waveGateStatus(wave) reads goal.plan.waves[i].status.due (a boolean on the wave object), but the API ships the derived status as a TOP-LEVEL goal.wave_status array (withWaveStatus in api.ts) whose entries carry status as a STRING ('merged'|'executing'|'due'|'waiting'|'partial') — the two shapes never met, so the button has never rendered for any due wave. Reconcile: make the client read goal.wave_status[i] (map the string to due/waiting display), keep dueWaveIndex consistent, and add a test that composes the page and asserts a goal with wave_status [merged, due] renders the 'due now' line and a Dispatch wave button — this must not be able to silently regress again. Do not change the server shape (other clients read wave_status). DEFECT 2 — inlineAsk rows do not survive re-renders: the inline reason row (from the recent prompt() replacement) disappears when a background revision render rebuilds the run-detail overlay (reproduced: opened Reject reason row, waited ~15s, row vanished with typed state lost). Make open inline-ask state survive: hold it in client state keyed by run id + action and re-render it (with its typed text preserved) after any rebuild, or suppress rebuilding the affordance region while open — whichever matches the codebase's revision-gated render pattern; add a test if the harness allows asserting it. CONSTRAINTS: app files are TS template literals — doubled escapes, no backticks or dollar-brace, h() only, never innerHTML; composed-page parse test stays green; cite files individually.
Verified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability

## Verification

npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability

# Citations

[1] explicit_capture (2026-08-21T08:32:29.461Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:fix-two-app-client-defects-found-by-oper-260821-cdaa:bug_fix:api-tss-withwavestatus-ships-goal-wave-status-as-a-top-level-array-on-the-goal-o","title":"api.ts's withWaveStatus ships goal.wave_status as a TOP-LEVEL array on the goal object ...","summary":"api.ts's withWaveStatus ships goal.wave status as a TOP LEVEL array on the goal object one entry per plan.wave, shape {status: 'merged'|'executing'|'due'|'waiting'|'partial', failed run ids?} , derived by goal.ts's goalW","body":"api.ts's withWaveStatus ships goal.wave_status as a TOP-LEVEL array on the goal object (one entry per plan.wave, shape {status: 'merged'|'executing'|'due'|'waiting'|'partial', failed_run_ids?}), derived by goal.ts's goalWaveStatus — never a field on the wave object itself. app-client.ts's waveGateStatus previously read a wave.status.due boolean that no server code ever produced, meaning the due-wave banner and Dispatch wave button had never rendered for any goal since inception.\n\nLearned while delivering: Fix two app-client defects found by operator dogfooding today, both in mcp/delegation/app-client.ts (+styles if needed). DEFECT 1 — the goal overlay never renders the due-wave banner or the Dispatch wave button: waveGateStatus(wave) reads goal.plan.waves[i].status.due (a boolean on the wave object), but the API ships the derived status as a TOP-LEVEL goal.wave_status array (withWaveStatus in api.ts) whose entries carry status as a STRING ('merged'|'executing'|'due'|'waiting'|'partial') — the two shapes never met, so the button has never rendered for any due wave. Reconcile: make the client read goal.wave_status[i] (map the string to due/waiting display), keep dueWaveIndex consistent, and add a test that composes the page and asserts a goal with wave_status [merged, due] renders the 'due now' line and a Dispatch wave button — this must not be able to silently regress again. Do not change the server shape (other clients read wave_status). DEFECT 2 — inlineAsk rows do not survive re-renders: the inline reason row (from the recent prompt() replacement) disappears when a background revision render rebuilds the run-detail overlay (reproduced: opened Reject reason row, waited ~15s, row vanished with typed state lost). Make open inline-ask state survive: hold it in client state keyed by run id + action and re-render it (with its typed text preserved) after any rebuild, or suppress rebuilding the affordance region while open — whichever matches the codebase's revision-gated render pattern; add a test if the harness allows asserting it. CONSTRAINTS: app files are TS template literals — doubled escapes, no backticks or dollar-brace, h() only, never innerHTML; composed-page parse test stays green; cite files individually.\nVerified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability","type":"bug_fix","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["delegated-run","kage-run:fix-two-app-client-defects-found-by-oper-260821-cdaa"],"paths":["mcp/audit-driven-parity-pass.test.ts","mcp/delegation/app-client.ts","mcp/fix-two-app-client.test.ts","mcp/sessions-ui.test.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-21T08:32:29.461Z"}],"context":{"fact":"api.ts's withWaveStatus ships goal.wave_status as a TOP-LEVEL array on the goal object (one entry per plan.wave, shape {status: 'merged'|'executing'|'due'|'waiting'|'partial', failed_run_ids?}), derived by goal.ts's goalWaveStatus — never a field on the wave object itself. app-client.ts's waveGateStatus previously read a wave.status.due boolean that no server code ever produced, meaning the due-wave banner and Dispatch wave button had never rendered for any goal since inception.","verification":"npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-21T08:32:29.461Z","path_fingerprints":[{"path":"mcp/audit-driven-parity-pass.test.ts","sha256":"19db7ef5028d1a26b80960046113a8030733a305ab6307e3daaa561c69e0201b","size":13791},{"path":"mcp/delegation/app-client.ts","sha256":"d0a0da1c3d3ca06e406bdc09b2cf71dee565c1e5b3ebb74c052298fea6362c25","size":225599},{"path":"mcp/fix-two-app-client.test.ts","sha256":"e005f7874488f490967ea950b6db861c3eb005236dfe34fb9b61e635d3e0490d","size":16859,"symbols":[{"name":"text","kind":"constant","sha256":"e8b272c2095b4f8425c7586633e61915e7047745dad938de92030c0fb6691d80"},{"name":"wave","kind":"constant","sha256":"196c3c3a271d8709d09a4abf444448b8d5eb1fc1d7f8af86b87c660947508c25"}]},{"path":"mcp/sessions-ui.test.ts","sha256":"090f43a454c53072d86aee0dc8cb627fb29221310ada5710f256c340fc747b71","size":45649,"symbols":[{"name":"rows","kind":"constant","sha256":"47160f4cea4a1d46971c11cb91aefc94092275d9eb21db2f71316bb90de08226"},{"name":"text","kind":"constant","sha256":"e8b272c2095b4f8425c7586633e61915e7047745dad938de92030c0fb6691d80"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":600,"unresolved_symbols":["noEmit"]},"created_at":"2026-08-21T08:32:29.461Z","updated_at":"2026-08-21T08:54:05.190Z","author_branch":"kage/fix-two-app-client-defects-found-by-oper-260821-cdaa"}
```

