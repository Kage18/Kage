---
type: "Decision"
title: "mcp test files compiled to dist/ have __dirname = mcp/dist/ at runtime; reaching mcp/ s..."
description: "mcp test files compiled to dist/ have dirname = mcp/dist/ at runtime; reaching mcp/ sibling sources needs join dirname, '..', ... and repo root files need join dirname, '..', '..', ... — same convention already used in m"
resource: "README.md"
tags: ["delegated-run", "kage-run:make-the-product-describe-itself-correct-260818-76da"]
timestamp: "2026-08-20T12:41:14.488Z"
x-kage-id: "repo:make-the-product-describe-itself-correct-260818-76da:decision:mcp-test-files-compiled-to-dist-have-dirname-mcp-dist-at-runtime-reaching-mcp-s-"
x-kage-type: "decision"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["README.md", "mcp/cli.ts", "mcp/package.json", "mcp/positioning.test.ts", "plugin/.claude-plugin/plugin.json", "plugin/.codex-plugin/plugin.json"]
---

# mcp test files compiled to dist/ have __dirname = mcp/dist/ at runtime; reaching mcp/ s...

> mcp test files compiled to dist/ have dirname = mcp/dist/ at runtime; reaching mcp/ sibling sources needs join dirnam…

mcp test files compiled to dist/ have __dirname = mcp/dist/ at runtime; reaching mcp/ sibling sources needs join(__dirname, '..', ...) and repo-root files need join(__dirname, '..', '..', ...) — same convention already used in mcp/release.test.ts's 'kernel's index' and 'every command' tests.

Learned while delivering: Make the product describe itself correctly. Every storefront currently sells a memory library; the product is an orchestrator that manages your memory and your agents.

THE OWNER'S LINE, to be used VERBATIM as the lead everywhere: "Kage manages your memory and agents"
Do not rewrite it, do not improvise a cleverer version, do not expand it into a slogan. It is the owner's own wording and it is the decision. Your job is to place it and to write the supporting sentence under it.

WHAT EVERY SURFACE SAYS TODAY (measured — all memory-only, none mentions managing agents):
- mcp/cli.ts:157, CORE_USAGE header: "Kage — repo memory, and an orchestrator that uses it"
- README.md headline: "Verified memory for coding agents — a Google OKF bundle, kept true to your code"
- mcp/package.json description (what npm shows): "Verified memory for coding agents, stored as a Google Open Knowledge Format (OKF) bundle and checked against your code. The verification layer OKF leaves out. MCP server, no account, no API key."
- server.json description: "Git-native, verified memory for coding agents: shared via git, kept true to your code."
- plugin/.claude-plugin/plugin.json AND plugin/.codex-plugin/plugin.json description (identical): "A framework for collaborative agent memory. Recall git-native, team-shared repo knowledge before acting, capture durable learnings as reviewable files in your repo, and get warned when your diff invalidates a memory. Every packet is checked against your code; deleted and stale citations are caught."

WHAT TO CHANGE — each of the five, leading with the owner's line, then ONE supporting sentence sized to the surface:
The supporting sentence must say what Kage actually does now, in this order of emphasis: it briefs and runs agents for you (in isolated worktrees, one run or a multi-wave goal), it re-runs the checks itself rather than trusting the agent's report, and the memory is what makes each brief smarter than the last. Write it plainly. Do not use the words "revolutionary", "seamless", "powerful", or "AI-powered", and do not compare to named competitors in shipped copy.

SPECIFIC GUIDANCE PER SURFACE:
- mcp/cli.ts CORE_USAGE header: one line. This is the first thing a terminal user reads.
- README.md: the headline under the banner, plus check the first paragraph beneath it still follows from the new headline — if it opens by explaining memory storage, reorder so the loop (state an intent -> Kage briefs and runs an agent -> Kage checks the work -> merge lands code and learning) comes first and memory's role follows. Keep it short; do not rewrite the whole README.
- mcp/package.json: this is the npm listing. Lead line plus one sentence. KEEP the factual "MCP server, no account, no API key" — that is a real differentiator and users search for it.
- server.json and both plugin manifests: same lead, one sentence, consistent with each other.

ON OKF: keep any FACTUAL statement that memory is stored as OKF-conformant markdown — that is true and it is a no-lock-in guarantee worth stating. Remove it from the POSITIONING. It is the file format, not the pitch, and it should never be the first thing a reader learns about Kage. There is an existing memory packet recording that exact decision from 2026-08-05; do not reverse it.

DO NOT overclaim. Everything in the new copy must already be true of the shipped product: briefs compiled from repo memory, runs in isolated git worktrees, kernel-executed checks, merge that lands code and ratifies learnings, single runs and multi-wave goals, a desktop app, an MCP server. Do not describe team/multi-user features, cloud, or anything gated behind work that has not shipped.

WATCH THE VERSION GUARD: mcp/release.test.ts has a lockstep test asserting server.json, both plugin manifests and shell/package.json all match mcp/package.json's version. You are editing descriptions, not versions — do not touch any version field, and make sure that test still passes.

TESTS — new file mcp/positioning.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). Cover: the CLI's CORE_USAGE header contains the owner's line; every one of the five manifest/README surfaces leads with it; no shipped description leads with OKF; and the existing version-lockstep test still passes. Name in your claim which test fails if the change is reverted.

CONSTRAINTS: CommonJS — no import.meta (TS1470), no top-level await. Do NOT touch mcp/index.ts — another run is editing it right now. Do not touch mcp/delegation/reachability.ts or mcp/delegation/trackrecord.ts.
VERIFY: npm run test --prefix mcp (708 green before your change — use the "npm run test" form) and npm run build --prefix mcp with no "error TS". Run them for real and fill in the claim fence.
Verified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability

## Verification

npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability

# Citations

[1] explicit_capture (2026-08-18T18:58:30.716Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:make-the-product-describe-itself-correct-260818-76da:decision:mcp-test-files-compiled-to-dist-have-dirname-mcp-dist-at-runtime-reaching-mcp-s-","title":"mcp test files compiled to dist/ have __dirname = mcp/dist/ at runtime; reaching mcp/ s...","summary":"mcp test files compiled to dist/ have dirname = mcp/dist/ at runtime; reaching mcp/ sibling sources needs join dirname, '..', ... and repo root files need join dirname, '..', '..', ... — same convention already used in m","body":"mcp test files compiled to dist/ have __dirname = mcp/dist/ at runtime; reaching mcp/ sibling sources needs join(__dirname, '..', ...) and repo-root files need join(__dirname, '..', '..', ...) — same convention already used in mcp/release.test.ts's 'kernel's index' and 'every command' tests.\n\nLearned while delivering: Make the product describe itself correctly. Every storefront currently sells a memory library; the product is an orchestrator that manages your memory and your agents.\n\nTHE OWNER'S LINE, to be used VERBATIM as the lead everywhere: \"Kage manages your memory and agents\"\nDo not rewrite it, do not improvise a cleverer version, do not expand it into a slogan. It is the owner's own wording and it is the decision. Your job is to place it and to write the supporting sentence under it.\n\nWHAT EVERY SURFACE SAYS TODAY (measured — all memory-only, none mentions managing agents):\n- mcp/cli.ts:157, CORE_USAGE header: \"Kage — repo memory, and an orchestrator that uses it\"\n- README.md headline: \"Verified memory for coding agents — a Google OKF bundle, kept true to your code\"\n- mcp/package.json description (what npm shows): \"Verified memory for coding agents, stored as a Google Open Knowledge Format (OKF) bundle and checked against your code. The verification layer OKF leaves out. MCP server, no account, no API key.\"\n- server.json description: \"Git-native, verified memory for coding agents: shared via git, kept true to your code.\"\n- plugin/.claude-plugin/plugin.json AND plugin/.codex-plugin/plugin.json description (identical): \"A framework for collaborative agent memory. Recall git-native, team-shared repo knowledge before acting, capture durable learnings as reviewable files in your repo, and get warned when your diff invalidates a memory. Every packet is checked against your code; deleted and stale citations are caught.\"\n\nWHAT TO CHANGE — each of the five, leading with the owner's line, then ONE supporting sentence sized to the surface:\nThe supporting sentence must say what Kage actually does now, in this order of emphasis: it briefs and runs agents for you (in isolated worktrees, one run or a multi-wave goal), it re-runs the checks itself rather than trusting the agent's report, and the memory is what makes each brief smarter than the last. Write it plainly. Do not use the words \"revolutionary\", \"seamless\", \"powerful\", or \"AI-powered\", and do not compare to named competitors in shipped copy.\n\nSPECIFIC GUIDANCE PER SURFACE:\n- mcp/cli.ts CORE_USAGE header: one line. This is the first thing a terminal user reads.\n- README.md: the headline under the banner, plus check the first paragraph beneath it still follows from the new headline — if it opens by explaining memory storage, reorder so the loop (state an intent -> Kage briefs and runs an agent -> Kage checks the work -> merge lands code and learning) comes first and memory's role follows. Keep it short; do not rewrite the whole README.\n- mcp/package.json: this is the npm listing. Lead line plus one sentence. KEEP the factual \"MCP server, no account, no API key\" — that is a real differentiator and users search for it.\n- server.json and both plugin manifests: same lead, one sentence, consistent with each other.\n\nON OKF: keep any FACTUAL statement that memory is stored as OKF-conformant markdown — that is true and it is a no-lock-in guarantee worth stating. Remove it from the POSITIONING. It is the file format, not the pitch, and it should never be the first thing a reader learns about Kage. There is an existing memory packet recording that exact decision from 2026-08-05; do not reverse it.\n\nDO NOT overclaim. Everything in the new copy must already be true of the shipped product: briefs compiled from repo memory, runs in isolated git worktrees, kernel-executed checks, merge that lands code and ratifies learnings, single runs and multi-wave goals, a desktop app, an MCP server. Do not describe team/multi-user features, cloud, or anything gated behind work that has not shipped.\n\nWATCH THE VERSION GUARD: mcp/release.test.ts has a lockstep test asserting server.json, both plugin manifests and shell/package.json all match mcp/package.json's version. You are editing descriptions, not versions — do not touch any version field, and make sure that test still passes.\n\nTESTS — new file mcp/positioning.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). Cover: the CLI's CORE_USAGE header contains the owner's line; every one of the five manifest/README surfaces leads with it; no shipped description leads with OKF; and the existing version-lockstep test still passes. Name in your claim which test fails if the change is reverted.\n\nCONSTRAINTS: CommonJS — no import.meta (TS1470), no top-level await. Do NOT touch mcp/index.ts — another run is editing it right now. Do not touch mcp/delegation/reachability.ts or mcp/delegation/trackrecord.ts.\nVERIFY: npm run test --prefix mcp (708 green before your change — use the \"npm run test\" form) and npm run build --prefix mcp with no \"error TS\". Run them for real and fill in the claim fence.\nVerified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability","type":"decision","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["delegated-run","kage-run:make-the-product-describe-itself-correct-260818-76da"],"paths":["README.md","mcp/cli.ts","mcp/package.json","mcp/positioning.test.ts","plugin/.claude-plugin/plugin.json","plugin/.codex-plugin/plugin.json"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-18T18:58:30.716Z"}],"context":{"fact":"mcp test files compiled to dist/ have __dirname = mcp/dist/ at runtime; reaching mcp/ sibling sources needs join(__dirname, '..', ...) and repo-root files need join(__dirname, '..', '..', ...) — same convention already used in mcp/release.test.ts's 'kernel's index' and 'every command' tests.","verification":"npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-20T12:41:14.488Z","path_fingerprints":[{"path":"README.md","sha256":"578d70c03862796d17d631b9ab3093626b1846d2e379ad6a21ab64ee18a3177f","size":16230},{"path":"mcp/cli.ts","sha256":"f49dea5e35b66b1309c62c71f90a8b0a6986061d7956f3417bcb330139cd14e3","size":150054,"symbols":[{"name":"core_usage","kind":"constant","sha256":"1699ce759d2feb154c644317efd80fa472559b13a884ddc293defc65013a7f7c"},{"name":"index","kind":"constant","sha256":"c201ee64cf24065dd2ba0bbdc7ff8399e5c1dd89783820e501d28f82019f08a8"},{"name":"files","kind":"constant","sha256":"8984fb1011c0e9e4a2f8140ee1b80011ee39c80fba2fb0d6012223d8cc01fbfb"},{"name":"command","kind":"constant","sha256":"9e594169e1f8559f50d7e73b407f1aa3a9a0f14bf43a676fedefa72734badb98"},{"name":"json","kind":"constant","sha256":"9115381310c6d4c5ecb25a7e67e4fd0bb4b20a9b328adb25b606065454f79370"},{"name":"agent","kind":"constant","sha256":"5b9caa614311fe691d6af171b9a8985b0d49464b9df178ad28ff5b9883eb4cf2"},{"name":"from","kind":"constant","sha256":"b386d829bcacbd9e216c726f6dff71fc7d8a53aade383fa45b850387414b780c"},{"name":"learning","kind":"constant","sha256":"63a6bc653564dc277072e3fbe66b4c0437da63c9baf29b68d209ab4efc61383f"},{"name":"durable","kind":"constant","sha256":"59e1af86ee722f58562da0478f6f3fe6621ae96f9473de8c0037e63fe779215e"},{"name":"intent","kind":"constant","sha256":"cc8274cd5bd6f2bc477398f01c00c9a3cb3a3b034466e3e61cfa5679ae6040a9"},{"name":"runs","kind":"constant","sha256":"f7d055af62b25c4cb13983f9c81fa5b35c09b64c06279e258694f6f7b180485a"},{"name":"test","kind":"constant","sha256":"c67f3770bf3c3e9b9aff73d99406c7664a900a970c68cfe75a1efefbd4447185"}]},{"path":"mcp/package.json","sha256":"1f7f8529aec287d4c40afaa6ada0fbb936b7648da507ea9c197428faaddad40a","size":1551},{"path":"mcp/positioning.test.ts","sha256":"8d2f0af69b19dd4b8822f768967da447dcc5bee755b9358a0e4a8496524dbe0d","size":5607,"symbols":[{"name":"headline","kind":"constant","sha256":"7e0e2d6834fc5dc03c43f9dfd5a88adfac9f4dd86e1b5c4ef8d0c4a83b4ca0ce"},{"name":"claude","kind":"constant","sha256":"70d5d8e899edb04c1fb9ff361fb1c634bb2cae95c97e3033d3c3474a99b22227"},{"name":"codex","kind":"constant","sha256":"c6779e15324711b009b7a70c211def287513f12e77ca509f36980833661200b4"},{"name":"server","kind":"constant","sha256":"62140a6d785f9b67fb594858d41a2b8f66d393625d4f02dde98030c03b1f597f"},{"name":"leads","kind":"constant","sha256":"cb9b9214cf96a1e1b53cde2856e8b95351fc6b8ac03ae197c2b506bee0ec71fb"},{"name":"version","kind":"constant","sha256":"0b68263b920d93c8de4f1b2d0b85c90f412bd9ab2f312fa86bfc88522a8997e1"},{"name":"manifests","kind":"constant","sha256":"de74ce60f4a6b6dfe34ea9c559fc091074bc50ba7b3037910a4a0fe663598669"}]},{"path":"plugin/.claude-plugin/plugin.json","sha256":"34488149c7e42e63b6ecbe6bc4dac3b072c2b4e14ad7a565c2705818b589d467","size":698},{"path":"plugin/.codex-plugin/plugin.json","sha256":"054f10c0009c5855fc0336bc2225a8fa40ef3e72db37ad34e2b79c84bf43646a","size":357}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":4000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":1301,"unresolved_symbols":["noEmit"],"reverified_at":"2026-08-20T12:41:14.488Z","stale":true,"stale_reasons":["linked path changed since memory was verified: README.md"],"suggested_action":"update"},"created_at":"2026-08-18T18:58:30.716Z","updated_at":"2026-08-21T08:54:05.764Z","author_branch":"kage/make-the-product-describe-itself-correct-260818-76da"}
```

