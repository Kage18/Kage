---
type: "Decision"
title: "Writable.write()'s boolean return is a backpressure/highWaterMark signal (\"you may want..."
description: "Writable.write 's boolean return is a backpressure/highWaterMark signal \"you may want to wait for 'drain' before writing more\" , not a delivery/success signal — treating it as delivery is a live bug pattern anywhere a sc"
resource: "mcp/delegation/supervisor.ts"
tags: ["delegated-run", "kage-run:the-interaction-substrate-run-1-of-3-bac-260818-4077"]
timestamp: "2026-08-20T12:41:55.793Z"
x-kage-id: "repo:the-interaction-substrate-run-1-of-3-bac-260818-4077:decision:writable-write-s-boolean-return-is-a-backpressure-highwatermark-signal-you-may-w"
x-kage-type: "decision"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/delegation/supervisor.ts"]
---

# Writable.write()'s boolean return is a backpressure/highWaterMark signal ("you may want...

> Writable.write 's boolean return is a backpressure/highWaterMark signal "you may want to wait for 'drain' before writ…

Writable.write()'s boolean return is a backpressure/highWaterMark signal ("you may want to wait for 'drain' before writing more"), not a delivery/success signal — treating it as delivery is a live bug pattern anywhere a scripted or slow-draining child sits behind a stdin pipe, since a normal, successful, queued write can still return false.

Learned while delivering: THE INTERACTION SUBSTRATE (run 1 of 3, backend only — a parallel run owns pty work and a later run owns all UI; to avoid merge conflicts DO NOT touch room-pty.ts, app-client.ts, app-html.ts, app-styles.ts). Build full Conductor-parity interaction plumbing in the kernel and API. PART A, steer queue with per-message truth: today steer.jsonl lines are bare {message} appended unconditionally, so queued-vs-delivered cannot be answered. Give each steer a record {id (short random), message, at, status: queued|delivered, delivered_at?}: steerRun (mcp/delegation/steer.ts) writes status queued and flips to delivered (atomic temp+rename rewrite of the file, matching task.json crash-safety) when live-socket delivery succeeds; superviseRun (mcp/delegation/supervisor.ts) marks a steer delivered at the moment it injects it (both the held-socket path and the reattach-resume path — the resume path currently redelivers only the newest stored steer; upgrade it to deliver ALL queued steers in order, oldest first, now that delivery is tracked). Keep readSteers in mcp/delegation/dispatch.ts returning plain messages for brief re-rendering (compat), add readSteerRecords beside it. Kernel queue mutations in steer.ts: editQueuedSteer(projectDir, runId, steerId, message), deleteQueuedSteer(projectDir, runId, steerId), reorderQueuedSteers(projectDir, runId, orderedIds) — each refuses (returns ok:false with a reason) for delivered records; delivered history is immutable. PART B, plan review as a typed object: extend the reporting protocol in mcp/delegation/contract.ts with a third fence, kage-plan: {plan: string, question?: string} — parseReportFence gains kind plan (plan required non-empty; degrade to null like the others); CLAIM_PROTOCOL_INSTRUCTIONS documents when to emit it (before large or risky changes, propose the plan and wait for approval). superviseRun on a plan fence: transition blocked with waiting_on {needs: plan approval, detail: <the plan text>} — an explicit tell (the approval or revision) resumes through the existing blocked machinery; never auto-approve. PART C, true interrupt: POST /runs/:id/interrupt in mcp/delegation/api.ts — sendControl interrupt to the live supervisor socket (mcp/delegation/control.ts has the frame), honest 503 {error: no live supervisor} when isRunLive is false; the session stays alive. PART D, queue API: GET /runs/:id/steers returns the records; POST /runs/:id/steers with {op: edit|delete|reorder, id?, message?, order?} maps to the kernel mutations, guarded like every mutation. Tests for every part in mcp/delegation.test.ts and mcp/delegation-api.test.ts using existing seams (part-1/part-2 supervisor seams, scripted spawnLive stub, real sockets, startApi harness): queue lifecycle queued→delivered with immutability refusals; reattach delivers ALL queued steers oldest-first and marks them; plan fence parses and lands blocked-with-plan and resumes on tell; interrupt route 503s honestly and delivers on a live socket; existing steer delivery-vocabulary tests keep passing. Success = npm test fully green.
Verified by: diff-size, citations

## Verification

diff-size, citations

# Citations

[1] explicit_capture (2026-08-18T05:26:50.078Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:the-interaction-substrate-run-1-of-3-bac-260818-4077:decision:writable-write-s-boolean-return-is-a-backpressure-highwatermark-signal-you-may-w","title":"Writable.write()'s boolean return is a backpressure/highWaterMark signal (\"you may want...","summary":"Writable.write 's boolean return is a backpressure/highWaterMark signal \"you may want to wait for 'drain' before writing more\" , not a delivery/success signal — treating it as delivery is a live bug pattern anywhere a sc","body":"Writable.write()'s boolean return is a backpressure/highWaterMark signal (\"you may want to wait for 'drain' before writing more\"), not a delivery/success signal — treating it as delivery is a live bug pattern anywhere a scripted or slow-draining child sits behind a stdin pipe, since a normal, successful, queued write can still return false.\n\nLearned while delivering: THE INTERACTION SUBSTRATE (run 1 of 3, backend only — a parallel run owns pty work and a later run owns all UI; to avoid merge conflicts DO NOT touch room-pty.ts, app-client.ts, app-html.ts, app-styles.ts). Build full Conductor-parity interaction plumbing in the kernel and API. PART A, steer queue with per-message truth: today steer.jsonl lines are bare {message} appended unconditionally, so queued-vs-delivered cannot be answered. Give each steer a record {id (short random), message, at, status: queued|delivered, delivered_at?}: steerRun (mcp/delegation/steer.ts) writes status queued and flips to delivered (atomic temp+rename rewrite of the file, matching task.json crash-safety) when live-socket delivery succeeds; superviseRun (mcp/delegation/supervisor.ts) marks a steer delivered at the moment it injects it (both the held-socket path and the reattach-resume path — the resume path currently redelivers only the newest stored steer; upgrade it to deliver ALL queued steers in order, oldest first, now that delivery is tracked). Keep readSteers in mcp/delegation/dispatch.ts returning plain messages for brief re-rendering (compat), add readSteerRecords beside it. Kernel queue mutations in steer.ts: editQueuedSteer(projectDir, runId, steerId, message), deleteQueuedSteer(projectDir, runId, steerId), reorderQueuedSteers(projectDir, runId, orderedIds) — each refuses (returns ok:false with a reason) for delivered records; delivered history is immutable. PART B, plan review as a typed object: extend the reporting protocol in mcp/delegation/contract.ts with a third fence, kage-plan: {plan: string, question?: string} — parseReportFence gains kind plan (plan required non-empty; degrade to null like the others); CLAIM_PROTOCOL_INSTRUCTIONS documents when to emit it (before large or risky changes, propose the plan and wait for approval). superviseRun on a plan fence: transition blocked with waiting_on {needs: plan approval, detail: <the plan text>} — an explicit tell (the approval or revision) resumes through the existing blocked machinery; never auto-approve. PART C, true interrupt: POST /runs/:id/interrupt in mcp/delegation/api.ts — sendControl interrupt to the live supervisor socket (mcp/delegation/control.ts has the frame), honest 503 {error: no live supervisor} when isRunLive is false; the session stays alive. PART D, queue API: GET /runs/:id/steers returns the records; POST /runs/:id/steers with {op: edit|delete|reorder, id?, message?, order?} maps to the kernel mutations, guarded like every mutation. Tests for every part in mcp/delegation.test.ts and mcp/delegation-api.test.ts using existing seams (part-1/part-2 supervisor seams, scripted spawnLive stub, real sockets, startApi harness): queue lifecycle queued→delivered with immutability refusals; reattach delivers ALL queued steers oldest-first and marks them; plan fence parses and lands blocked-with-plan and resumes on tell; interrupt route 503s honestly and delivers on a live socket; existing steer delivery-vocabulary tests keep passing. Success = npm test fully green.\nVerified by: diff-size, citations","type":"decision","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["delegated-run","kage-run:the-interaction-substrate-run-1-of-3-bac-260818-4077"],"paths":["mcp/delegation/supervisor.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-18T05:26:50.078Z"}],"context":{"fact":"Writable.write()'s boolean return is a backpressure/highWaterMark signal (\"you may want to wait for 'drain' before writing more\"), not a delivery/success signal — treating it as delivery is a live bug pattern anywhere a scripted or slow-draining child sits behind a stdin pipe, since a normal, successful, queued write can still return false.","verification":"diff-size, citations"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-20T12:41:55.793Z","path_fingerprints":[{"path":"mcp/delegation/supervisor.ts","sha256":"d8ed31b23d0078e3e8e44e233ec819b4b203ed5e0b1a47f1a4858c834f5bfd4f","size":43021,"symbols":[{"name":"superviserun","kind":"function","sha256":"97e6a6a3bcf1aed316113cd585eb865f5bc448c4c9cbcbedeadf145e4870b8ab"},{"name":"task","kind":"constant","sha256":"85f8a3124cd54be2d855713df25f16201af7f8bd479bd55ac37f1f399bd11753"},{"name":"status","kind":"constant","sha256":"6683e3695618df41ae034fe4367f65488053601ebcee570d2cd9702978330438"},{"name":"plan","kind":"constant","sha256":"5d3f838173f32509bd4b5a9d0b18b167eb5962c3444a843333b422f0ec3d3009"},{"name":"brief","kind":"constant","sha256":"d6f0c21082d08ce5ca0501e184cb30dff7dc321ae8ae3f48a93ed013d30ef12f"},{"name":"child","kind":"constant","sha256":"5721bb1c9d8a1466f95c83d918930a72466e963d14d7efa461c7d82cf4c57a4d"},{"name":"record","kind":"constant","sha256":"42b464544b1bbb3bece105cafedc2bb1d102343181ac99a5f20ce3461a94ce34"},{"name":"stdin","kind":"constant","sha256":"b05ebe1a401554387e825521f0e1a196bd2a9eb5150e8fc9177490e21ca2c14f"},{"name":"lines","kind":"constant","sha256":"5f197d0245e4b15b6dc5b3e32bf249b2b95b701c002512a7ca7e015e8f461b6e"},{"name":"signal","kind":"constant","sha256":"58cf9dbc0b422f6562f67838df8679182ff3f26f0e871a7bd2368007ac08130b"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":4000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":868,"unresolved_symbols":["highWaterMark","delivered_at","steerRun","readSteers","editQueuedSteer","deleteQueuedSteer","reorderQueuedSteers","orderedIds"],"reverified_at":"2026-08-20T12:41:55.793Z"},"created_at":"2026-08-18T05:26:50.078Z","updated_at":"2026-08-20T12:41:55.793Z","author_branch":"kage/the-interaction-substrate-run-1-of-3-bac-260818-4077"}
```

