---
type: "Bug Fix"
title: "Tests that spawn child Node processes to exercise verify-lock.ts must explicitly `delet..."
description: "Tests that spawn child Node processes to exercise verify lock.ts must explicitly delete process.env.KAGE VERIFY LOCK HELD both in the spawned child's own script AND, for any direct same process acquireVerifyLock/runComma"
resource: "mcp/delegation/verify-lock.ts"
tags: ["delegated-run", "kage-run:critical-reentrancy-bug-in-the-just-merg-260821-6451"]
timestamp: "2026-08-21T11:40:50.813Z"
x-kage-id: "repo:critical-reentrancy-bug-in-the-just-merg-260821-6451:bug_fix:tests-that-spawn-child-node-processes-to-exercise-verify-lock-ts-must-explicitly"
x-kage-type: "bug_fix"
x-kage-status: "pending"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/delegation/verify-lock.ts", "mcp/delegation/verify.ts", "mcp/serialize-kernel-verifications-per.test.ts", "mcp/verify-lock-reentrancy.test.ts"]
---

# Tests that spawn child Node processes to exercise verify-lock.ts must explicitly `delet...

> Tests that spawn child Node processes to exercise verify lock.ts must explicitly delete process.env.KAGE VERIFY LOCK …

Tests that spawn child Node processes to exercise verify-lock.ts must explicitly `delete process.env.KAGE_VERIFY_LOCK_HELD` (both in the spawned child's own script AND, for any direct same-process acquireVerifyLock/runCommandCheck call within the test itself) because the test-runner process may itself be running nested inside a real kernel verification that already stamped the marker into its env — otherwise the test's deliberately-separate KAGE_VERIFY_LOCK_PATH override gets short-circuited by the inherited marker and the test silently proves nothing (returns waitedMs 0 / no steal, always).

Learned while delivering: Critical reentrancy bug in the just-merged verification lock (mcp/delegation/verify-lock.ts + verify.ts): spawnWithTreeKill acquires the per-machine lock for EVERY executed command, so the kernel's outer verification holds the default lock for the entire npm-test run — and any test inside that suite whose code path reaches spawnWithTreeKill without overriding KAGE_VERIFY_LOCK_PATH blocks forever on its own ancestor's lock (holder pid is alive, so the steal path never fires) until the 1200s tree-kill. This is what failed run visual-foundation-pass-on-the-app-s-room-260821-be77: tests.log exit 124 after 1200s while the agent's own inner suite (no outer lock held) ran green. Fix by environment-token reentrancy: when acquireVerifyLock actually acquires (not skipped, not stolen), export a held marker into child processes — spawnWithTreeKill already builds the child env, add KAGE_VERIFY_LOCK_HELD=1 there — and acquireVerifyLock returns immediately as 'inherited' when that marker is present, because a nested execution is part of the same serialized unit; record 'lock inherited from parent verification' in the evidence note instead of a wait. The existing lock tests must keep testing REAL acquisition: their spawned child scripts already set KAGE_VERIFY_LOCK_PATH — have them also delete KAGE_VERIFY_LOCK_HELD in the child env so the nested-marker never leaks into them from the test runner itself. Add one regression test: a stub verification executed INSIDE an environment where KAGE_VERIFY_LOCK_HELD=1 completes immediately without waiting on a held default lock. Keep the module dependency-free; suite green; cite files individually.
Verified by: diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability

## Verification

diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability

# Citations

[1] explicit_capture (2026-08-21T11:40:50.813Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:critical-reentrancy-bug-in-the-just-merg-260821-6451:bug_fix:tests-that-spawn-child-node-processes-to-exercise-verify-lock-ts-must-explicitly","title":"Tests that spawn child Node processes to exercise verify-lock.ts must explicitly `delet...","summary":"Tests that spawn child Node processes to exercise verify lock.ts must explicitly delete process.env.KAGE VERIFY LOCK HELD both in the spawned child's own script AND, for any direct same process acquireVerifyLock/runComma","body":"Tests that spawn child Node processes to exercise verify-lock.ts must explicitly `delete process.env.KAGE_VERIFY_LOCK_HELD` (both in the spawned child's own script AND, for any direct same-process acquireVerifyLock/runCommandCheck call within the test itself) because the test-runner process may itself be running nested inside a real kernel verification that already stamped the marker into its env — otherwise the test's deliberately-separate KAGE_VERIFY_LOCK_PATH override gets short-circuited by the inherited marker and the test silently proves nothing (returns waitedMs 0 / no steal, always).\n\nLearned while delivering: Critical reentrancy bug in the just-merged verification lock (mcp/delegation/verify-lock.ts + verify.ts): spawnWithTreeKill acquires the per-machine lock for EVERY executed command, so the kernel's outer verification holds the default lock for the entire npm-test run — and any test inside that suite whose code path reaches spawnWithTreeKill without overriding KAGE_VERIFY_LOCK_PATH blocks forever on its own ancestor's lock (holder pid is alive, so the steal path never fires) until the 1200s tree-kill. This is what failed run visual-foundation-pass-on-the-app-s-room-260821-be77: tests.log exit 124 after 1200s while the agent's own inner suite (no outer lock held) ran green. Fix by environment-token reentrancy: when acquireVerifyLock actually acquires (not skipped, not stolen), export a held marker into child processes — spawnWithTreeKill already builds the child env, add KAGE_VERIFY_LOCK_HELD=1 there — and acquireVerifyLock returns immediately as 'inherited' when that marker is present, because a nested execution is part of the same serialized unit; record 'lock inherited from parent verification' in the evidence note instead of a wait. The existing lock tests must keep testing REAL acquisition: their spawned child scripts already set KAGE_VERIFY_LOCK_PATH — have them also delete KAGE_VERIFY_LOCK_HELD in the child env so the nested-marker never leaks into them from the test runner itself. Add one regression test: a stub verification executed INSIDE an environment where KAGE_VERIFY_LOCK_HELD=1 completes immediately without waiting on a held default lock. Keep the module dependency-free; suite green; cite files individually.\nVerified by: diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability","type":"bug_fix","scope":"repo","visibility":"team","sensitivity":"internal","status":"pending","confidence":0.7,"tags":["delegated-run","kage-run:critical-reentrancy-bug-in-the-just-merg-260821-6451"],"paths":["mcp/delegation/verify-lock.ts","mcp/delegation/verify.ts","mcp/serialize-kernel-verifications-per.test.ts","mcp/verify-lock-reentrancy.test.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-21T11:40:50.813Z"}],"context":{"fact":"Tests that spawn child Node processes to exercise verify-lock.ts must explicitly `delete process.env.KAGE_VERIFY_LOCK_HELD` (both in the spawned child's own script AND, for any direct same-process acquireVerifyLock/runCommandCheck call within the test itself) because the test-runner process may itself be running nested inside a real kernel verification that already stamped the marker into its env — otherwise the test's deliberately-separate KAGE_VERIFY_LOCK_PATH override gets short-circuited by the inherited marker and the test silently proves nothing (returns waitedMs 0 / no steal, always).","verification":"diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-21T11:40:50.813Z","path_fingerprints":[{"path":"mcp/delegation/verify-lock.ts","sha256":"2b61b9736b35838928ae10f249e969f0137e4aa377f1a2291095733ad3f22338","size":7167,"symbols":[{"name":"acquireverifylock","kind":"function","sha256":"81cf06b5a24189dd20f5de437d0fc6c76fbefaf5719c592e432e270ccc713831"}]},{"path":"mcp/delegation/verify.ts","sha256":"e2b994d7b4bafa84ee67db23246dda325a6fa01ea42772a7776125ab61864efd","size":29052,"symbols":[{"name":"spawnwithtreekill","kind":"function","sha256":"149fc5492c023344ec768a2b37e35c0084d814025d28404bbf654c4423892355"},{"name":"lock","kind":"constant","sha256":"292b79081e6908fb487a8c6bbd866ab8bb7105de53241e8e11fba224ef79a9e1"},{"name":"path","kind":"constant","sha256":"83c2238b8ba558b62d6620118338401da219426c2a75ca1481aa2f18ec551d1e"},{"name":"runcommandcheck","kind":"function","sha256":"89950ba1e91d11b1ad95ff22931cba6dee5e362d8b6cbfd25eeb3631d19fd3ed"},{"name":"spawned","kind":"constant","sha256":"d41e3b705a71b37378e54876374054ce480aefc4493113a71e26e4b2a2b1b904"},{"name":"token","kind":"constant","sha256":"30f37849cd0e90e2322c756bf267ebd55c70f52a5ca4d35956df9c9ea3206502"},{"name":"direct","kind":"constant","sha256":"602210f18f34e3816cbdcc85cf2f8e0a49e1cf48373002c40a2ef9aa72fa7689"},{"name":"files","kind":"constant","sha256":"78f3e429f44325f7479571d6918ab610db233ba388e71a89dd5241b86a603f84"},{"name":"executed","kind":"constant","sha256":"aeb60ebecdbc628ac6c05e4052a34713594ec50b08341a6e8830a1b40e6d7dd9"}]},{"path":"mcp/serialize-kernel-verifications-per.test.ts","sha256":"afed83810854626782533cda381b57c72cfcddeb9b6c4f432d36c1b6fd98ec9f","size":11987,"symbols":[{"name":"child","kind":"constant","sha256":"053cb8a56b660478ceade381ca34454debb0af6f7d16fb36967bad666f54d01a"},{"name":"holder","kind":"constant","sha256":"5177439d826a377f370a627635a74174b7b53212a84a7577257a5f1b18cd0809"}]},{"path":"mcp/verify-lock-reentrancy.test.ts","sha256":"22be9935c75144dcdd374141ae27ad6d1cbe4ce44469cc028d264dbf660c07a2","size":4410,"symbols":[{"name":"holder","kind":"constant","sha256":"5177439d826a377f370a627635a74174b7b53212a84a7577257a5f1b18cd0809"},{"name":"acquisition","kind":"constant","sha256":"de9220e70acc6386dd03e384b1e499dd73b3b0422618a4c4307d6e3fc42c9334"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"stale_reasons":[],"estimated_tokens_saved":591,"unresolved_symbols":["noEmit"]},"created_at":"2026-08-21T11:40:50.813Z","updated_at":"2026-08-21T11:40:51.084Z","author_branch":"kage/critical-reentrancy-bug-in-the-just-merg-260821-6451"}
```

