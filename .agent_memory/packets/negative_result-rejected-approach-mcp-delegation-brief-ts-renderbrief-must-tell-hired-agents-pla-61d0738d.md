---
type: "Negative Result"
title: "Rejected approach: mcp/delegation/brief.ts renderBrief must tell hired agents plainly that"
description: "A delegated attempt at \"mcp/delegation/brief.ts renderBrief must tell hired agents plainly that they never run repo harness tools — kage refresh, kage learn, kage pr check are the operator's job, not something the agent "
tags: ["delegated-run", "rejected", "kage-run:mcp-delegation-brief-ts-renderbrief-must-260818-6762"]
timestamp: "2026-08-18T09:58:02.461Z"
x-kage-id: "repo:https-github-com-kage-core-kage:negative_result:rejected-approach-mcp-delegation-brief-ts-renderbrief-must-tell-hired-agents-pla"
x-kage-type: "negative_result"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
---

# Rejected approach: mcp/delegation/brief.ts renderBrief must tell hired agents plainly that

> A delegated attempt at "mcp/delegation/brief.ts renderBrief must tell hired agents plainly that they never run repo h…

A delegated attempt at "mcp/delegation/brief.ts renderBrief must tell hired agents plainly that they never run repo harness tools — kage_refresh, kage_learn, kage_pr_check are the operator's job, not something the agent should request permission for. Agents' learnings reach memory through the learned[] field of their claim fence, which merge-ratification promotes. Add this as a clear line near the reporting protocol in the rendered brief text, plus a matching scoped note in CLAUDE.md so hired agents reading repo instructions see it too. Agents keep blocking mid-run to ask for kage_refresh/kage_learn/kage_pr_check permissions, costing a full round trip each time. Scope your edits to mcp/delegation/brief.ts and CLAUDE.md; add tests to mcp/delegation.test.ts in a clearly separated block near the END of the file (a companion run is adding unrelated tests to the same file in parallel — minimize hunk overlap so both can merge cleanly)." was rejected.

Reason: Superseded: the manager re-dispatched this same intent (in-mcp-delegation-brief-ts-ren...) while this one was stuck. This run had already produced its work and a final message, but its reattached supervisor died during verification, leaving it in verifying with no process. Keeping one live attempt instead of two. Manager behavior worth noting: it has now dispatched duplicate runs for the same intent in both orchestrated waves.

Claimed: renderBrief in mcp/delegation/brief.ts now emits a '## You never run repo harness tools' section immediately before the kage-claim-v1 reporting protocol, telling hired agents kage_refresh/kage_learn/kage_pr_check are the operator's job and that learnings flow through the claim fence's learned[] field; CLAUDE.md gained a matching '## For Hired/Delegated Agents' section with the same guidance; two new tests were added in a clearly separated block at the end of mcp/delegation.test.ts verifying both.
Branch kept for inspection: kage/mcp-delegation-brief-ts-renderbrief-must-260818-6762

# Citations

[1] explicit_capture (2026-08-18T09:58:02.461Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:https-github-com-kage-core-kage:negative_result:rejected-approach-mcp-delegation-brief-ts-renderbrief-must-tell-hired-agents-pla","title":"Rejected approach: mcp/delegation/brief.ts renderBrief must tell hired agents plainly that","summary":"A delegated attempt at \"mcp/delegation/brief.ts renderBrief must tell hired agents plainly that they never run repo harness tools — kage refresh, kage learn, kage pr check are the operator's job, not something the agent ","body":"A delegated attempt at \"mcp/delegation/brief.ts renderBrief must tell hired agents plainly that they never run repo harness tools — kage_refresh, kage_learn, kage_pr_check are the operator's job, not something the agent should request permission for. Agents' learnings reach memory through the learned[] field of their claim fence, which merge-ratification promotes. Add this as a clear line near the reporting protocol in the rendered brief text, plus a matching scoped note in CLAUDE.md so hired agents reading repo instructions see it too. Agents keep blocking mid-run to ask for kage_refresh/kage_learn/kage_pr_check permissions, costing a full round trip each time. Scope your edits to mcp/delegation/brief.ts and CLAUDE.md; add tests to mcp/delegation.test.ts in a clearly separated block near the END of the file (a companion run is adding unrelated tests to the same file in parallel — minimize hunk overlap so both can merge cleanly).\" was rejected.\n\nReason: Superseded: the manager re-dispatched this same intent (in-mcp-delegation-brief-ts-ren...) while this one was stuck. This run had already produced its work and a final message, but its reattached supervisor died during verification, leaving it in verifying with no process. Keeping one live attempt instead of two. Manager behavior worth noting: it has now dispatched duplicate runs for the same intent in both orchestrated waves.\n\nClaimed: renderBrief in mcp/delegation/brief.ts now emits a '## You never run repo harness tools' section immediately before the kage-claim-v1 reporting protocol, telling hired agents kage_refresh/kage_learn/kage_pr_check are the operator's job and that learnings flow through the claim fence's learned[] field; CLAUDE.md gained a matching '## For Hired/Delegated Agents' section with the same guidance; two new tests were added in a clearly separated block at the end of mcp/delegation.test.ts verifying both.\nBranch kept for inspection: kage/mcp-delegation-brief-ts-renderbrief-must-260818-6762","type":"negative_result","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["delegated-run","rejected","kage-run:mcp-delegation-brief-ts-renderbrief-must-260818-6762"],"paths":[],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-18T09:58:02.461Z"}],"context":{"fact":"A delegated attempt at \"mcp/delegation/brief.ts renderBrief must tell hired agents plainly that they never run repo harness tools — kage_refresh, kage_learn, kage_pr_check are the operator's job, not something the agent should request permission for. Agents' learnings reach memory through the learned[] field of their claim fence, which merge-ratification promotes. Add this as a clear line near the reporting protocol in the rendered brief text, plus a matching scoped note in CLAUDE.md so hired agents reading repo instructions see it too. Agents keep blocking mid-run to ask for kage_refresh/kage_learn/kage_pr_check permissions, costing a full round trip each time. Scope your edits to mcp/delegation/brief.ts and CLAUDE.md; add tests to mcp/delegation.test.ts in a clearly separated block near the END of the file (a companion run is adding unrelated tests to the same file in parallel — minimize hunk overlap so both can merge cleanly).\" was rejected."},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-18T09:58:02.461Z","path_fingerprints":[],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":2000,"discovery_tokens_estimated":true,"score":74,"reasons":["high-value memory type","has source evidence","tagged","actionable rationale or verification"],"risks":["not grounded to paths"],"duplicate_candidates":[],"stale_reasons":[],"estimated_tokens_saved":499},"created_at":"2026-08-18T09:58:02.461Z","updated_at":"2026-08-18T09:58:02.461Z","author_branch":"release-prep"}
```

