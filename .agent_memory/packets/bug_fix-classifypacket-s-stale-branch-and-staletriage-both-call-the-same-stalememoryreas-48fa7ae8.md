---
type: "Bug Fix"
title: "classifyPacket()'s 'stale' branch and staleTriage() both call the same staleMemoryReaso..."
description: "classifyPacket 's 'stale' branch and staleTriage both call the same staleMemoryReasons check, but on different packet sets/pre processing: staleTriage iterates loadApprovedPackets with raw unpruned packet.paths, while re"
resource: "mcp/delegation/app-client.ts"
tags: ["delegated-run", "kage-run:the-memory-view-s-health-numbers-are-fro-260819-88da"]
timestamp: "2026-08-19T09:42:18.142Z"
x-kage-id: "repo:the-memory-view-s-health-numbers-are-fro-260819-88da:bug_fix:classifypacket-s-stale-branch-and-staletriage-both-call-the-same-stalememoryreas"
x-kage-type: "bug_fix"
x-kage-status: "pending"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/delegation/app-client.ts", "mcp/delegation/memory-view.ts", "mcp/kernel.ts", "mcp/metrics-freshness.test.ts"]
---

# classifyPacket()'s 'stale' branch and staleTriage() both call the same staleMemoryReaso...

> classifyPacket 's 'stale' branch and staleTriage both call the same staleMemoryReasons check, but on different packet…

classifyPacket()'s 'stale' branch and staleTriage() both call the same staleMemoryReasons() check, but on different packet sets/pre-processing: staleTriage iterates loadApprovedPackets() with raw (unpruned) packet.paths, while refreshPacketStaleness (called inside refreshProject) iterates all packetsDir entries after prunePacketGroundingPaths() strips .kageignore'd paths first — so a packet whose only staleness reason is a kageignored path shows as stale under kage stale but not under refresh's staleness pass. This is intentional per an existing regression test, not something to 'fix' into agreement.

Learned while delivering: The Memory view's health numbers are frozen, and the command that promises to refresh them does not. Found by using the app, not by reading code.

WHAT A USER SEES: the app's Memory tab leads with a health strip — active, stale, avg quality, evidence-backed, used recently, never recalled. Right now it reads "8 stale". The packet store on disk has 92 packets with quality.stale === true, and `kage stale` reports 142 needing a human call. Three surfaces, three numbers, same word. The one the user reads first is the most wrong, and it is wrong by an order of magnitude.

ROOT CAUSE, confirmed by grep: .agent_memory/metrics.json is written in exactly TWO places in mcp/kernel.ts — inside gcProject (around line 9565) and inside compactProject (around line 9730). refreshProject (around line 9432) never writes it. So metrics.json only updates when someone runs `kage gc` or `kage compact`, which are maintenance commands a normal user never runs. On this repo the file is stamped 2026-08-17 while the work happened on 08-18 and 08-19.

THIS IS A BROKEN PROMISE, not just a stale cache: `kage refresh`'s own help line reads "rebuild indexes, graphs and metrics". It exits 0, prints "Refreshed <dir>", reports packets indexed and both graphs rebuilt — and silently does not do the third thing it advertises. I ran it and watched generated_at stay at 2026-08-17.

Note for whoever fixes this: do NOT assume the cause is quiet-refresh. Refresh on a non-default branch prints "Quiet refresh (non-default branch): packet metadata not rewritten on disk" — that behaviour is intentional and must be preserved (it avoids merge conflicts from metadata-only packet rewrites). I initially blamed it and was wrong. metrics.json is not packet metadata and its absence is unrelated: refreshProject simply never writes it on any branch.

WHAT TO BUILD:
1. Have refreshProject write metrics.json, using the same call the other two sites use — buildGraphIndexes then writeJson(join(memoryRoot(projectDir), "metrics.json"), kageMetricsShallow(projectDir, rebuilt)). refreshProject already rebuilds the graphs, so reuse that result rather than rebuilding a second time; check what it already has in hand before adding work.
2. Make sure this still happens on a quiet (non-default-branch) refresh. Metrics are a derived report, not packet metadata — freezing them on a feature branch is exactly the bug being fixed. If there is a reason they must be skipped, say so in your claim rather than silently skipping.
3. While you are there: the health strip's "stale" and `kage stale`'s count should not disagree. Read both definitions and either make them the same number or label them differently in the app so a user is not comparing two things called "stale". State which you chose and why. Do not change what `kage stale` counts — it is the triage surface and its number is the actionable one.

TESTS — new file mcp/metrics-freshness.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). Cover: refreshProject writes metrics.json where none existed; it updates an existing stale one (assert generated_at moves); the totals it writes match a live count of the packet store rather than a cached figure; and a quiet refresh still updates metrics while still not rewriting packet metadata. Name in your claim which test fails if the change is reverted.

CONSTRAINTS: CommonJS — no import.meta (TS1470), no top-level await. mcp/kernel.ts is large — make a surgical change, do not restructure it. Do not touch mcp/delegation/reachability.ts.
VERIFY: npm run test --prefix mcp (754 green before your change — use the "npm run test" form) and npm run build --prefix mcp with no "error TS". Run them for real and fill in the claim fence.
Verified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability

## Verification

npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability

# Citations

[1] explicit_capture (2026-08-19T09:42:18.142Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:the-memory-view-s-health-numbers-are-fro-260819-88da:bug_fix:classifypacket-s-stale-branch-and-staletriage-both-call-the-same-stalememoryreas","title":"classifyPacket()'s 'stale' branch and staleTriage() both call the same staleMemoryReaso...","summary":"classifyPacket 's 'stale' branch and staleTriage both call the same staleMemoryReasons check, but on different packet sets/pre processing: staleTriage iterates loadApprovedPackets with raw unpruned packet.paths, while re","body":"classifyPacket()'s 'stale' branch and staleTriage() both call the same staleMemoryReasons() check, but on different packet sets/pre-processing: staleTriage iterates loadApprovedPackets() with raw (unpruned) packet.paths, while refreshPacketStaleness (called inside refreshProject) iterates all packetsDir entries after prunePacketGroundingPaths() strips .kageignore'd paths first — so a packet whose only staleness reason is a kageignored path shows as stale under kage stale but not under refresh's staleness pass. This is intentional per an existing regression test, not something to 'fix' into agreement.\n\nLearned while delivering: The Memory view's health numbers are frozen, and the command that promises to refresh them does not. Found by using the app, not by reading code.\n\nWHAT A USER SEES: the app's Memory tab leads with a health strip — active, stale, avg quality, evidence-backed, used recently, never recalled. Right now it reads \"8 stale\". The packet store on disk has 92 packets with quality.stale === true, and `kage stale` reports 142 needing a human call. Three surfaces, three numbers, same word. The one the user reads first is the most wrong, and it is wrong by an order of magnitude.\n\nROOT CAUSE, confirmed by grep: .agent_memory/metrics.json is written in exactly TWO places in mcp/kernel.ts — inside gcProject (around line 9565) and inside compactProject (around line 9730). refreshProject (around line 9432) never writes it. So metrics.json only updates when someone runs `kage gc` or `kage compact`, which are maintenance commands a normal user never runs. On this repo the file is stamped 2026-08-17 while the work happened on 08-18 and 08-19.\n\nTHIS IS A BROKEN PROMISE, not just a stale cache: `kage refresh`'s own help line reads \"rebuild indexes, graphs and metrics\". It exits 0, prints \"Refreshed <dir>\", reports packets indexed and both graphs rebuilt — and silently does not do the third thing it advertises. I ran it and watched generated_at stay at 2026-08-17.\n\nNote for whoever fixes this: do NOT assume the cause is quiet-refresh. Refresh on a non-default branch prints \"Quiet refresh (non-default branch): packet metadata not rewritten on disk\" — that behaviour is intentional and must be preserved (it avoids merge conflicts from metadata-only packet rewrites). I initially blamed it and was wrong. metrics.json is not packet metadata and its absence is unrelated: refreshProject simply never writes it on any branch.\n\nWHAT TO BUILD:\n1. Have refreshProject write metrics.json, using the same call the other two sites use — buildGraphIndexes then writeJson(join(memoryRoot(projectDir), \"metrics.json\"), kageMetricsShallow(projectDir, rebuilt)). refreshProject already rebuilds the graphs, so reuse that result rather than rebuilding a second time; check what it already has in hand before adding work.\n2. Make sure this still happens on a quiet (non-default-branch) refresh. Metrics are a derived report, not packet metadata — freezing them on a feature branch is exactly the bug being fixed. If there is a reason they must be skipped, say so in your claim rather than silently skipping.\n3. While you are there: the health strip's \"stale\" and `kage stale`'s count should not disagree. Read both definitions and either make them the same number or label them differently in the app so a user is not comparing two things called \"stale\". State which you chose and why. Do not change what `kage stale` counts — it is the triage surface and its number is the actionable one.\n\nTESTS — new file mcp/metrics-freshness.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). Cover: refreshProject writes metrics.json where none existed; it updates an existing stale one (assert generated_at moves); the totals it writes match a live count of the packet store rather than a cached figure; and a quiet refresh still updates metrics while still not rewriting packet metadata. Name in your claim which test fails if the change is reverted.\n\nCONSTRAINTS: CommonJS — no import.meta (TS1470), no top-level await. mcp/kernel.ts is large — make a surgical change, do not restructure it. Do not touch mcp/delegation/reachability.ts.\nVERIFY: npm run test --prefix mcp (754 green before your change — use the \"npm run test\" form) and npm run build --prefix mcp with no \"error TS\". Run them for real and fill in the claim fence.\nVerified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability","type":"bug_fix","scope":"repo","visibility":"team","sensitivity":"internal","status":"pending","confidence":0.7,"tags":["delegated-run","kage-run:the-memory-view-s-health-numbers-are-fro-260819-88da"],"paths":["mcp/delegation/app-client.ts","mcp/delegation/memory-view.ts","mcp/kernel.ts","mcp/metrics-freshness.test.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-19T09:42:18.142Z"}],"context":{"fact":"classifyPacket()'s 'stale' branch and staleTriage() both call the same staleMemoryReasons() check, but on different packet sets/pre-processing: staleTriage iterates loadApprovedPackets() with raw (unpruned) packet.paths, while refreshPacketStaleness (called inside refreshProject) iterates all packetsDir entries after prunePacketGroundingPaths() strips .kageignore'd paths first — so a packet whose only staleness reason is a kageignored path shows as stale under kage stale but not under refresh's staleness pass. This is intentional per an existing regression test, not something to 'fix' into agreement.","verification":"npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-19T09:42:18.142Z","path_fingerprints":[{"path":"mcp/delegation/app-client.ts","sha256":"f3cb5257394b5ee80620e82cac1367c771be72180e644374289fc414e3d32f36","size":134786},{"path":"mcp/delegation/memory-view.ts","sha256":"090e63abddc1483b0e94fba009547aa577d618ddef7e48e92d21db779dfeb1da","size":12618,"symbols":[{"name":"metrics","kind":"constant","sha256":"150f578852656b9ae8c5cd157564d7ada9960f7dd40e44d72051a00fe3866b3d"},{"name":"packets","kind":"constant","sha256":"22dac1dd9c77c83107e5604a45ade76ff32cda65f3d4ac6d0d97883d7bc3ebe3"},{"name":"totals","kind":"constant","sha256":"d3578ef06f37f219814422ceac206a446e9751cc59bef6928f3a29ded1962b1c"},{"name":"quality","kind":"constant","sha256":"a81039172319ef6ea8f29ebc7f2f925502261821cf024a138eb399a76bb7a104"},{"name":"counts","kind":"constant","sha256":"31c35297ac5d28efe773e44deac97331d73cfaf9a43a5fabdfe72a510aaa74fa"},{"name":"match","kind":"constant","sha256":"f5a40465934c11612083973740125458cacbd0eb0d085bfec5611b2567ab082e"},{"name":"packetsdir","kind":"constant","sha256":"bf454052f6268678a54aff6c31217fe3c88abad3decf28a24adc627ca097e7db"},{"name":"file","kind":"constant","sha256":"5086c05641dfa674796f399e04ca3d8058f1d121560d180e9571c5b180754a4f"},{"name":"path","kind":"constant","sha256":"104c28156b6426a330e50a5f79847c070375f731fe6a5f8e25a009b1db36ce33"},{"name":"fence","kind":"constant","sha256":"14c4efcc67e9c27f8fcc6283d58a0095d88d8c99d3f203766aab75fe97bb43c7"},{"name":"runs","kind":"constant","sha256":"60b920541e9d8e774c466227ac2e7b4dc314721e4534a6d26c8fa5ba993323db"},{"name":"result","kind":"constant","sha256":"463ad7b9468c616b8fb3bbbb7d969325a833910cb3bd2e6db86ca70e005fb401"}]},{"path":"mcp/kernel.ts","sha256":"d6ce5f1e1d598079c516cccba85908de97daeb1fca8605a7f285901881664c39","size":898614,"symbols":[{"name":"memoryroot","kind":"function","sha256":"32e8d7b4a96aeb6656f3995b9e666a7d9267115a46dd9a3a2e38828292575189"},{"name":"packetsdir","kind":"function","sha256":"8b388e60c192db5ed3cd7301005caee1986cf243b2554ee254c3acca5afc9da2"},{"name":"writejson","kind":"function","sha256":"d27a80deafd04b4e7bbefe5632874bdcffb6773ac63f0d506037178108f5b076"},{"name":"time","kind":"constant","sha256":"36aa0a901e7fda47d7d7571d9c21879432386e4418ed56010d7e03c9a91f5482"},{"name":"order","kind":"constant","sha256":"db30b7ccb07d7f43001699200ace64babea1129360233ab863e59e09e926ef2c"},{"name":"verified","kind":"constant","sha256":"9e1998eeb03a854663c4ce2fa27bc0dde75922738f56430c157d33ea3ab8d3b8"},{"name":"size","kind":"constant","sha256":"9008cc27791958971fe1d1c0049dd1e46d083d1bcf756d40e903520337322226"},{"name":"stalememoryreasons","kind":"function","sha256":"4e884b72e777070ffbcd049a35b2c0b92bc6363aec7e3dbdbf850898872fdf1f"},{"name":"classifypacket","kind":"function","sha256":"6ec67d853184e841c22b077b61b92e636ea37d070a4931b77371bde4cab22beb"},{"name":"loadapprovedpackets","kind":"function","sha256":"ec90d02db5233af1aef3463a144153d16130ca67e1c9a1dc1ff5d8c7df1ca9ca"},{"name":"same","kind":"constant","sha256":"b150fada949a5c6d4babe0a0bc108765c6e7e1819d0ddbee55112b9c9a708447"},{"name":"prunepacketgroundingpaths","kind":"function","sha256":"ff9a84d615ed6e7eac98fc290124f6afd255de9dda5846ee0feeaf817e8c0a1f"},{"name":"call","kind":"constant","sha256":"0e1296678bffa8037b9dc66bffb9dbec82218bdd1cd66c11e341879266eb9454"},{"name":"compact","kind":"constant","sha256":"c9b1c12cc7a86cc1cd5e8fe0a5d8f5153a50e841cc6135962452153780c8557f"},{"name":"level","kind":"constant","sha256":"832bbcc09db2b0a822fe5b0f77875aea29528b0343bffcb1c71cf2fe73b9aa5e"},{"name":"buildgraphindexes","kind":"function","sha256":"371c684de127d4df344b5eed762dc2799c8833baa3bee6d33112c64c6136c48b"},{"name":"refreshpacketstaleness","kind":"function","sha256":"558186658c76f6e351401469ca8da343b7f6abfe0c48a5b6091c089f663d10f2"},{"name":"refreshproject","kind":"function","sha256":"955d063a189eb4ed6d3626e6a0fc13b7b69c6c6f32a4339461f7d1f9b62d224c"},{"name":"quiet","kind":"constant","sha256":"b0960735ee347c47ea2422888dd7ebfe019320d8af2f0b1ad395c8ef19dd9f6d"},{"name":"gcproject","kind":"function","sha256":"dcfdfd3ec2c13406436cb13f2cd45ace4ae0c3ed060e0692a7888ab0999220fd"},{"name":"compactproject","kind":"function","sha256":"638328f9016462ba2e4b01329c155150068d6478dddd832faa6793cc9b04a1ac"},{"name":"code","kind":"constant","sha256":"64b81d42a4c6de11c6ff891787a63a33c198717dba5a924932817e97a8d1f7cf"},{"name":"json","kind":"constant","sha256":"f7bcd9f5ef8a5696c0f0ecbd148a4db30b52b674922455ec67a7561c86be4da4"},{"name":"fence","kind":"constant","sha256":"9fb69bce5a7c9e07a7d7bef5df29aba19e65861ab16d4cff75ea3f0f1ff4d978"},{"name":"citations","kind":"constant","sha256":"78b7b49fddee7f5495108e631981c2962515ed3d581135bfa9b536e2b3f30e9d"},{"name":"read","kind":"constant","sha256":"ffe49534fbcdb7c556f32fa5120c3dd4c00fce60f148c94f79d0d94d71145efd"},{"name":"used","kind":"constant","sha256":"8d494a3272de91027985cda665364b9004e227897e62b1a7ad4810337f40141e"},{"name":"health","kind":"constant","sha256":"33eb541d6a88b3aa5afe334db526bd0fe151b5c53bb07be1ae6eee2b0b9c9e14"},{"name":"graphs","kind":"constant","sha256":"5483925183f5f71c1681f32dbf4b745a68dc9fc52cb81abcc8410e5ad02b3dac"},{"name":"right","kind":"constant","sha256":"806b35b647603a1db642ad7d81c94f9c2b8304fad2eef27ffdcf862017e687c7"},{"name":"active","kind":"constant","sha256":"9f7bde308adc0c5d9cbba74f1c67a9e7f730e67037fee0682772d6443a410c94"},{"name":"before","kind":"constant","sha256":"70c005bc3586ccb93799dfa1989dccca69b847916fd3fde1583578730922c50c"}]},{"path":"mcp/metrics-freshness.test.ts","sha256":"fef2280acce38f95c9ddf218b4c1f0d3c514ebbd1ecff609345716fed3e43dd9","size":6348,"symbols":[{"name":"frozen","kind":"constant","sha256":"272e4b8fe8af656b54671b2119ba7e1c1f53cfcd28e9efb6b5545e6c35a4f78f"},{"name":"refreshed","kind":"constant","sha256":"7dc1a388ec16b09ca755c5c30294e6557afa56017366557cf2c256980a9bdaa0"},{"name":"first","kind":"constant","sha256":"1147595fcd0e531c22f8ee997a010964eeb86566f1dbd6b9e7e7c2eb23672375"},{"name":"second","kind":"constant","sha256":"a9364ed8fb2d82206701978186d51edd2bd4aefb501703921ef88c71264dc13b"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"stale_reasons":[],"estimated_tokens_saved":1133,"unresolved_symbols":["noEmit"]},"created_at":"2026-08-19T09:42:18.142Z","updated_at":"2026-08-19T09:42:18.310Z","author_branch":"kage/the-memory-view-s-health-numbers-are-fro-260819-88da"}
```

