---
type: "Bug Fix"
title: "transitionRun (contract.ts) already appends every state change's note to the run ledger..."
description: "transitionRun contract.ts already appends every state change's note to the run ledger via appendRunLedger as a side effect of the transition itself — a budget halt or any reason passed to transitionRun's note argument is"
resource: "mcp/delegation/adapters/stub.ts"
tags: ["delegated-run", "kage-run:enforce-the-per-run-budget-it-is-display-260818-1166"]
timestamp: "2026-08-20T12:41:53.466Z"
x-kage-id: "repo:enforce-the-per-run-budget-it-is-display-260818-1166:bug_fix:transitionrun-contract-ts-already-appends-every-state-changes-note-to-the-run-le"
x-kage-type: "bug_fix"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/delegation/adapters/stub.ts", "mcp/delegation/app-client.ts", "mcp/delegation/contract.ts", "mcp/delegation/supervisor.ts", "mcp/delegation/tui/app.ts", "mcp/run-budget.test.ts"]
---

# transitionRun (contract.ts) already appends every state change's note to the run ledger...

> transitionRun contract.ts already appends every state change's note to the run ledger via appendRunLedger as a side e…

transitionRun (contract.ts) already appends every state change's note to the run ledger via appendRunLedger as a side effect of the transition itself — a budget-halt (or any) reason passed to transitionRun's `note` argument is automatically ledger-recorded with no separate write needed.

Learned while delivering: Enforce the per-run budget. It is displayed on every run and enforced on none.

MEASURED: run build-a-stale-memory-triage-surface-do-n-260818-ec2c recorded spend.usd_est 3.4124 against budgets.usd 2 — 70% over its own cap — and nothing stopped it, warned about it, or noted it. It also ran 13.2 minutes against budgets.minutes 30, so the minutes cap was never tested; the dollar one simply does not exist as a control.

WHERE IT STANDS: budgets are written onto every run record (budgets: {usd, minutes, diff_lines}) and shown per-run in the app. diff_lines IS enforced — it is a real check in the claim. usd and minutes are not: grep of mcp/delegation/dispatch.ts and mcp/delegation/supervisor.ts finds no usd comparison anywhere. Budget refusal was built for GOAL attachment only (checkGoalAcceptsNewRun), so a run dispatched outside a goal — which is most runs — has an unenforced cap displayed next to it.

WHY THIS MATTERS: a displayed limit that does not hold is worse than no limit, because the user reads it as a control. This product's whole position is that it reports honestly; a fake cap is the same class of defect as a claim that says VERIFIED when nothing ran.

WHAT TO BUILD — pick the honest option and justify it in your claim:
(a) ENFORCE. The supervisor already sees token usage as the run streams (tokens_used is on the record and updated during the run). Convert that to an estimated dollar figure with the same rate the rest of the product uses — there is an existing KAGE_USD_PER_MTOK env var and a default documented in the gains output; find it and reuse it, do not invent a second rate. When estimated spend crosses budgets.usd, stop the run the same way a stop already works, record the reason on the run ledger, and land it in a state that makes clear it was halted on budget rather than failed on its work. A halted run must still keep its worktree and its partial diff — the user may well want to merge or resume it.
(b) STOP CALLING IT A BUDGET. If enforcement mid-run is genuinely not feasible with the data available, rename the field to what it is (an estimate, or a target) everywhere it is written and displayed, and say plainly in the app that it is not a cap.
Prefer (a). Choose (b) only if you find a concrete reason (a) cannot work, and state that reason.

WHATEVER YOU CHOOSE, also make the overrun VISIBLE after the fact: a run whose final estimated spend exceeded its budget must show that on its receipt and in the app's run detail, not silently. That is the minimum honest behaviour even if enforcement lands later.

CONSIDER the minutes cap in the same change if it is cheap — the same "is this run over its stated limit" question, the same honest reporting. If it is not cheap, leave it and say so.

TESTS — new file mcp/run-budget.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). Cover: a run whose estimated spend crosses budgets.usd is halted (or, under option b, is never described as capped); the halt reason is recorded and names the limit and the actual figure; a run under budget is untouched; the worktree and diff survive a budget halt; and a completed run that exceeded its budget reports the overrun on its receipt. Name in your claim which test fails if the change is reverted.

CONSTRAINTS: CommonJS — no import.meta (TS1470), no top-level await. Do NOT touch mcp/index.ts, mcp/delegation/manager-client.ts, mcp/delegation/manager-prompt.ts, mcp/delegation/room-supervisor.ts, mcp/cli.ts or README.md — two other runs are editing those right now.
VERIFY: npm run test --prefix mcp (708 green before your change — use the "npm run test" form) and npm run build --prefix mcp with no "error TS". Run them for real and fill in the claim fence.
Verified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability

## Verification

npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability

# Citations

[1] explicit_capture (2026-08-18T19:38:45.189Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:enforce-the-per-run-budget-it-is-display-260818-1166:bug_fix:transitionrun-contract-ts-already-appends-every-state-changes-note-to-the-run-le","title":"transitionRun (contract.ts) already appends every state change's note to the run ledger...","summary":"transitionRun contract.ts already appends every state change's note to the run ledger via appendRunLedger as a side effect of the transition itself — a budget halt or any reason passed to transitionRun's note argument is","body":"transitionRun (contract.ts) already appends every state change's note to the run ledger via appendRunLedger as a side effect of the transition itself — a budget-halt (or any) reason passed to transitionRun's `note` argument is automatically ledger-recorded with no separate write needed.\n\nLearned while delivering: Enforce the per-run budget. It is displayed on every run and enforced on none.\n\nMEASURED: run build-a-stale-memory-triage-surface-do-n-260818-ec2c recorded spend.usd_est 3.4124 against budgets.usd 2 — 70% over its own cap — and nothing stopped it, warned about it, or noted it. It also ran 13.2 minutes against budgets.minutes 30, so the minutes cap was never tested; the dollar one simply does not exist as a control.\n\nWHERE IT STANDS: budgets are written onto every run record (budgets: {usd, minutes, diff_lines}) and shown per-run in the app. diff_lines IS enforced — it is a real check in the claim. usd and minutes are not: grep of mcp/delegation/dispatch.ts and mcp/delegation/supervisor.ts finds no usd comparison anywhere. Budget refusal was built for GOAL attachment only (checkGoalAcceptsNewRun), so a run dispatched outside a goal — which is most runs — has an unenforced cap displayed next to it.\n\nWHY THIS MATTERS: a displayed limit that does not hold is worse than no limit, because the user reads it as a control. This product's whole position is that it reports honestly; a fake cap is the same class of defect as a claim that says VERIFIED when nothing ran.\n\nWHAT TO BUILD — pick the honest option and justify it in your claim:\n(a) ENFORCE. The supervisor already sees token usage as the run streams (tokens_used is on the record and updated during the run). Convert that to an estimated dollar figure with the same rate the rest of the product uses — there is an existing KAGE_USD_PER_MTOK env var and a default documented in the gains output; find it and reuse it, do not invent a second rate. When estimated spend crosses budgets.usd, stop the run the same way a stop already works, record the reason on the run ledger, and land it in a state that makes clear it was halted on budget rather than failed on its work. A halted run must still keep its worktree and its partial diff — the user may well want to merge or resume it.\n(b) STOP CALLING IT A BUDGET. If enforcement mid-run is genuinely not feasible with the data available, rename the field to what it is (an estimate, or a target) everywhere it is written and displayed, and say plainly in the app that it is not a cap.\nPrefer (a). Choose (b) only if you find a concrete reason (a) cannot work, and state that reason.\n\nWHATEVER YOU CHOOSE, also make the overrun VISIBLE after the fact: a run whose final estimated spend exceeded its budget must show that on its receipt and in the app's run detail, not silently. That is the minimum honest behaviour even if enforcement lands later.\n\nCONSIDER the minutes cap in the same change if it is cheap — the same \"is this run over its stated limit\" question, the same honest reporting. If it is not cheap, leave it and say so.\n\nTESTS — new file mcp/run-budget.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). Cover: a run whose estimated spend crosses budgets.usd is halted (or, under option b, is never described as capped); the halt reason is recorded and names the limit and the actual figure; a run under budget is untouched; the worktree and diff survive a budget halt; and a completed run that exceeded its budget reports the overrun on its receipt. Name in your claim which test fails if the change is reverted.\n\nCONSTRAINTS: CommonJS — no import.meta (TS1470), no top-level await. Do NOT touch mcp/index.ts, mcp/delegation/manager-client.ts, mcp/delegation/manager-prompt.ts, mcp/delegation/room-supervisor.ts, mcp/cli.ts or README.md — two other runs are editing those right now.\nVERIFY: npm run test --prefix mcp (708 green before your change — use the \"npm run test\" form) and npm run build --prefix mcp with no \"error TS\". Run them for real and fill in the claim fence.\nVerified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability","type":"bug_fix","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["delegated-run","kage-run:enforce-the-per-run-budget-it-is-display-260818-1166"],"paths":["mcp/delegation/adapters/stub.ts","mcp/delegation/app-client.ts","mcp/delegation/contract.ts","mcp/delegation/supervisor.ts","mcp/delegation/tui/app.ts","mcp/run-budget.test.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-18T19:38:45.189Z"}],"context":{"fact":"a run whose final estimated spend exceeded its budget must show that on its receipt and in the app's run detail, not silently. That is the minimum honest behaviour even if enforcement lands later.\n\nCONSIDER the minutes cap in the same change if it is cheap — the same \"is this run over its stated limit\" question, the same honest reporting. If it is not cheap, leave it and say so.\n\nTESTS — new file mcp/run-budget.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). Cover: a run whose estimated spend crosses budgets.usd is halted (or, under option b, is never described as capped); the halt reason is recorded and names the limit and the actual figure; a run under budget is untouched; the worktree and diff survive a budget halt; and a completed run that exceeded its budget reports the overrun on its receipt. Name in your claim which test fails if the change is reverted.\n\nCONSTRAINTS: CommonJS — no import.meta (TS1470), no top-level await. Do NOT touch mcp/index.ts, mcp/delegation/manager-client.ts, mcp/delegation/manager-prompt.ts, mcp/delegation/room-supervisor.ts, mcp/cli.ts or README.md — two other runs are editing those right now.\nVERIFY: npm run test --prefix mcp (708 green before your change — use the \"npm run test\" form) and npm run build --prefix mcp with no \"error TS\". Run them for real and fill in the claim fence.","verification":"npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-20T12:41:53.466Z","path_fingerprints":[{"path":"mcp/delegation/adapters/stub.ts","sha256":"cdc84c417c788a131f15065defa6f6fb05cc431d8ab5b55d4290c07ffd1d0094","size":6853,"symbols":[{"name":"target","kind":"constant","sha256":"2e6b3e76423eea2b5c968ffc0586404163cedf8323926aa2a333459c6fbccb55"},{"name":"claim","kind":"constant","sha256":"602135d65f3b26dfe7f87092d358f9f34b0f33f12251207bbc0f8c75056ce468"}]},{"path":"mcp/delegation/app-client.ts","sha256":"ded26e7b2ea02db2b4eade9056358d995b0517b8f9cf1181245a867beb33bafb","size":206711},{"path":"mcp/delegation/contract.ts","sha256":"b00fed2868c1a4c64be39b8d82e4b49a7414c7f2eb1c9e6b5cd561c954f03891","size":50356,"symbols":[{"name":"needed","kind":"constant","sha256":"a3b91d52a820aa557b45d115cceaa93a3d3db344c5fbe63006f01df0cca0a3db"},{"name":"existing","kind":"constant","sha256":"c177225431249e7966d42bfa5252d1c401ad846abc24a52c7724d86613a61e08"},{"name":"prefix","kind":"constant","sha256":"d0c83c86f4e94d7a484b3325b54d1c90e1c6fc37b342a4cf7d3c33dd33dfb85a"},{"name":"appendrunledger","kind":"function","sha256":"a41b8494478aca0a6feeb8e3a4639b34e557106ada13960c73912fefa0ceee71"},{"name":"budget","kind":"constant","sha256":"384d19c5e4dcc60e64c4cba1aba25131d3930a3de3c394f0ee612f3d3a68873e"},{"name":"runs","kind":"constant","sha256":"aeed515a96dc123ddceac928842184b148f9bbd85f63bccbb0918926221c95e2"},{"name":"next","kind":"constant","sha256":"2714b791ab1cca4841a0c39ee5a1ca4fe5afced1e4735914dc3575b7817fc732"},{"name":"minutes","kind":"constant","sha256":"39c314b988d0b3d91761a0676f0210c72e20ddf208003edb41782a203e48aeb7"},{"name":"shown","kind":"constant","sha256":"578bbe2b18dd2af35fc139386daeba28b2d314996ff6d322c89304dc9fa9ce09"},{"name":"limit","kind":"constant","sha256":"a9b006660ef09f3f308a344d8688ceb1dbdbfb8db73c53e4ba92cbecb08e152e"},{"name":"transitionrun","kind":"function","sha256":"a7b4382bfc115668d6b8f9f3f087a5bdb1a77600634b8cff2aa122fbe58277fb"},{"name":"change","kind":"constant","sha256":"e499aa50ffbb9c260caec4bb421746f795f0062475fe49fbee7f283705f385e8"}]},{"path":"mcp/delegation/supervisor.ts","sha256":"d8ed31b23d0078e3e8e44e233ec819b4b203ed5e0b1a47f1a4858c834f5bfd4f","size":43021,"symbols":[{"name":"state","kind":"constant","sha256":"74ff1664f384b9305eedf989c0d85f9eb9f5142fd6d024b00630476f177a9c6e"},{"name":"record","kind":"constant","sha256":"42b464544b1bbb3bece105cafedc2bb1d102343181ac99a5f20ce3461a94ce34"},{"name":"check","kind":"constant","sha256":"c66ffea80735db586a3cb711454818ae97c9c05b55b9a22863f1be5bdcd90ff2"},{"name":"claim","kind":"constant","sha256":"503d03bbad627f38bd9f2f7b2dd08561cca0c09fbdcba09345502c785d52a736"}]},{"path":"mcp/delegation/tui/app.ts","sha256":"9cfd718cfa050f41e316077360e30237ce744f380409ca58ebdbffe0ac027e83","size":22761,"symbols":[{"name":"stale","kind":"constant","sha256":"483431b2aaec80a413e1c48d4e3f221eeea7831bca582351e7070f69b6d3967f"},{"name":"note","kind":"constant","sha256":"2084996f152953a81e2e51085c8025733200a8827e265837ea00caaa840c3aeb"},{"name":"diff","kind":"constant","sha256":"b6de11fe792100d939c304e211e21823ad8fa0fea9ccb90635283762aca5598f"},{"name":"rest","kind":"constant","sha256":"c2cf7105c75da01b6a1d80d8b6dd33a3141ffef90c243fbb0582a16340ff7d73"},{"name":"touch","kind":"function","sha256":"6ec04965f26ee2df1d13ad9ccd7c55ed2d08cbe6b12c161c5688de144aaad966"},{"name":"before","kind":"constant","sha256":"488e79ec69cdf9bb2422a10852fda8ce7e9af2f9af67cac96deaba78f8ed6a24"},{"name":"next","kind":"constant","sha256":"8ebbbf5b410d1e36c0b9b13adc5bd3397ae8452b00dabaeb8739589745f94650"}]},{"path":"mcp/run-budget.test.ts","sha256":"08932eb17f286ea81c464893f077eb86605d6f7ffff3c4777005cb54b9a72230","size":8330}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":1052,"unresolved_symbols":["KAGE_USD_PER_MTOK","noEmit"],"reverified_at":"2026-08-20T12:41:53.466Z","stale":true,"stale_reasons":["linked path changed since memory was verified: mcp/delegation/app-client.ts"],"suggested_action":"update"},"created_at":"2026-08-18T19:38:45.189Z","updated_at":"2026-08-20T20:13:09.639Z","author_branch":"kage/enforce-the-per-run-budget-it-is-display-260818-1166"}
```

