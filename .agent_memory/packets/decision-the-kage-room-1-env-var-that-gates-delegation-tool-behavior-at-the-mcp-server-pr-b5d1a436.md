---
type: "Decision"
title: "The KAGE_ROOM=1 env var that gates delegation-tool behavior at the MCP-server-process l..."
description: "The KAGE ROOM=1 env var that gates delegation tool behavior at the MCP server process level is set via writeRoomMcpConfig's mcp config env block mcp/delegation/room.ts , not by the spawning shell — both the persistent ro"
resource: "mcp/delegation/manager-client.ts"
tags: ["delegated-run", "kage-run:give-the-manager-the-tools-to-actually-r-260818-3c5c"]
timestamp: "2026-08-20T12:41:20.866Z"
x-kage-id: "repo:give-the-manager-the-tools-to-actually-r-260818-3c5c:decision:the-kage-room-1-env-var-that-gates-delegation-tool-behavior-at-the-mcp-server-pr"
x-kage-type: "decision"
x-kage-status: "deprecated"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "deprecated"
x-kage-paths: ["mcp/delegation/manager-client.ts", "mcp/delegation/manager-prompt.ts", "mcp/delegation/room-supervisor.ts", "mcp/index.ts", "mcp/manager-orchestration.test.ts"]
---

# The KAGE_ROOM=1 env var that gates delegation-tool behavior at the MCP-server-process l...

> The KAGE ROOM=1 env var that gates delegation tool behavior at the MCP server process level is set via writeRoomMcpCo…

The KAGE_ROOM=1 env var that gates delegation-tool behavior at the MCP-server-process level is set via writeRoomMcpConfig's --mcp-config env block (mcp/delegation/room.ts), not by the spawning shell — both the persistent room supervisor (superviseRoom) and the one-shot askManager fallback go through writeRoomMcpConfig, so the same KAGE_ROOM===1 gate used by kage_dispatch works correctly for both manager code paths.

Learned while delivering: Give the manager the tools to actually run a plan. Right now it is instructed to orchestrate and has no way to do it.

THE DEFECT, measured today: there are ZERO goal tools on the MCP surface — grep -c "kage_goal" mcp/index.ts returns 0 — and MANAGER_ALLOWED_TOOLS in mcp/delegation/manager-client.ts lists only kage_room_state, kage_events_since, kage_compile_brief, kage_dispatch, kage_task, kage_tell, kage_stop, kage_merge_run, kage_reject_run, kage_report, kage_judgment. Meanwhile manager-prompt.ts has an entire "Orchestrating a goal" section telling the manager it is the orchestrator, to decompose work into waves of parallel runs with disjoint file scopes, and to dispatch an approved wave. The manager is told to manage and given no instrument to manage with. A goal can only be created from the app's orchestrate shortcut today.

This is now the central gap, not a side one: the product's position is that Kage manages your work the way AO and Conductor do, with memory as what makes it better. A manager that cannot open a goal, see where it stands, or move it forward is a chat partner, not a manager.

WHAT TO BUILD — three MCP tools in mcp/index.ts, added to MANAGER_ALLOWED_TOOLS, each thin over the goal.ts functions that already exist. Do NOT add new goal semantics; the engine landed yesterday and works (state advances planning -> executing -> done from real run outcomes, autonomy governs auto-merge, files_scope disjointness and budgets are enforced at attach, waves fill first-open-first).

1. kage_goal_create — intent, optional plan (waves of run specs with intent/type/files_scope), optional autonomy ("recommend" default | "merge"), optional budgets. Wraps createGoal. Inside a room thread (KAGE_ROOM=1) it must also make the new goal that thread's ACTIVE goal via setActiveGoal, exactly as POST /goals in mcp/delegation/api.ts already does — that is what makes subsequent kage_dispatch calls attach automatically, and it is the mechanism that stops the manager from having to remember goal_id.
2. kage_goal_status — given a goal id, or defaulting to the thread's active goal, return the goal's state, its waves with each run's id and CURRENT state, spend against budget, and which wave is next to dispatch. This is the tool that lets the manager answer "where are we" without guessing, and it must read through the reconciling readGoal so a stale record heals on read.
3. kage_goal_finish — abandon a goal with a reason (wraps abandonGoal). Do NOT add a tool that forces a goal to "done": done is derived from its runs and must stay derived, never asserted. If the manager wants to stop a goal early, that is abandoning it, and the reason is worth keeping.

ALSO update mcp/delegation/manager-prompt.ts's "Orchestrating a goal" section to name these tools, so the instruction and the capability finally match. Keep it to a few lines in the existing voice — the section already explains the wave discipline, it just needs to say which tool opens a goal, which one reports on it, and that runs dispatched while a goal is active attach automatically.

WAVE ADVANCE — the point of the whole feature. The manager is already woken by "[kage event]" frames when a goal-owned run changes state (the wake loop is built and proven). Make the event that matters explicit: when the LAST run of a wave reaches a terminal state and the goal has a further wave planned, the frame the manager receives must say so plainly and name what is next — e.g. "wave 1 of 3 complete: 2 merged, 1 rejected. Wave 2 is next: <the wave's run specs>." Today the frame reports a single run's state change and leaves the manager to infer wave completion, which is exactly the inference it keeps getting wrong. Do this in the coalescing that already exists in mcp/delegation/room-supervisor.ts rather than inventing a second event path.

TESTS — new file mcp/manager-orchestration.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). Drive the tools through callTool(name, args) — mcp/index.ts exports it and the MCP-boundary tests already use it. Cover: kage_goal_create makes the goal and sets it active under KAGE_ROOM=1; a dispatch with no goal_id afterwards attaches to it; kage_goal_status reports each wave's runs with their current states and names the next wave; kage_goal_status on a goal whose runs all settled reports "done" (proving it reads through the reconciling path); kage_goal_finish abandons with the reason recorded; there is NO tool that can force a goal to done; and the wave-complete frame names the next wave's specs. Name in your claim which test fails if the change is reverted.

CONSTRAINTS: CommonJS — no import.meta (TS1470), no top-level await. Read mcp/delegation/goal.ts and mcp/delegation/api.ts's /goals routes FIRST and reuse them; the API already does most of this over HTTP and the MCP tools should not diverge from it. Do not touch mcp/delegation/reachability.ts or mcp/delegation/trackrecord.ts.
VERIFY: npm run test --prefix mcp (708 green before your change — use the "npm run test" form) and npm run build --prefix mcp with no "error TS". Run them for real and fill in the claim fence.
Verified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability

## Verification

npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability

# Citations

[1] explicit_capture (2026-08-18T18:58:43.424Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:give-the-manager-the-tools-to-actually-r-260818-3c5c:decision:the-kage-room-1-env-var-that-gates-delegation-tool-behavior-at-the-mcp-server-pr","title":"The KAGE_ROOM=1 env var that gates delegation-tool behavior at the MCP-server-process l...","summary":"The KAGE ROOM=1 env var that gates delegation tool behavior at the MCP server process level is set via writeRoomMcpConfig's mcp config env block mcp/delegation/room.ts , not by the spawning shell — both the persistent ro","body":"The KAGE_ROOM=1 env var that gates delegation-tool behavior at the MCP-server-process level is set via writeRoomMcpConfig's --mcp-config env block (mcp/delegation/room.ts), not by the spawning shell — both the persistent room supervisor (superviseRoom) and the one-shot askManager fallback go through writeRoomMcpConfig, so the same KAGE_ROOM===1 gate used by kage_dispatch works correctly for both manager code paths.\n\nLearned while delivering: Give the manager the tools to actually run a plan. Right now it is instructed to orchestrate and has no way to do it.\n\nTHE DEFECT, measured today: there are ZERO goal tools on the MCP surface — grep -c \"kage_goal\" mcp/index.ts returns 0 — and MANAGER_ALLOWED_TOOLS in mcp/delegation/manager-client.ts lists only kage_room_state, kage_events_since, kage_compile_brief, kage_dispatch, kage_task, kage_tell, kage_stop, kage_merge_run, kage_reject_run, kage_report, kage_judgment. Meanwhile manager-prompt.ts has an entire \"Orchestrating a goal\" section telling the manager it is the orchestrator, to decompose work into waves of parallel runs with disjoint file scopes, and to dispatch an approved wave. The manager is told to manage and given no instrument to manage with. A goal can only be created from the app's orchestrate shortcut today.\n\nThis is now the central gap, not a side one: the product's position is that Kage manages your work the way AO and Conductor do, with memory as what makes it better. A manager that cannot open a goal, see where it stands, or move it forward is a chat partner, not a manager.\n\nWHAT TO BUILD — three MCP tools in mcp/index.ts, added to MANAGER_ALLOWED_TOOLS, each thin over the goal.ts functions that already exist. Do NOT add new goal semantics; the engine landed yesterday and works (state advances planning -> executing -> done from real run outcomes, autonomy governs auto-merge, files_scope disjointness and budgets are enforced at attach, waves fill first-open-first).\n\n1. kage_goal_create — intent, optional plan (waves of run specs with intent/type/files_scope), optional autonomy (\"recommend\" default | \"merge\"), optional budgets. Wraps createGoal. Inside a room thread (KAGE_ROOM=1) it must also make the new goal that thread's ACTIVE goal via setActiveGoal, exactly as POST /goals in mcp/delegation/api.ts already does — that is what makes subsequent kage_dispatch calls attach automatically, and it is the mechanism that stops the manager from having to remember goal_id.\n2. kage_goal_status — given a goal id, or defaulting to the thread's active goal, return the goal's state, its waves with each run's id and CURRENT state, spend against budget, and which wave is next to dispatch. This is the tool that lets the manager answer \"where are we\" without guessing, and it must read through the reconciling readGoal so a stale record heals on read.\n3. kage_goal_finish — abandon a goal with a reason (wraps abandonGoal). Do NOT add a tool that forces a goal to \"done\": done is derived from its runs and must stay derived, never asserted. If the manager wants to stop a goal early, that is abandoning it, and the reason is worth keeping.\n\nALSO update mcp/delegation/manager-prompt.ts's \"Orchestrating a goal\" section to name these tools, so the instruction and the capability finally match. Keep it to a few lines in the existing voice — the section already explains the wave discipline, it just needs to say which tool opens a goal, which one reports on it, and that runs dispatched while a goal is active attach automatically.\n\nWAVE ADVANCE — the point of the whole feature. The manager is already woken by \"[kage event]\" frames when a goal-owned run changes state (the wake loop is built and proven). Make the event that matters explicit: when the LAST run of a wave reaches a terminal state and the goal has a further wave planned, the frame the manager receives must say so plainly and name what is next — e.g. \"wave 1 of 3 complete: 2 merged, 1 rejected. Wave 2 is next: <the wave's run specs>.\" Today the frame reports a single run's state change and leaves the manager to infer wave completion, which is exactly the inference it keeps getting wrong. Do this in the coalescing that already exists in mcp/delegation/room-supervisor.ts rather than inventing a second event path.\n\nTESTS — new file mcp/manager-orchestration.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). Drive the tools through callTool(name, args) — mcp/index.ts exports it and the MCP-boundary tests already use it. Cover: kage_goal_create makes the goal and sets it active under KAGE_ROOM=1; a dispatch with no goal_id afterwards attaches to it; kage_goal_status reports each wave's runs with their current states and names the next wave; kage_goal_status on a goal whose runs all settled reports \"done\" (proving it reads through the reconciling path); kage_goal_finish abandons with the reason recorded; there is NO tool that can force a goal to done; and the wave-complete frame names the next wave's specs. Name in your claim which test fails if the change is reverted.\n\nCONSTRAINTS: CommonJS — no import.meta (TS1470), no top-level await. Read mcp/delegation/goal.ts and mcp/delegation/api.ts's /goals routes FIRST and reuse them; the API already does most of this over HTTP and the MCP tools should not diverge from it. Do not touch mcp/delegation/reachability.ts or mcp/delegation/trackrecord.ts.\nVERIFY: npm run test --prefix mcp (708 green before your change — use the \"npm run test\" form) and npm run build --prefix mcp with no \"error TS\". Run them for real and fill in the claim fence.\nVerified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability","type":"decision","scope":"repo","visibility":"team","sensitivity":"internal","status":"deprecated","confidence":0.7,"tags":["delegated-run","kage-run:give-the-manager-the-tools-to-actually-r-260818-3c5c"],"paths":["mcp/delegation/manager-client.ts","mcp/delegation/manager-prompt.ts","mcp/delegation/room-supervisor.ts","mcp/index.ts","mcp/manager-orchestration.test.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-18T18:58:43.424Z"}],"context":{"fact":"The KAGE_ROOM=1 env var that gates delegation-tool behavior at the MCP-server-process level is set via writeRoomMcpConfig's --mcp-config env block (mcp/delegation/room.ts), not by the spawning shell — both the persistent room supervisor (superviseRoom) and the one-shot askManager fallback go through writeRoomMcpConfig, so the same KAGE_ROOM===1 gate used by kage_dispatch works correctly for both manager code paths.","verification":"npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-20T12:41:20.866Z","path_fingerprints":[{"path":"mcp/delegation/manager-client.ts","sha256":"4bbb5a304840532bef034a50e86535295093ecb9f791d4f0c3a8a3450bba8f94","size":15285,"symbols":[{"name":"manager_allowed_tools","kind":"constant","sha256":"997ccb87479d59b4685435bab0dd973ac3ae241e80dff3b42663b38dcef8ff1b"},{"name":"askmanager","kind":"function","sha256":"0a2bd48c7a3767c56a855cdf93563759f7cbff8e575f993848a639c9c354af55"},{"name":"claim","kind":"constant","sha256":"ef48de498403884d80f38315e6140d973a71ba2234c9875592308ddc96fe47fd"},{"name":"tools","kind":"constant","sha256":"f3130dc0cd643bb9401d3c3fed77e365efcb3bfa0a1c8f10f442945323102101"}]},{"path":"mcp/delegation/manager-prompt.ts","sha256":"8fc2751009b59173b5bfa03f715a894d549e517ef50c40350d5cb4442621772a","size":8048},{"path":"mcp/delegation/room-supervisor.ts","sha256":"78d92419a70d835de3a0df55acbd64adc15521ec16783f1005b4eb01d9b90fc2","size":37422,"symbols":[{"name":"merged","kind":"constant","sha256":"a8c25e665d6415ce9104f7b246a8ae496c1dca92ad25179b6da8b889364c40cf"},{"name":"config","kind":"constant","sha256":"fe47c35225c3b6e27ef07be8d775377be3f137208fb516c6b8f47e17e7bc4779"},{"name":"next","kind":"constant","sha256":"bef68a356033cf375840d2ffe854a864ed5587cd294d8c00ed3dfbb02266d8a2"},{"name":"superviseroom","kind":"function","sha256":"09d3239923d95d3a8dbe94a4b72657bca3f0c7b60fa7eda99d6690bfdcd3ebb3"},{"name":"args","kind":"constant","sha256":"c512b429f455d1c53a6134057adfae6c16742353c4cb2d2ae957a270aa7ba9cb"},{"name":"server","kind":"constant","sha256":"1890f22b44bbc44bfc34f48117c7d0d7742072b6ac1827d0836eb3af69a99e82"},{