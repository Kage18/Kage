---
type: "Bug Fix"
title: "Root cause of every orphaned run: ALL supervisors exit at blocked — spec said hold the session, no test pinned it"
description: "Correction and root cause for the orphaned run pattern supersedes the CLI only framing in the first dogfood day gotcha : superviseRun in mcp/delegation/supervisor.ts exits when a run emits a kage blocked fence — for DETA"
resource: "mcp/delegation/supervisor.ts"
tags: ["session-learning", "supervisor", "blocked", "orphaned-run", "steer", "spec-without-test", "root-cause"]
timestamp: "2026-08-17T20:52:23.383Z"
x-kage-id: "repo:https-github-com-kage-core-kage:bug_fix:root-cause-of-every-orphaned-run-all-supervisors-exit-at-blocked-spec-said-hold-"
x-kage-type: "bug_fix"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/delegation/supervisor.ts", "mcp/delegation/steer.ts", "mcp/delegation/contract.ts"]
---

# Root cause of every orphaned run: ALL supervisors exit at blocked — spec said hold the session, no test pinned it

> Correction and root cause for the orphaned run pattern supersedes the CLI only framing in the first dogfood day gotch…

Correction and root cause for the orphaned-run pattern (supersedes the CLI-only framing in the first-dogfood-day gotcha): superviseRun in mcp/delegation/supervisor.ts exits when a run emits a kage-blocked fence — for DETACHED supervisors too, not just CLI-inline ones. From that moment the run is unownable: steerRun finds no live socket, falls back to fire-and-forget claude --resume, and the resumed turn's kage-claim fence lands in the transcript with no collector — no claim.json, no verification, no transition; the reaper later records agent-process-gone. Observed three times in one day (two chat-fix attempts and the tokenizer fix; each time the finished work sat uncommitted in the worktree and the transcript held the fence). The parity spec explicitly required the opposite ("the agent process stays alive for the session with stdin open; answering writes into the live session") — the behavior was specified, half-built, and no test ever asserted supervisor survival across blocked: the same disease as reapRun-with-zero-callers, an invariant written in prose with no executable witness. Fix dispatched as run fix-the-root-cause-of-orphaned-runs-the-260817-7cae (supervisor holds through blocked: child alive, socket served, tell injects and resumes, same supervisor verifies the eventual claim), with a follow-up planned for steer re-attaching a supervisor when none is alive. Operator rule until both land: treat blocked runs dispatched before the fix as one-answer-then-orphan; do not steer them expecting completion.
Evidence: Three orphans in one day, each with the kage-claim fence present in transcript.jsonl and no claim.json; steer delivery reported "resumed" (no live socket) on a POST /runs detached dispatch, proving the detached supervisor was gone at blocked.
Verified by: Run records and transcripts under .agent_memory/runs/ for fix-three-room-chat-rendering-defects-260817-7831, land-the-room-chat-rendering-fix-third-a-260817-2791, one-array-bugfix-in-mcp-delegation-app-c-260817-d1a3.

## Verification

Three orphans in one day, each with the kage-claim fence present in transcript.jsonl and no claim.json; steer delivery reported "resumed" (no live socket) on a POST /runs detached dispatch, proving the detached supervisor was gone at blocked.

# Citations

[1] explicit_capture (2026-08-17T20:07:20.007Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:https-github-com-kage-core-kage:bug_fix:root-cause-of-every-orphaned-run-all-supervisors-exit-at-blocked-spec-said-hold-","title":"Root cause of every orphaned run: ALL supervisors exit at blocked — spec said hold the session, no test pinned it","summary":"Correction and root cause for the orphaned run pattern supersedes the CLI only framing in the first dogfood day gotcha : superviseRun in mcp/delegation/supervisor.ts exits when a run emits a kage blocked fence — for DETA","body":"Correction and root cause for the orphaned-run pattern (supersedes the CLI-only framing in the first-dogfood-day gotcha): superviseRun in mcp/delegation/supervisor.ts exits when a run emits a kage-blocked fence — for DETACHED supervisors too, not just CLI-inline ones. From that moment the run is unownable: steerRun finds no live socket, falls back to fire-and-forget claude --resume, and the resumed turn's kage-claim fence lands in the transcript with no collector — no claim.json, no verification, no transition; the reaper later records agent-process-gone. Observed three times in one day (two chat-fix attempts and the tokenizer fix; each time the finished work sat uncommitted in the worktree and the transcript held the fence). The parity spec explicitly required the opposite (\"the agent process stays alive for the session with stdin open; answering writes into the live session\") — the behavior was specified, half-built, and no test ever asserted supervisor survival across blocked: the same disease as reapRun-with-zero-callers, an invariant written in prose with no executable witness. Fix dispatched as run fix-the-root-cause-of-orphaned-runs-the-260817-7cae (supervisor holds through blocked: child alive, socket served, tell injects and resumes, same supervisor verifies the eventual claim), with a follow-up planned for steer re-attaching a supervisor when none is alive. Operator rule until both land: treat blocked runs dispatched before the fix as one-answer-then-orphan; do not steer them expecting completion.\nEvidence: Three orphans in one day, each with the kage-claim fence present in transcript.jsonl and no claim.json; steer delivery reported \"resumed\" (no live socket) on a POST /runs detached dispatch, proving the detached supervisor was gone at blocked.\nVerified by: Run records and transcripts under .agent_memory/runs/ for fix-three-room-chat-rendering-defects-260817-7831, land-the-room-chat-rendering-fix-third-a-260817-2791, one-array-bugfix-in-mcp-delegation-app-c-260817-d1a3.","type":"bug_fix","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["session-learning","supervisor","blocked","orphaned-run","steer","spec-without-test","root-cause"],"paths":["mcp/delegation/supervisor.ts","mcp/delegation/steer.ts","mcp/delegation/contract.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-17T20:07:20.007Z"}],"context":{"fact":"Correction and root cause for the orphaned-run pattern (supersedes the CLI-only framing in the first-dogfood-day gotcha): superviseRun in mcp/delegation/supervisor.ts exits when a run emits a kage-blocked fence — for DETACHED supervisors too, not just CLI-inline ones. From that moment the run is unownable: steerRun finds no live socket, falls back to fire-and-forget claude --resume, and the resumed turn's kage-claim fence lands in the transcript with no collector — no claim.json, no verification, no transition; the reaper later records agent-process-gone. Observed three times in one day (two chat-fix attempts and the tokenizer fix; each time the finished work sat uncommitted in the worktree and the transcript held the fence). The parity spec explicitly required the opposite (\"the agent process stays alive for the session with stdin open; answering writes into the live session\") — the behavior was specified, half-built, and no test ever asserted supervisor survival across blocked: the same disease as reapRun-with-zero-callers, an invariant written in prose with no executable witness. Fix dispatched as run fix-the-root-cause-of-orphaned-runs-the-260817-7cae (supervisor holds through blocked: child alive, socket served, tell injects and resumes, same supervisor verifies the eventual claim), with a follow-up planned for steer re-attaching a supervisor when none is alive. Operator rule until both land: treat blocked runs dispatched before the fix as one-answer-then-orphan; do not steer them expecting completion.\nEvidence: Three orphans in one day, each with the kage-claim fence present in transcript.jsonl and no claim.json; steer delivery reported \"resumed\" (no live socket) on a POST /runs detached dispatch, proving the detached supervisor was gone at blocked.\nVerified by: Run records and transcripts under .agent_memory/runs/ for fix-three-room-chat-rendering-defects-260817-7831, land-the-room-chat-rendering-fix-third-a-260817-2791, one-array-bugfix-in-mcp-delegation-app-c-260817-d1a3.","verification":"Three orphans in one day, each with the kage-claim fence present in transcript.jsonl and no claim.json; steer delivery reported \"resumed\" (no live socket) on a POST /runs detached dispatch, proving the detached supervisor was gone at blocked."},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-17T20:52:23.383Z","path_fingerprints":[{"path":"mcp/delegation/supervisor.ts","sha256":"b12f7b9ac588c293faedafb881f2fa6d43eeeea923f00f5f394804767753571c","size":21103,"symbols":[{"name":"superviserun","kind":"function","sha256":"0e853eded807c3f3e9830e22254072c9f2504d8d44e53282cdb3f8be45834647"},{"name":"child","kind":"constant","sha256":"5721bb1c9d8a1466f95c83d918930a72466e963d14d7efa461c7d82cf4c57a4d"},{"name":"verification","kind":"constant","sha256":"0e8ef41a682662a3f4b76b32d9df952232c5f1493a9fb8e53946e94cbe8489d1"},{"name":"claim","kind":"constant","sha256":"e94b9085c778497525d8daef323c7725188bde4197da98cc6eb46190ae5d7506"}]},{"path":"mcp/delegation/steer.ts","sha256":"dabb8533b061e21a5cb9519c5bf285a80b43659f481ce79171405b2387893985","size":5772,"symbols":[{"name":"steerrun","kind":"function","sha256":"e925bf784ec233093e01ac5f3f9885e5f264411d10b3ebe866e1e021de53c79a"},{"name":"live","kind":"constant","sha256":"191651c087cd2101addfbd73048e1b431f8792b424f7bd54febe1036ecceb2fe"}]},{"path":"mcp/delegation/contract.ts","sha256":"bb253d21af7ec1046ddcf3c1ab86fefa1284546ea9422b73719280d677d7d95e","size":27947,"symbols":[{"name":"runs","kind":"constant","sha256":"aeed515a96dc123ddceac928842184b148f9bbd85f63bccbb0918926221c95e2"},{"name":"reaprun","kind":"function","sha256":"c65240ad87675bf6a8fdfc13efd8acfe69b0fed53bdc3b8eba549ba7c7bdc94a"},{"name":"from","kind":"constant","sha256":"bd2200c07292af9f1ac297cdf2d079dea9c17bd2c3ee4d9522b7a3bb5c07a013"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":504,"reverified_at":"2026-08-17T20:52:23.383Z"},"created_at":"2026-08-17T20:07:20.007Z","updated_at":"2026-08-17T20:52:23.383Z","author_branch":"release-prep"}
```

