---
type: "Decision"
title: "sessions-ui.test.ts:50 hard-asserts the exact literal source string `if (current) list...."
description: "sessions ui.test.ts:50 hard asserts the exact literal source string if current list.appendChild renderSidebarFleet inside renderProjects via a regex against the composed page — this constrains any future rewrite of rende"
resource: "mcp/delegation/app-client.ts"
tags: ["delegated-run", "kage-run:write-if-changed-finish-the-render-calm-260820-3502"]
timestamp: "2026-08-20T12:41:16.080Z"
x-kage-id: "repo:write-if-changed-finish-the-render-calm-260820-3502:decision:sessions-ui-test-ts-50-hard-asserts-the-exact-literal-source-string-if-current-l"
x-kage-type: "decision"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/delegation/app-client.ts", "mcp/render-calm.test.ts"]
---

# sessions-ui.test.ts:50 hard-asserts the exact literal source string `if (current) list....

> sessions ui.test.ts:50 hard asserts the exact literal source string if current list.appendChild renderSidebarFleet in…

sessions-ui.test.ts:50 hard-asserts the exact literal source string `if (current) list.appendChild(renderSidebarFleet())` inside renderProjects() via a regex against the composed page — this constrains any future rewrite of renderProjects() (e.g. a per-row keyed cache) to preserve that exact call-site shape, or that unrelated test file breaks even though it isn't in a run's own touch set.

Learned while delivering: Write-if-changed: finish the render calm to AO-at-rest zero. Measured on the live app just now with a MutationObserver over 8 seconds at rest (nothing running, no data changing): childList:SPAN 202 mutations (~25/s) - the age ticker rewrites every age label's text node each second even when the rendered string is IDENTICAL ('17h' -> '17h'); plus plist 9 (~1/s, the sidebar project list rewriting on its poll), label 14, qtime 9, pnl-tl-at 4, sidefoot 4. Section render counts were ZERO over the same window - the revision gating from the just-merged render-calm run works; this is the last mile.

THE FIX, in mcp/delegation/app-client.ts: every periodic text patch becomes write-if-changed - compute the next string, compare against the node's current textContent, and skip the write when equal (the ticker, the qtime/pnl-tl-at/sidefoot/label updaters, and the plist poll's row updates - find each of the six mutation sources named above and gate it; name each site in your claim). Prefer nodeValue on the text node over textContent replacement where a text node already exists, so an actual change is characterData, not childList churn. No behaviour change when strings differ - ages still tick the second they change.

ACCEPTANCE: extend mcp/render-calm.test.ts (this feature's own test file - editing it is in scope for exactly this) with: the extracted write-if-changed helper skips the write on equal strings and writes on differing ones; the ticker path uses it (assert via the page string that the helper name appears in the ticker's code path). State in your claim the expected at-rest mutation rate after the change (should be ~0 when no age string rolls over).

CONSTRAINTS: app-client.ts is a TS TEMPLATE LITERAL - escapes DOUBLED, no backticks, no ${ }, never innerHTML. Your files: mcp/delegation/app-client.ts and mcp/render-calm.test.ts only. CommonJS. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage. Rebase onto release-prep if your base carries an inherited red. End with the kage-claim-v1 fence, citing files individually.
VERIFY: npm run test --prefix mcp (1035 unit + 12 dogfood green on main) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence.
Verified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability

## Verification

npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability

# Citations

[1] explicit_capture (2026-08-20T11:22:11.450Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:write-if-changed-finish-the-render-calm-260820-3502:decision:sessions-ui-test-ts-50-hard-asserts-the-exact-literal-source-string-if-current-l","title":"sessions-ui.test.ts:50 hard-asserts the exact literal source string `if (current) list....","summary":"sessions ui.test.ts:50 hard asserts the exact literal source string if current list.appendChild renderSidebarFleet inside renderProjects via a regex against the composed page — this constrains any future rewrite of rende","body":"sessions-ui.test.ts:50 hard-asserts the exact literal source string `if (current) list.appendChild(renderSidebarFleet())` inside renderProjects() via a regex against the composed page — this constrains any future rewrite of renderProjects() (e.g. a per-row keyed cache) to preserve that exact call-site shape, or that unrelated test file breaks even though it isn't in a run's own touch set.\n\nLearned while delivering: Write-if-changed: finish the render calm to AO-at-rest zero. Measured on the live app just now with a MutationObserver over 8 seconds at rest (nothing running, no data changing): childList:SPAN 202 mutations (~25/s) - the age ticker rewrites every age label's text node each second even when the rendered string is IDENTICAL ('17h' -> '17h'); plus plist 9 (~1/s, the sidebar project list rewriting on its poll), label 14, qtime 9, pnl-tl-at 4, sidefoot 4. Section render counts were ZERO over the same window - the revision gating from the just-merged render-calm run works; this is the last mile.\n\nTHE FIX, in mcp/delegation/app-client.ts: every periodic text patch becomes write-if-changed - compute the next string, compare against the node's current textContent, and skip the write when equal (the ticker, the qtime/pnl-tl-at/sidefoot/label updaters, and the plist poll's row updates - find each of the six mutation sources named above and gate it; name each site in your claim). Prefer nodeValue on the text node over textContent replacement where a text node already exists, so an actual change is characterData, not childList churn. No behaviour change when strings differ - ages still tick the second they change.\n\nACCEPTANCE: extend mcp/render-calm.test.ts (this feature's own test file - editing it is in scope for exactly this) with: the extracted write-if-changed helper skips the write on equal strings and writes on differing ones; the ticker path uses it (assert via the page string that the helper name appears in the ticker's code path). State in your claim the expected at-rest mutation rate after the change (should be ~0 when no age string rolls over).\n\nCONSTRAINTS: app-client.ts is a TS TEMPLATE LITERAL - escapes DOUBLED, no backticks, no ${ }, never innerHTML. Your files: mcp/delegation/app-client.ts and mcp/render-calm.test.ts only. CommonJS. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage. Rebase onto release-prep if your base carries an inherited red. End with the kage-claim-v1 fence, citing files individually.\nVERIFY: npm run test --prefix mcp (1035 unit + 12 dogfood green on main) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence.\nVerified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability","type":"decision","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["delegated-run","kage-run:write-if-changed-finish-the-render-calm-260820-3502"],"paths":["mcp/delegation/app-client.ts","mcp/render-calm.test.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-20T11:22:11.450Z"}],"context":{"fact":"sessions-ui.test.ts:50 hard-asserts the exact literal source string `if (current) list.appendChild(renderSidebarFleet())` inside renderProjects() via a regex against the composed page — this constrains any future rewrite of renderProjects() (e.g. a per-row keyed cache) to preserve that exact call-site shape, or that unrelated test file breaks even though it isn't in a run's own touch set.","verification":"npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-20T12:41:16.080Z","path_fingerprints":[{"path":"mcp/delegation/app-client.ts","sha256":"ded26e7b2ea02db2b4eade9056358d995b0517b8f9cf1181245a867beb33bafb","size":206711},{"path":"mcp/render-calm.test.ts","sha256":"0f661419f597b68cc90898beb8c0962031a1b4385b6eb11b4845fb6b08d16361","size":33274,"symbols":[{"name":"appendchild","kind":"method","sha256":"bd2b567760f351b3bad84b451de22b9446a49b92d32d19372aa7dbf079c3803c"},{"name":"from","kind":"constant","sha256":"065163bf37492fa85215c8e7227dd10049d7371eb08c5f1b1846e82a28c76ac8"},{"name":"node","kind":"function","sha256":"d351ae2e8fe0822490ba12f7abf52ac1e7204884caead1fdd40cee2029426fd4"},{"name":"expected","kind":"constant","sha256":"787b768be87677188599f72ccdc43d3f2b4537ae086e505a9a4708bf3c390f5a"},{"name":"writes","kind":"constant","sha256":"6d5f4692611812ead67aaf2b01dbeae5f3d4faadb19e834de4fcb878d237f0a8"},{"name":"span","kind":"constant","sha256":"556f83788e6b8c3b459e3a24edfd42d8a93e1705cc18d17ed291230b08da8ee7"},{"name":"second","kind":"constant","sha256":"f4d32e8185646657593f2a004a0e755187469b1f0319e3d197f120d137ef02f0"},{"name":"file","kind":"constant","sha256":"04eabbf3c8129428a1072ddf2dcf0409551dc725b45835fedb84e2e978633398"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":4000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":711,"unresolved_symbols":["noEmit"],"reverified_at":"2026-08-20T12:41:16.080Z","stale":true,"stale_reasons":["linked path changed since memory was verified: mcp/delegation/app-client.ts"],"suggested_action":"update"},"created_at":"2026-08-20T11:22:11.450Z","updated_at":"2026-08-20T20:13:09.716Z","author_branch":"kage/write-if-changed-finish-the-render-calm-260820-3502"}
```

