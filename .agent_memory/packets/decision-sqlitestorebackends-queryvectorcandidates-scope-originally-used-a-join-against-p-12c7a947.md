---
type: "Decision"
title: "SqliteStoreBackend's queryVectorCandidates(scope) originally used a JOIN against packet..."
description: "SqliteStoreBackend's queryVectorCandidates scope originally used a JOIN against packet paths, which fans a single vectors row out once per path when a packet cites multiple paths under the scope prefix fixed with an EXIS"
resource: "mcp/store-layer.test.ts"
tags: ["delegated-run", "kage-run:m1-of-the-memory-store-build-the-storeba-260820-3ff8"]
timestamp: "2026-08-20T06:10:51.409Z"
x-kage-id: "repo:m1-of-the-memory-store-build-the-storeba-260820-3ff8:decision:sqlitestorebackends-queryvectorcandidates-scope-originally-used-a-join-against-p"
x-kage-type: "decision"
x-kage-status: "pending"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/store-layer.test.ts", "mcp/store/json.ts", "mcp/store/manifest.ts", "mcp/store/rebuild.ts", "mcp/store/sqlite.ts", "mcp/store/types.ts"]
---

# SqliteStoreBackend's queryVectorCandidates(scope) originally used a JOIN against packet...

> SqliteStoreBackend's queryVectorCandidates scope originally used a JOIN against packet paths, which fans a single vec…

SqliteStoreBackend's queryVectorCandidates(scope) originally used a JOIN against packet_paths, which fans a single vectors row out once per path when a packet cites multiple paths under the scope prefix -- fixed with an EXISTS semi-join (SELECT ... WHERE EXISTS (SELECT 1 FROM packet_paths pp WHERE pp.packet_id = v.packet_id AND pp.path LIKE ?)), which can only keep or drop a row, never multiply it.

Learned while delivering: M1 of the memory store: build the StoreBackend layer itself, exactly as docs/design/MEMORY_STORE.md (just landed on this branch) specifies. New files under mcp/store/ ONLY - this run touches no existing file except to add nothing at all; the ports that wire it into kernel and graph reads come in M2/M3, not here.

READ FIRST: docs/design/MEMORY_STORE.md end to end - it is the contract for this run, it was verified against the code today, and your work must match it or say precisely where and why it deviates. Key laws from it: markdown packets stay the only truth and the store is a derived, rebuildable cache; ZERO new npm dependencies - the sqlite backend uses node's built-in node:sqlite behind feature detection (engines is >=18, so absence is a normal, first-class state, not an error); when node:sqlite is absent the JSON backend preserves today's semantics; no account, no server, an embedded file under .agent_memory/ only.

WHAT TO BUILD, all under mcp/store/:
1. types.ts - the StoreBackend interface, exactly the seam the design names: open/close/migrate; upserts for files, symbols, import and call edges; upserts for packets, packet_paths, packet_symbols; FTS document upsert + query; vector chunk upsert + nearest query; kg entity/edge/episode upserts and reads; the scoped variants that take a subtree path prefix. Every method's contract in a doc comment - the M2/M3 ports will be written against these words.
2. sqlite.ts - the node:sqlite backend. Schema per the design's sketch (files, symbols, edges, packets, packet_paths, packet_symbols, an FTS5 virtual table, vectors as per-chunk BLOB rows, kg_entities/kg_edges/kg_episodes), WAL mode, a schema_version pragma-table and forward-only migrate(). Feature detection lives here: a single detect() that try-requires node:sqlite and reports {available, reason} - never a throw at import time, and TypeScript must not need @types for it (use a narrow local declaration; no new deps).
3. json.ts - the JSON backend: an adapter over TODAY'S file layout (indexes/, graph/, structural/ as the design records them) implementing the same interface read-side, and write-side preserving current whole-file behaviour. It must NOT re-implement ranking - it serves rows; ranking stays with the callers.
4. manifest.ts - reads/writes .agent_memory/store/manifest.json: active backend, schema version, counts, last rebuild. openStore(projectDir) picks sqlite when detect().available and the manifest allows, else json - one function, the only door M2/M3 will use.
5. rebuild.ts - rebuildStore(projectDir): regenerate the ENTIRE derived store from packets + the structural file cache, idempotent, safe to run anytime; this is what makes 'derived cache' true rather than aspirational. Wire NOTHING into the CLI yet - M2 owns the `kage store` commands; export it and prove it in tests.

TESTS - new file mcp/store-layer.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). Cover at minimum: detect() reports available on this machine and the suite still passes when node:sqlite is FORCED absent (inject the require seam - the fallback path must be tested, not assumed); sqlite backend round-trips every entity type (file, symbol, edge, packet, packet_path, FTS doc, vector chunk, kg triple) and survives close/reopen; migrate() is forward-only and re-running it is a no-op; FTS query returns the doc a term appears in and not one it does not; the scoped variants restrict to the subtree prefix; json backend serves the same read answers as today's files for a small fixture; rebuildStore() on a fixture project produces a store whose counts match the fixture; manifest round-trips and openStore() honours a forced backend. Name in your claim which test fails if the change is reverted.

CONSTRAINTS: CommonJS - no import.meta (TS1470), no top-level await. Zero new entries in package.json dependencies or devDependencies - the diff to package.json must be EMPTY. Do not touch mcp/kernel.ts, mcp/cli.ts, mcp/index.ts, mcp/daemon.ts, anything under mcp/delegation/, or any existing test file - the budget run owns several of those right now. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage.
VERIFY: npm run test --prefix mcp (882 unit + 12 dogfood green before your change) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence.
Verified by: citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability

## Verification

citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability

# Citations

[1] explicit_capture (2026-08-20T06:10:51.409Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:m1-of-the-memory-store-build-the-storeba-260820-3ff8:decision:sqlitestorebackends-queryvectorcandidates-scope-originally-used-a-join-against-p","title":"SqliteStoreBackend's queryVectorCandidates(scope) originally used a JOIN against packet...","summary":"SqliteStoreBackend's queryVectorCandidates scope originally used a JOIN against packet paths, which fans a single vectors row out once per path when a packet cites multiple paths under the scope prefix fixed with an EXIS","body":"SqliteStoreBackend's queryVectorCandidates(scope) originally used a JOIN against packet_paths, which fans a single vectors row out once per path when a packet cites multiple paths under the scope prefix -- fixed with an EXISTS semi-join (SELECT ... WHERE EXISTS (SELECT 1 FROM packet_paths pp WHERE pp.packet_id = v.packet_id AND pp.path LIKE ?)), which can only keep or drop a row, never multiply it.\n\nLearned while delivering: M1 of the memory store: build the StoreBackend layer itself, exactly as docs/design/MEMORY_STORE.md (just landed on this branch) specifies. New files under mcp/store/ ONLY - this run touches no existing file except to add nothing at all; the ports that wire it into kernel and graph reads come in M2/M3, not here.\n\nREAD FIRST: docs/design/MEMORY_STORE.md end to end - it is the contract for this run, it was verified against the code today, and your work must match it or say precisely where and why it deviates. Key laws from it: markdown packets stay the only truth and the store is a derived, rebuildable cache; ZERO new npm dependencies - the sqlite backend uses node's built-in node:sqlite behind feature detection (engines is >=18, so absence is a normal, first-class state, not an error); when node:sqlite is absent the JSON backend preserves today's semantics; no account, no server, an embedded file under .agent_memory/ only.\n\nWHAT TO BUILD, all under mcp/store/:\n1. types.ts - the StoreBackend interface, exactly the seam the design names: open/close/migrate; upserts for files, symbols, import and call edges; upserts for packets, packet_paths, packet_symbols; FTS document upsert + query; vector chunk upsert + nearest query; kg entity/edge/episode upserts and reads; the scoped variants that take a subtree path prefix. Every method's contract in a doc comment - the M2/M3 ports will be written against these words.\n2. sqlite.ts - the node:sqlite backend. Schema per the design's sketch (files, symbols, edges, packets, packet_paths, packet_symbols, an FTS5 virtual table, vectors as per-chunk BLOB rows, kg_entities/kg_edges/kg_episodes), WAL mode, a schema_version pragma-table and forward-only migrate(). Feature detection lives here: a single detect() that try-requires node:sqlite and reports {available, reason} - never a throw at import time, and TypeScript must not need @types for it (use a narrow local declaration; no new deps).\n3. json.ts - the JSON backend: an adapter over TODAY'S file layout (indexes/, graph/, structural/ as the design records them) implementing the same interface read-side, and write-side preserving current whole-file behaviour. It must NOT re-implement ranking - it serves rows; ranking stays with the callers.\n4. manifest.ts - reads/writes .agent_memory/store/manifest.json: active backend, schema version, counts, last rebuild. openStore(projectDir) picks sqlite when detect().available and the manifest allows, else json - one function, the only door M2/M3 will use.\n5. rebuild.ts - rebuildStore(projectDir): regenerate the ENTIRE derived store from packets + the structural file cache, idempotent, safe to run anytime; this is what makes 'derived cache' true rather than aspirational. Wire NOTHING into the CLI yet - M2 owns the `kage store` commands; export it and prove it in tests.\n\nTESTS - new file mcp/store-layer.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). Cover at minimum: detect() reports available on this machine and the suite still passes when node:sqlite is FORCED absent (inject the require seam - the fallback path must be tested, not assumed); sqlite backend round-trips every entity type (file, symbol, edge, packet, packet_path, FTS doc, vector chunk, kg triple) and survives close/reopen; migrate() is forward-only and re-running it is a no-op; FTS query returns the doc a term appears in and not one it does not; the scoped variants restrict to the subtree prefix; json backend serves the same read answers as today's files for a small fixture; rebuildStore() on a fixture project produces a store whose counts match the fixture; manifest round-trips and openStore() honours a forced backend. Name in your claim which test fails if the change is reverted.\n\nCONSTRAINTS: CommonJS - no import.meta (TS1470), no top-level await. Zero new entries in package.json dependencies or devDependencies - the diff to package.json must be EMPTY. Do not touch mcp/kernel.ts, mcp/cli.ts, mcp/index.ts, mcp/daemon.ts, anything under mcp/delegation/, or any existing test file - the budget run owns several of those right now. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage.\nVERIFY: npm run test --prefix mcp (882 unit + 12 dogfood green before your change) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence.\nVerified by: citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability","type":"decision","scope":"repo","visibility":"team","sensitivity":"internal","status":"pending","confidence":0.7,"tags":["delegated-run","kage-run:m1-of-the-memory-store-build-the-storeba-260820-3ff8"],"paths":["mcp/store-layer.test.ts","mcp/store/json.ts","mcp/store/manifest.ts","mcp/store/rebuild.ts","mcp/store/sqlite.ts","mcp/store/types.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-20T06:10:51.409Z"}],"context":{"fact":"SqliteStoreBackend's queryVectorCandidates(scope) originally used a JOIN against packet_paths, which fans a single vectors row out once per path when a packet cites multiple paths under the scope prefix -- fixed with an EXISTS semi-join (SELECT ... WHERE EXISTS (SELECT 1 FROM packet_paths pp WHERE pp.packet_id = v.packet_id AND pp.path LIKE ?)), which can only keep or drop a row, never multiply it.","verification":"citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-20
601d2a570e861e19b756b5725f6"},{"name":"backend","kind":"constant","sha256":"574c694adfa78229643170b04a40e7d2952dacf241292719ff53a00e6bffebde"},{"name":"manifest","kind":"constant","sha256":"e354066b660d1cc7f339b5cff2933efc42407639e1b6a35435eecaa0a0c8f146"}]},{"path":"mcp/store/rebuild.ts","sha256":"b0fa1804cba85c70cb4c83b112bb79ab758a76c751fb1c591fa9bba3725d15ae","size":9859,"symbols":[{"name":"files","kind":"constant","sha256":"5130b71ec78570aa21e51a7e0ae8609c8b3f5af0d4a5e1a509db80e23d56fc07"},{"name":"symbols","kind":"constant","sha256":"a3a8df12f3c8cee28db6a26c4ecba87c5b32afaabd5dbd155c9a331ae50daf29"},{"name":"packets","kind":"constant","sha256":"314b51dff9d2bd537f3dfa796693f740cef6f6daffca7d27210644c2170c5a64"},{"name":"rebuildstore","kind":"function","sha256":"4dd00958c02656a5b7cf9a2829c5aa7021aa3ba5c98d4d98a983f171e625e04b"},{"name":"counts","kind":"constant","sha256":"f846b264ba15c5bf9f4e3ebe066d1dc23d7406b036bf14e4a6771bb49fbfb7c5"}]},{"path":"mcp/store/sqlite.ts","sha256":"b598db33fc564b6b60ee6c7739e907de0a2f4d4e343ab82a452b9df546e62a5c","size":19727,"symbols":[{"name":"detect","kind":"function","sha256":"b8a1c198d7a88837d2099dabede6bb802abe2bfd026514d5fe559121591ea45f"},{"name":"schema_version","kind":"constant","sha256":"2eafa54f6c5da620e6c8cafb2e1c2bb704bd47d1b7166a7780aebab1b1c377cb"},{"name":"sqlitestorebackend","kind":"class","sha256":"d2fbb1278eefb38137689c5574a7d7e901baf3d45700b3f6497503806a80e064"},{"name":"open","kind":"method","sha256":"8286d79bca4c4eb3301930e4c30fd19a77af23ac0b9155e3f2eb9c9480f4797b"},{"name":"path","kind":"constant","sha256":"6ad78d396b60527578fb5856d5b44944038626bbeb38f943e6542fdb95c22a2d"},{"name":"close","kind":"method","sha256":"38e3c648143fd7f356bd877c4adfcb688796761e2320d50d7d2246c27708508a"},{"name":"migrate","kind":"method","sha256":"25bb1db04efd0f3542b3904da72ab124c1fd55ea21bafb5dbe8f0ad24ba76050"},{"name":"queryvectorcandidates","kind":"method","sha256":"179d4345e582d38e9cbccad668501a8030c1696c1a1bf35a2d2df10d899da92a"},{"name":"counts","kind":"method","sha256":"5e1f0ef7174a00f60a42f04297181f054538d97782aa5a7b65219c14295be684"}]},{"path":"mcp/store/types.ts","sha256":"01a0fc3da6480ba9deefdf48e5e8520e0b2dc45db30c9498ed12d06d13e97f52","size":9610}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":4000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"stale_reasons":[],"estimated_tokens_saved":1233,"unresolved_symbols":["devDependencies","noEmit"]},"created_at":"2026-08-20T06:10:51.409Z","updated_at":"2026-08-20T06:10:51.996Z","author_branch":"kage/m1-of-the-memory-store-build-the-storeba-260820-3ff8"}
```

