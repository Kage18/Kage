---
type: "Decision"
title: "mcp/delegation/api.ts already had GET/POST /runs/:id/steers (edit/delete/reorder) and P..."
description: "mcp/delegation/api.ts already had GET/POST /runs/:id/steers edit/delete/reorder and POST /runs/:id/interrupt fully built from an earlier wave — only op:add and the take over routes were actually missing, contrary to the "
resource: "mcp/delegation-api.test.ts"
tags: ["delegated-run", "kage-run:wave-3-the-interaction-surface-wire-the-260818-9bfd"]
timestamp: "2026-08-20T12:41:18.049Z"
x-kage-id: "repo:wave-3-the-interaction-surface-wire-the-260818-9bfd:decision:mcp-delegation-api-ts-already-had-get-post-runs-id-steers-edit-delete-reorder-an"
x-kage-type: "decision"
x-kage-status: "deprecated"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "deprecated"
x-kage-paths: ["mcp/delegation-api.test.ts", "mcp/delegation/api.ts", "mcp/delegation/app-client.ts", "mcp/delegation/app-html.ts", "mcp/delegation/app-styles.ts"]
---

# mcp/delegation/api.ts already had GET/POST /runs/:id/steers (edit/delete/reorder) and P...

> mcp/delegation/api.ts already had GET/POST /runs/:id/steers edit/delete/reorder and POST /runs/:id/interrupt fully bu…

mcp/delegation/api.ts already had GET/POST /runs/:id/steers (edit/delete/reorder) and POST /runs/:id/interrupt fully built from an earlier wave — only op:add and the take-over routes were actually missing, contrary to the brief's framing that suggested more of the queue API was new.

Learned while delivering: WAVE 3 — the interaction surface: wire the merged AO+Conductor parity backends into the app. Backends already in the tree: steer records with queued/delivered status + queue mutations (steer.ts, dispatch.ts readSteerRecords) exposed at GET /runs/:id/steers and POST /runs/:id/steers {op: edit|delete|reorder}; POST /runs/:id/interrupt (true interrupt, honest 503); the kage-plan fence (blocked runs carry waiting_on.needs = plan approval with the plan text in waiting_on.detail); and mcp/delegation/run-pty.ts exporting takeOverRun/handBack/takeOverState/write/resize/snapshot with a pty factory seam (NO api wiring yet — that is yours). FILE CONVENTIONS FIRST: app-client.ts / app-html.ts / app-styles.ts are TS template literals — doubled escapes, no backticks, no ${, prefer split()/indexOf over regex, never innerHTML (a gate enforces it), and delegation-api.test.ts gates run on the composed page (parse, vocab, CSS-var, orphan-class). Diff budget is 800. BUILD, in one coherent pass: (1) QUEUE PANEL in the run detail (a Queue tab or a section under the steer composer): list steer records — delivered ones as immutable history with timestamps, queued ones editable in place, deletable, and reorderable (up/down buttons are fine; no drag dependency) via the steers API; refresh after each op; errors via showError. (2) DELIVERY CHOICE on the detail steer composer: default ⏎ = steer now (existing tell route); a visible toggle plus modifier ⌘⏎ = queue for later WITHOUT delivery — add op:add to POST /runs/:id/steers in api.ts mapping to appendSteerRecord for that (pure queue append, no delivery attempt). Immediate stays the default (Conductor shipped queue-by-default and reverted). (3) INTERRUPT button beside Stop for running/dispatched/verifying runs → POST /runs/:id/interrupt; on 503 showError; label it honestly (Interrupt — session stays alive). (4) PLAN CARD: when a blocked run has waiting_on.needs === plan approval, render a distinct plan-review card in the Follow pane and the work row: the plan text styled as a document (mono, bordered), an Approve button that tells the run approved — proceed as planned, and the normal answer field for revisions; Enter on the focused Approve button approves. (5) TAKE OVER: wire run-pty into api.ts — POST /runs/:id/takeover, POST /runs/:id/handback, plus pty write/resize/snapshot routes for the run, streaming output over the existing SSE pty channel with session key run:<runId> so room terminals and run terminals never cross (feed.notifyPty already carries a session field); in the app, a Take Over button on eligible runs opens a Terminal pane inside the run detail reusing the existing xterm machinery (same vendored assets, same repaint ladder patterns documented in the client), with a Hand Back button that calls handback and returns to the normal detail; state chips must reflect the kernel truth throughout. Add or extend tests: api tests for op:add, the takeover/handback routes with the pty factory seam (never a real claude), and keep every existing gate green. Success = npm test fully green.
Verified by: diff-size, citations

## Verification

diff-size, citations

# Citations

[1] explicit_capture (2026-08-18T05:58:15.488Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:wave-3-the-interaction-surface-wire-the-260818-9bfd:decision:mcp-delegation-api-ts-already-had-get-post-runs-id-steers-edit-delete-reorder-an","title":"mcp/delegation/api.ts already had GET/POST /runs/:id/steers (edit/delete/reorder) and P...","summary":"mcp/delegation/api.ts already had GET/POST /runs/:id/steers edit/delete/reorder and POST /runs/:id/interrupt fully built from an earlier wave — only op:add and the take over routes were actually missing, contrary to the ","body":"mcp/delegation/api.ts already had GET/POST /runs/:id/steers (edit/delete/reorder) and POST /runs/:id/interrupt fully built from an earlier wave — only op:add and the take-over routes were actually missing, contrary to the brief's framing that suggested more of the queue API was new.\n\nLearned while delivering: WAVE 3 — the interaction surface: wire the merged AO+Conductor parity backends into the app. Backends already in the tree: steer records with queued/delivered status + queue mutations (steer.ts, dispatch.ts readSteerRecords) exposed at GET /runs/:id/steers and POST /runs/:id/steers {op: edit|delete|reorder}; POST /runs/:id/interrupt (true interrupt, honest 503); the kage-plan fence (blocked runs carry waiting_on.needs = plan approval with the plan text in waiting_on.detail); and mcp/delegation/run-pty.ts exporting takeOverRun/handBack/takeOverState/write/resize/snapshot with a pty factory seam (NO api wiring yet — that is yours). FILE CONVENTIONS FIRST: app-client.ts / app-html.ts / app-styles.ts are TS template literals — doubled escapes, no backticks, no ${, prefer split()/indexOf over regex, never innerHTML (a gate enforces it), and delegation-api.test.ts gates run on the composed page (parse, vocab, CSS-var, orphan-class). Diff budget is 800. BUILD, in one coherent pass: (1) QUEUE PANEL in the run detail (a Queue tab or a section under the steer composer): list steer records — delivered ones as immutable history with timestamps, queued ones editable in place, deletable, and reorderable (up/down buttons are fine; no drag dependency) via the steers API; refresh after each op; errors via showError. (2) DELIVERY CHOICE on the detail steer composer: default ⏎ = steer now (existing tell route); a visible toggle plus modifier ⌘⏎ = queue for later WITHOUT delivery — add op:add to POST /runs/:id/steers in api.ts mapping to appendSteerRecord for that (pure queue append, no delivery attempt). Immediate stays the default (Conductor shipped queue-by-default and reverted). (3) INTERRUPT button beside Stop for running/dispatched/verifying runs → POST /runs/:id/interrupt; on 503 showError; label it honestly (Interrupt — session stays alive). (4) PLAN CARD: when a blocked run has waiting_on.needs === plan approval, render a distinct plan-review card in the Follow pane and the work row: the plan text styled as a document (mono, bordered), an Approve button that tells the run approved — proceed as planned, and the normal answer field for revisions; Enter on the focused Approve button approves. (5) TAKE OVER: wire run-pty into api.ts — POST /runs/:id/takeover, POST /runs/:id/handback, plus pty write/resize/snapshot routes for the run, streaming output over the existing SSE pty channel with session key run:<runId> so room terminals and run terminals never cross (feed.notifyPty already carries a session field); in the app, a Take Over button on eligible runs opens a Terminal pane inside the run detail reusing the existing xterm machinery (same vendored assets, same repaint ladder patterns documented in the client), with a Hand Back button that calls handback and returns to the normal detail; state chips must reflect the kernel truth throughout. Add or extend tests: api tests for op:add, the takeover/handback routes with the pty factory seam (never a real claude), and keep every existing gate green. Success = npm test fully green.\nVerified by: diff-size, citations","type":"decision","scope":"repo","visibility":"team","sensitivity":"internal","status":"deprecated","confidence":0.7,"tags":["delegated-run","kage-run:wave-3-the-interaction-surface-wire-the-260818-9bfd"],"paths":["mcp/delegation-api.test.ts","mcp/delegation/api.ts","mcp/delegation/app-client.ts","mcp/delegation/app-html.ts","mcp/delegation/app-styles.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-18T05:58:15.488Z"}],"context":{"fact":"mcp/delegation/api.ts already had GET/POST /runs/:id/steers (edit/delete/reorder) and POST /runs/:id/interrupt fully built from an earlier wave — only op:add and the take-over routes were actually missing, contrary to the brief's framing that suggested more of the queue API was new.","verification":"diff-size, citations"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-20T12:41:18.049Z","path_fingerprints":[{"path":"mcp/delegation-api.test.ts","sha256":"5664b212d3adef17954a296672ff7ce2be4feb285f82989abbcdae276b854adb","size":75803,"symbols":[{"name":"feed","kind":"constant","sha256":"38003f546da73239ba957c86a2cf7d173f8179a7240599f252d2e3027cbd6a72"},{"name":"room","kind":"constant","sha256":"daca1174495725bcfe924b660b84e822be76aea4fbb7dc3ebb244be8b8a9aa47"},{"name":"handback","kind":"constant","sha256":"e84df2bc6b62428021cf9a002a5712b6421db0e04a06ac4d01fa7a860a74fd1e"},{"name":"styled","kind":"constant","sha256":"3083781ab51ef013633592cc7265473865e667db87519db415190dc5704ba82d"},{"name":"learned","kind":"constant","sha256":"86a9cef845cb3f653282806e5a05f474fd9c20f4ad4af48ec5d031f762c896d5"},{"name":"diff","kind":"constant","sha256":"4755f26124dc87f6c211031d5c8f7b97491ca68a2f89a4a806acbacee62110a6"},{"name":"after","kind":"constant","sha256":"d051c692f49a215432f0e8b7ba363f12fcdcfb0f1c763962e308b5b556d01b54"},{"name":"first","kind":"constant","sha256":"cc3317ae29ab364733fbfa206f85ac441e0100f2033087b0c3c2d81126a7593a"},{"name":"calls","kind":"constant","sha256":"75e9b50cf46652b7740e78542d25175ee80d8f52094521553c80138965325eb9"},{"name":"list","kind":"constant","sha256":"385ad89762910c2d07a3657a28d80425db452ebb7c9db280f45ac7b213534128"}]},{"path":"mcp/delegation/api.ts","sha256":"30af4be6d286f7c204e38ee72a79599e7a65a2349516e9bac8245d3f420f97d5","size":78606,"symbols":[{"name":"size","kind":"constant","sha256":"c50004fdf51859ca83f5e78c1a746656c12e6cb1fb8c2d94cfd50af26c37f659"},{"name":"file","kind":"constant","sha256":"7817ad4add160daea6c517abf160c42394543b2463d05ee215dd88089585bf3f"},{"name":"runid","kind":"constant","sha256":"e6d8f7fd437887e978362ff035d9b70367434718a9ee7b4f6745e70ffbab22d6"},{"name":"notifypty","kind":"method","sha256":"4ecc43c79be5bab74de9bd8511369ab918ad77ccefb6136c4e9a6236badea845"},{"name":"text","kind":"constant","sha256":"c821f21e865b5862788006905c0f4fd09dd7ef6d223fcf2c505033203d6555aa"},{"name":"room","kind":"constant","sha256":"a9a23abe365a041f26787a2ec73b24fda2145c66a24433d13a5a8f56c3aae380"},{"name":"runs","kind":"constant","sha256":"aeed515a96dc123ddceac928842184b148f9bbd85f63bccbb0918926221c95e2"},{"name":"page","kind":"constant","sha256":"f554098c599c6bc0911ba986cec34f04a6a4eb9a39e5fc0959e8a87b3643efea"},{"name":"session","kind":"constant","sha256":"469d8b6ff94888de462ef809debd9cc097e66bc3c70ee39f0e666afe4ece1530"},{"name":"gate","kind":"constant","sha256":"becb3b1d8496a4898bc372345d8337e1848b2ac1a5e35e47100125e5d7370d12"},{"name":"existing","kind":"constant","sha256":"8f42ae48908113ba602c6cbb466167e0ffe6e4f2402c5796dd923ea4aa0803a7"},{"name":"takeover","kind":"constant","sha256":"f8a005efdfd67420661ba36562bc9825c67afef52b679dbb328e41757e89236e"}]},{"path":"mcp/delegation/app-client.ts","sha256":"ded26e7b2ea02db2b4eade9056358d995b0517b8f9cf1181245a867beb33bafb","size":206711},{"path":"mcp/delegation/app-html.ts","sha256":"ce8d9a5a3bc47b68e8aef4dd6277c2b22a08bdbb6a50e0e583d0024a2e9728e7","size":14159},{"path":"mcp/delegation/app-styles.ts","sha256":"c8d1bb16e765a5659d5a425df9d3e21f50cc10456d0095463a03618f21efbb77","size":76146}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":4000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":853,"unresolved_symbols":["takeOverState"],"reverified_at":"2026-08-20T12:41:18.049Z","stale":true,"stale_reasons":["linked path changed since memory was verified: mcp/delegation/app-client.ts, mcp/delegation/app-styles.ts"],"suggested_action":"update"},"created_at":"2026-08-18T05:58:15.488Z","updated_at":"2026-08-21T14:51:19.913Z","author_branch":"kage/wave-3-the-interaction-surface-wire-the-260818-9bfd"}
```

