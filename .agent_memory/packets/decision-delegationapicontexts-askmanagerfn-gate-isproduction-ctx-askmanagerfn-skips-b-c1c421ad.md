---
type: "Decision"
title: "DelegationApiContext's `askManagerFn` gate (`isProduction = !ctx.askManagerFn`) skips B..."
description: "DelegationApiContext's askManagerFn gate isProduction = !ctx.askManagerFn skips BOTH the pty and headless supervisor legs entirely — a test wanting to exercise the headless leg specifically must leave askManagerFn/askRoo"
resource: "mcp/delegation/api.ts"
tags: ["delegated-run", "kage-run:make-the-room-chat-interactable-the-owne-260821-b0e0"]
timestamp: "2026-08-21T16:01:29.429Z"
x-kage-id: "repo:make-the-room-chat-interactable-the-owne-260821-b0e0:decision:delegationapicontexts-askmanagerfn-gate-isproduction-ctx-askmanagerfn-skips-b-17"
x-kage-type: "decision"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/delegation/api.ts", "mcp/delegation/app-client.ts", "mcp/delegation/app-styles.ts", "mcp/delegation/manager-prompt.ts", "mcp/delegation/room-actions.ts", "mcp/delegation/room-history.ts"]
---

# DelegationApiContext's `askManagerFn` gate (`isProduction = !ctx.askManagerFn`) skips B...

> DelegationApiContext's askManagerFn gate isProduction = !ctx.askManagerFn skips BOTH the pty and headless supervisor …

DelegationApiContext's `askManagerFn` gate (`isProduction = !ctx.askManagerFn`) skips BOTH the pty and headless-supervisor legs entirely — a test wanting to exercise the headless leg specifically must leave askManagerFn/askRoomFn unset and instead inject `ensurePtyAttachedFn` returning null (to skip pty fast) plus the new `askRoomSupervisorFn` seam; reaching the headless branch also requires isAgentInstalled('claude') to be true in the test environment, since that's a real PATH check with no existing DI seam.

Learned while delivering: Make the Room chat interactable — the owner: users currently type everything; the manager must offer clickable cards and options. Build the structured-reply layer end to end: (1) PROTOCOL — the manager may end any reply with one fenced block labelled kage-actions containing JSON: either {question, options:[{label, send}]} (2-4 options; send is the message a click submits) or {proposals:[{intent, type}]} (runs to dispatch) or {actions:[{label, kind: open_run|dispatch|create_goal, payload}]}. Parse it in the reply pipeline (BOTH legs — the room-supervisor leg in room-supervisor.ts/api.ts AND the askManager leg), strip the fence from the visible text, and persist it as an actions field on the room turn (extend the turn schema additively — old turns without actions render unchanged). (2) WHILE IN THAT PIPELINE, unify the empty-reply retry: the previous run added retry-once+enriched-failure to the askManager leg only and honestly flagged that the room-supervisor leg has its own empty fallback with no DI seam — add the seam and apply the SAME retry-once + enriched-failure there, with a test through the new seam. (3) CLIENT (app-client.ts/app-styles.ts) — render turn.actions in the thread column: question options as clickable chips (click sends option.send as a normal room message via the existing composer path and disables the chip row for that turn afterward); proposals as compact cards with the intent text and a Dispatch button wired to the existing dispatch API; open_run actions as run chips that open the existing run detail; every control uses existing token-guarded APIs and existing action functions, h() only, never innerHTML, revision-gated like the rest. Style per the app design language (chip/atom vocabulary — see docs/design/mockups/Main.dc.html for the chip treatments). (4) MANAGER PROMPT — extend the constitution: whenever the manager asks a clarifying question it MUST provide the kage-actions options block (2-4 concrete choices); whenever it proposes work it SHOULD attach proposals so the user can click Dispatch; typing always remains possible. (5) TESTS — protocol parse (valid block extracted+stripped, malformed block ignored gracefully and left visible as text so nothing is silently lost), retry-once on the supervisor leg via the new seam, turn schema round-trip, and composed-page string assertions that a turn with actions renders the chips/buttons and one without renders exactly as before. Template-literal constraints as always; parse test and full suite green; cite files individually.
Verified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability

## Verification

npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability

# Citations

[1] explicit_capture (2026-08-21T16:01:29.429Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:make-the-room-chat-interactable-the-owne-260821-b0e0:decision:delegationapicontexts-askmanagerfn-gate-isproduction-ctx-askmanagerfn-skips-b-17","title":"DelegationApiContext's `askManagerFn` gate (`isProduction = !ctx.askManagerFn`) skips B...","summary":"DelegationApiContext's askManagerFn gate isProduction = !ctx.askManagerFn skips BOTH the pty and headless supervisor legs entirely — a test wanting to exercise the headless leg specifically must leave askManagerFn/askRoo","body":"DelegationApiContext's `askManagerFn` gate (`isProduction = !ctx.askManagerFn`) skips BOTH the pty and headless-supervisor legs entirely — a test wanting to exercise the headless leg specifically must leave askManagerFn/askRoomFn unset and instead inject `ensurePtyAttachedFn` returning null (to skip pty fast) plus the new `askRoomSupervisorFn` seam; reaching the headless branch also requires isAgentInstalled('claude') to be true in the test environment, since that's a real PATH check with no existing DI seam.\n\nLearned while delivering: Make the Room chat interactable — the owner: users currently type everything; the manager must offer clickable cards and options. Build the structured-reply layer end to end: (1) PROTOCOL — the manager may end any reply with one fenced block labelled kage-actions containing JSON: either {question, options:[{label, send}]} (2-4 options; send is the message a click submits) or {proposals:[{intent, type}]} (runs to dispatch) or {actions:[{label, kind: open_run|dispatch|create_goal, payload}]}. Parse it in the reply pipeline (BOTH legs — the room-supervisor leg in room-supervisor.ts/api.ts AND the askManager leg), strip the fence from the visible text, and persist it as an actions field on the room turn (extend the turn schema additively — old turns without actions render unchanged). (2) WHILE IN THAT PIPELINE, unify the empty-reply retry: the previous run added retry-once+enriched-failure to the askManager leg only and honestly flagged that the room-supervisor leg has its own empty fallback with no DI seam — add the seam and apply the SAME retry-once + enriched-failure there, with a test through the new seam. (3) CLIENT (app-client.ts/app-styles.ts) — render turn.actions in the thread column: question options as clickable chips (click sends option.send as a normal room message via the existing composer path and disables the chip row for that turn afterward); proposals as compact cards with the intent text and a Dispatch button wired to the existing dispatch API; open_run actions as run chips that open the existing run detail; every control uses existing token-guarded APIs and existing action functions, h() only, never innerHTML, revision-gated like the rest. Style per the app design language (chip/atom vocabulary — see docs/design/mockups/Main.dc.html for the chip treatments). (4) MANAGER PROMPT — extend the constitution: whenever the manager asks a clarifying question it MUST provide the kage-actions options block (2-4 concrete choices); whenever it proposes work it SHOULD attach proposals so the user can click Dispatch; typing always remains possible. (5) TESTS — protocol parse (valid block extracted+stripped, malformed block ignored gracefully and left visible as text so nothing is silently lost), retry-once on the supervisor leg via the new seam, turn schema round-trip, and composed-page string assertions that a turn with actions renders the chips/buttons and one without renders exactly as before. Template-literal constraints as always; parse test and full suite green; cite files individually.\nVerified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability","type":"decision","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["delegated-run","kage-run:make-the-room-chat-interactable-the-owne-260821-b0e0"],"paths":["mcp/delegation/api.ts","mcp/delegation/app-client.ts","mcp/delegation/app-styles.ts","mcp/delegation/manager-prompt.ts","mcp/delegation/room-actions.ts","mcp/delegation/room-history.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-21T16:01:29.429Z"}],"context":{"fact":"DelegationApiContext's `askManagerFn` gate (`isProduction = !ctx.askManagerFn`) skips BOTH the pty and headless-supervisor legs entirely — a test wanting to exercise the headless leg specifically must leave askManagerFn/askRoomFn unset and instead inject `ensurePtyAttachedFn` returning null (to skip pty fast) plus the new `askRoomSupervisorFn` seam; reaching the headless branch also requires isAgentInstalled('claude') to be true in the test environment, since that's a real PATH check with no existing DI seam.","verification":"npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-21T16:01:29.429Z","path_fingerprints":[{"path":"mcp/delegation/api.ts","sha256":"3602a09963caac0df4835c377e9346a00b4bb8d39a6d899e4d5342c41c0635a8","size":91732,"symbols":[{"name":"size","kind":"constant","sha256":"c50004fdf51859ca83f5e78c1a746656c12e6cb1fb8c2d94cfd50af26c37f659"},{"name":"json","kind":"function","sha256":"9534facbff0d20476918887174c4edf739d502278aa0750b5fa1da683358d3e7"},{"name":"payload","kind":"constant","sha256":"b1ed24c96b58c2dfa676321e47cd098e4ce523d579498a0a7e0ade0806103538"},{"name":"send","kind":"function","sha256":"260e116cd0e58cf5299d79197928a1a2e46505a4296fb62dd56ba127df1e5fc7"},{"name":"text","kind":"constant","sha256":"68c7127268d3091b048db955501b6a9d6ccb8cd92a38de59feba8e2ca24d4bb3"},{"name":"isproduction","kind":"constant","sha256":"f146bb5f6d20c7b2f2037bb94704949f2c8f88487f0b9de3ec5a376829d7c569"},{"name":"room","kind":"constant","sha256":"a9a23abe365a041f26787a2ec73b24fda2145c66a24433d13a5a8f56c3aae380"},{"name":"files","kind":"constant","sha256":"10f5a322e356009fe3570a8b1fc93b0e50f550074efda18f91eb7ba5b0cd7445"},{"name":"turns","kind":"constant","sha256":"6371ca84790d293029008fcbb1e91ebdd76d8973683174e8ed02b5e536c367a8"},{"name":"runs","kind":"constant","sha256":"aeed515a96dc123ddceac928842184b148f9bbd85f63bccbb0918926221c95e2"},{"name":"kind","kind":"constant","sha256":"92e9fb412aebc9338a3043784e954ded94230b8e9a5aca41db155614c031c002"},{"name":"added","kind":"constant","sha256":"5c2ea43039560f155ddc1204384e06546679e98d2cb5962618c2ea1cd9292f9c"},{"name":"page","kind":"constant","sha256":"f554098c599c6bc0911ba986cec34f04a6a4eb9a39e5fc0959e8a87b3643efea"},{"name":"gate","kind":"constant","sha256":"becb3b1d8496a4898bc372345d8337e1848b2ac1a5e35e47100125e5d7370d12"},{"name":"existing","kind":"constant","sha256":"8f42ae48908113ba602c6cbb466167e0ffe6e4f2402c5796dd923ea4aa0803a7"}]},{"path":"mcp/delegation/app-client.ts","sha256":"3db880c8af62c1c78761cbcb185e82dbd36891c9bdc91a38593b97afb357b45f","size":238329},{"path":"mcp/delegation/app-styles.ts","sha256":"b93b669e70acccdab406a84943224325f180ac644844b847efc82a5861b24369","size":89225},{"path":"mcp/delegation/manager-prompt.ts","sha256":"c28e400298020e9fb61794bf3edf8443edb42a7f25f1bfc5b4490dabf6570872","size":12767},{"path":"mcp/delegation/room-actions.ts","sha256":"09927e0232957238ca9edd21370e497f4dd353fdf1694d5efb78a283a4cb7129","size":4832,"symbols":[{"name":"options","kind":"constant","sha256":"2cbf091e2cf79f0e75daf9033228d9cde571ba50e2acbfd0b6113dfb4e9105ff"},{"name":"proposals","kind":"constant","sha256":"b767966770c4a74620eb60026ce59ba5c52a7492848f66eb2ca6051e0ee8e6cb"}]},{"path":"mcp/delegation/room-history.ts","sha256":"8b4a70c3cc399e85d5675cc2ebda5fe47b7962c99a7937d996f4f0e9792f2c95","size":3653}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":4000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"stale_reasons":[],"estimated_tokens_saved":808,"unresolved_symbols":["innerHTML","noEmit"]},"created_at":"2026-08-21T16:01:29.429Z","updated_at":"2026-08-21T16:02:38.719Z","author_branch":"kage/make-the-room-chat-interactable-the-owne-260821-b0e0"}
```

