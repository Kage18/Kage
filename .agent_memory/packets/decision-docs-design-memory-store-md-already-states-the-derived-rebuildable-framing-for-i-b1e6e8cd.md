---
type: "Decision"
title: "docs/design/MEMORY_STORE.md already states the derived/rebuildable framing for its .age..."
description: "docs/design/MEMORY STORE.md already states the derived/rebuildable framing for its .agent memory/ artifact table in one place: 'Every artifact this doc's problem section names is, today, disposable — delete the whole tre"
resource: "mcp/store-doc.test.ts"
tags: ["delegated-run", "kage-run:doc-truth-maintenance-small-and-surgical-260820-afac"]
timestamp: "2026-08-20T06:47:28.030Z"
x-kage-id: "repo:doc-truth-maintenance-small-and-surgical-260820-afac:decision:docs-design-memory-store-md-already-states-the-derived-rebuildable-framing-for-i"
x-kage-type: "decision"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/store-doc.test.ts"]
---

# docs/design/MEMORY_STORE.md already states the derived/rebuildable framing for its .age...

> docs/design/MEMORY STORE.md already states the derived/rebuildable framing for its .agent memory/ artifact table in o…

docs/design/MEMORY_STORE.md already states the derived/rebuildable framing for its .agent_memory/ artifact table in one place: 'Every artifact this doc's problem section names is, today, disposable — delete the whole tree and `kage index` regenerates it from the markdown packets plus the repo's own source' (around the Law 1 section) — this doubles as the exact sentence a doc-truth test should key off of rather than re-deriving new prose.

Learned while delivering: Doc truth-maintenance, small and surgical: docs/design/SESSIONS_SURFACE.md is now BEHIND the code, and its own guard test caught it - exactly as designed. The failing test: mcp/sessions-doc.test.ts, 'the doc's stall-detector gap is real: no stall-detection code exists yet' - it asserts no 'stall' identifier exists in mcp/delegation/contract.ts, and the budget-circuit-breaker run just merged the stall detector (STALL_SAME_COMMAND_STREAK and friends now live in contract.ts/supervisor.ts). This is the ONLY red test in the suite (902/903).

WHAT TO DO: (1) Update SESSIONS_SURFACE.md wherever it describes the stall detector and the live agent_session_id persistence as in-flight - both landed with the budget run; rewrite those sentences to cite them as landed, naming the real identifiers you find in the code (grep contract.ts and supervisor.ts for the stall constants; verify agent_session_id is now written mid-run in supervisor.ts before you claim it). Also re-check the doc's other in-flight citation - the unified-session run - and leave it marked in-flight, because it still is. (2) Flip the corresponding assertions in mcp/sessions-doc.test.ts from 'gap is real' to 'landed and cited': the stall test should now assert the identifier DOES exist in contract.ts and that the doc no longer calls it in-flight. Keep the test's spirit - it exists to fail whenever doc and code disagree in EITHER direction; do not delete it or weaken it to a tautology. (3) Touch nothing else: your whole diff is those two files.

CONSTRAINTS: CommonJS - no import.meta, no top-level await. Do not touch any other file - two other runs are working right now. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage.
VERIFY: npm run test --prefix mcp must come back FULLY green (903 unit + 12 dogfood, zero failures - the point of this run is turning the one red test green) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence.
Verified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability

## Action

(1) Update SESSIONS_SURFACE.md wherever it describes the stall detector and the live agent_session_id persistence as in-flight - both landed with the budget run; rewrite those sentences to cite them as landed, naming the real identifiers you find in the code (grep contract.ts and supervisor.ts for the stall constants; verify agent_session_id is now written mid-run in supervisor.ts before you claim it). Also re-check the doc's other in-flight citation - the unified-session run - and leave it marked in-flight, because it still is. (2) Flip the corresponding assertions in mcp/sessions-doc.test.ts from 'gap is real' to 'landed and cited': the stall test should now assert the identifier DOES exist in contract.ts and that the doc no longer calls it in-flight. Keep the test's spirit - it exists to fail whenever doc and code disagree in EITHER direction; do not delete it or weaken it to a tautology. (3) Touch nothing else: your whole diff is those two files.

CONSTRAINTS: CommonJS - no import.meta, no top-level await. Do not touch any other file - two other runs are working right now. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage.
VERIFY: npm run test --prefix mcp must come back FULLY green (903 unit + 12 dogfood, zero failures - the point of this run is turning the one red test green) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence.

## Verification

npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability

# Citations

[1] explicit_capture (2026-08-20T06:47:28.030Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:doc-truth-maintenance-small-and-surgical-260820-afac:decision:docs-design-memory-store-md-already-states-the-derived-rebuildable-framing-for-i","title":"docs/design/MEMORY_STORE.md already states the derived/rebuildable framing for its .age...","summary":"docs/design/MEMORY STORE.md already states the derived/rebuildable framing for its .agent memory/ artifact table in one place: 'Every artifact this doc's problem section names is, today, disposable — delete the whole tre","body":"docs/design/MEMORY_STORE.md already states the derived/rebuildable framing for its .agent_memory/ artifact table in one place: 'Every artifact this doc's problem section names is, today, disposable — delete the whole tree and `kage index` regenerates it from the markdown packets plus the repo's own source' (around the Law 1 section) — this doubles as the exact sentence a doc-truth test should key off of rather than re-deriving new prose.\n\nLearned while delivering: Doc truth-maintenance, small and surgical: docs/design/SESSIONS_SURFACE.md is now BEHIND the code, and its own guard test caught it - exactly as designed. The failing test: mcp/sessions-doc.test.ts, 'the doc's stall-detector gap is real: no stall-detection code exists yet' - it asserts no 'stall' identifier exists in mcp/delegation/contract.ts, and the budget-circuit-breaker run just merged the stall detector (STALL_SAME_COMMAND_STREAK and friends now live in contract.ts/supervisor.ts). This is the ONLY red test in the suite (902/903).\n\nWHAT TO DO: (1) Update SESSIONS_SURFACE.md wherever it describes the stall detector and the live agent_session_id persistence as in-flight - both landed with the budget run; rewrite those sentences to cite them as landed, naming the real identifiers you find in the code (grep contract.ts and supervisor.ts for the stall constants; verify agent_session_id is now written mid-run in supervisor.ts before you claim it). Also re-check the doc's other in-flight citation - the unified-session run - and leave it marked in-flight, because it still is. (2) Flip the corresponding assertions in mcp/sessions-doc.test.ts from 'gap is real' to 'landed and cited': the stall test should now assert the identifier DOES exist in contract.ts and that the doc no longer calls it in-flight. Keep the test's spirit - it exists to fail whenever doc and code disagree in EITHER direction; do not delete it or weaken it to a tautology. (3) Touch nothing else: your whole diff is those two files.\n\nCONSTRAINTS: CommonJS - no import.meta, no top-level await. Do not touch any other file - two other runs are working right now. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage.\nVERIFY: npm run test --prefix mcp must come back FULLY green (903 unit + 12 dogfood, zero failures - the point of this run is turning the one red test green) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence.\nVerified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability","type":"decision","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["delegated-run","kage-run:doc-truth-maintenance-small-and-surgical-260820-afac"],"paths":["mcp/store-doc.test.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-20T06:47:28.030Z"}],"context":{"fact":"docs/design/MEMORY_STORE.md already states the derived/rebuildable framing for its .agent_memory/ artifact table in one place: 'Every artifact this doc's problem section names is, today, disposable — delete the whole tree and `kage index` regenerates it from the markdown packets plus the repo's own source' (around the Law 1 section) — this doubles as the exact sentence a doc-truth test should key off of rather than re-deriving new prose.","action":"(1) Update SESSIONS_SURFACE.md wherever it describes the stall detector and the live agent_session_id persistence as in-flight - both landed with the budget run; rewrite those sentences to cite them as landed, naming the real identifiers you find in the code (grep contract.ts and supervisor.ts for the stall constants; verify agent_session_id is now written mid-run in supervisor.ts before you claim it). Also re-check the doc's other in-flight citation - the unified-session run - and leave it marked in-flight, because it still is. (2) Flip the corresponding assertions in mcp/sessions-doc.test.ts from 'gap is real' to 'landed and cited': the stall test should now assert the identifier DOES exist in contract.ts and that the doc no longer calls it in-flight. Keep the test's spirit - it exists to fail whenever doc and code disagree in EITHER direction; do not delete it or weaken it to a tautology. (3) Touch nothing else: your whole diff is those two files.\n\nCONSTRAINTS: CommonJS - no import.meta, no top-level await. Do not touch any other file - two other runs are working right now. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage.\nVERIFY: npm run test --prefix mcp must come back FULLY green (903 unit + 12 dogfood, zero failures - the point of this run is turning the one red test green) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence.","verification":"npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-20T06:47:28.030Z","path_fingerprints":[{"path":"mcp/store-doc.test.ts","sha256":"ae7e277af85395fb1a8c064d9eb456f4c1a140929a9d77aa7a8811b8dec0642b","size":15187,"symbols":[{"name":"derived","kind":"constant","sha256":"ad9c3e9db31da28ad6fd1ec41f61743a78ab79445127422c084c4892dd348457"},{"name":"source","kind":"constant","sha256":"28ae27b0b5d6ef9cfba0b47462dc72ab83a29eef18b72153b454195b5674412a"},{"name":"identifier","kind":"constant","sha256":"25d18785294541bfa6cba1f30adb9060327eea6feb8858bc96a0f6ad9d13517f"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":4000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"stale_reasons":[],"estimated_tokens_saved":649,"unresolved_symbols":["SESSIONS_SURFACE","STALL_SAME_COMMAND_STREAK","agent_session_id","noEmit"]},"created_at":"2026-08-20T06:47:28.030Z","updated_at":"2026-08-20T06:49:22.779Z","author_branch":"kage/doc-truth-maintenance-small-and-surgical-260820-afac"}
```

