---
type: "Decision"
title: "The reachability-check brief's flagship self-test example (transitionGoal, 'its ONLY ca..."
description: "The reachability check brief's flagship self test example transitionGoal, 'its ONLY caller is abandonGoal' no longer reproduces at HEAD: abandonGoal is now wired to a live route daemon.ts:817 delegation/api.ts:1002's POS"
resource: "mcp/delegation/dispatch.ts"
tags: ["delegated-run", "kage-run:build-a-reachability-check-stop-runs-fro-260818-b1d4"]
timestamp: "2026-08-20T12:41:47.829Z"
x-kage-id: "repo:build-a-reachability-check-stop-runs-fro-260818-b1d4:decision:the-reachability-check-briefs-flagship-self-test-example-transitiongoal-its-only"
x-kage-type: "decision"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/delegation/dispatch.ts", "mcp/delegation/reachability.ts", "mcp/delegation/supervisor.ts", "mcp/reachability.test.ts"]
---

# The reachability-check brief's flagship self-test example (transitionGoal, 'its ONLY ca...

> The reachability check brief's flagship self test example transitionGoal, 'its ONLY caller is abandonGoal' no longer …

The reachability-check brief's flagship self-test example (transitionGoal, 'its ONLY caller is abandonGoal') no longer reproduces at HEAD: abandonGoal is now wired to a live route (daemon.ts:817 -> delegation/api.ts:1002's POST /goals/:id/abandon -> abandonGoal -> transitionGoal), so a correct reachability check must NOT flag it -- it's really called from a real entry point. The brief's other three orphan examples (reapRun, looksLikeGitRepo, resolveWorkspaceKind) have likewise already been given real production callers (delegation/ratify.ts + delegation/steer.ts; delegation/worktree.ts; delegation/supervisor.ts + delegation/dispatch.ts). The residual transitionGoal defect -- nothing in production ever passes 'executing' or 'done' as its `to` argument, only 'abandoned' ever fires -- is a value/argument-flow issue one level below symbol/field reachability, a fundamentally different and more false-positive-prone analysis than a reference graph.

Learned while delivering: Build a reachability check: stop runs from landing code that exists, compiles, passes tests, and is never reached from anything real.

WHY — this is the single most repeated defect class in this repo. Seven instances found, all shipped green:
- transitionGoal (mcp/delegation/goal.ts): its ONLY caller is abandonGoal, so a goal can never reach 'executing' or 'done'.
- supervisor_pid (mcp/delegation/contract.ts): READ by liveState, WRITTEN only by supervisor.ts — never on the CLI dispatch path, so the fix that reads it is inert where it matters most.
- goal attachment: existed, but ran AFTER the run finished, so goalForRun was null for the run's whole life.
- reapRun, looksLikeGitRepo, resolveWorkspaceKind: all shipped with zero callers.
- LEGAL_TRANSITIONS allows failed -> running with a comment explaining why, but no command anywhere performs it.
In every case tests passed, because the test called the new symbol DIRECTLY or patched the field by hand (the liveState test literally does patchRun(project, id, { supervisor_pid: ... })). Nothing ever asked whether the code was reachable from a real entry point. That is the hole to close.

WHAT TO BUILD — a new module mcp/delegation/reachability.ts:

1. Export findOrphans(projectDir: string, changedFiles: string[]): OrphanFinding[].
   An OrphanFinding names the symbol or field, its file and line, and which of the two rules it broke.

2. RULE A — orphan export. For each symbol EXPORTED by a changed file, resolve every reference to it across mcp/**/*.ts. If its only references are its own definition and *.test.ts files, it is an orphan: nothing in production reaches it. Report it.

3. RULE B — write-only or read-only field. For each field added to an interface in a changed file (TaskRecord, GoalRecord and friends), find its readers and its writers across non-test sources. A field with readers but no non-test writer (the supervisor_pid case) or with writers but no reader is reported. This rule is what would have caught the inert liveness fix.

4. Reachability must be judged from REAL entry points, not from 'something imports it'. Treat these as roots: mcp/cli.ts, mcp/index.ts (the MCP tool surface), mcp/daemon.ts and mcp/delegation/api.ts (HTTP routes), and any bin entry in mcp/package.json. A symbol only reachable from another orphan is still an orphan, so compute this transitively from the roots rather than checking one hop.

5. ESCAPE HATCH, mandatory: a symbol is exempt when the line above it carries the comment marker '// reachability: <reason>'. Some exports are deliberate public API. The marker must require a written reason — an unexplained exemption is how this rot returns. Honour it, and cover it with a test.

6. WIRE IT AS A KERNEL-EXECUTED CHECK on every run, exactly the way the static checks that just landed in mcp/delegation/static-checks.ts are wired — read that file first and follow its pattern, including how it writes evidence through verify.ts's writeEvidence and how it appears in the receipt as a check run by Kage rather than by the agent. IMPORTANT: static-checks.ts had to be wired into BOTH dispatch.ts's executeRun (foreground runs) and supervisor.ts's superviseRun (detached runs) because those two call sites hand-roll claim assembly independently and have drifted before. Wire yours into both, and add a test asserting both call sites run it — otherwise this task ships its own orphan, which would be a genuinely embarrassing outcome for this particular feature.

7. A finding must NOT hard-fail the run. Report orphans as a distinct, visible check result with the symbol names and the rule each broke, so the operator sees them in the receipt and decides. Getting the signal in front of a human is the goal; a false positive that blocks a merge would get this check disabled within a week.

TESTS — put them in a NEW file mcp/reachability.test.ts, NOT in mcp/delegation.test.ts. That file is a merge-conflict hotspot because every run appends to its end; three merges in a row have collided there today. Cover: an export referenced only by a test is reported; an export reachable from cli.ts is not; an export reachable only via another orphan IS reported (the transitive case); a field with a reader and no non-test writer is reported; the '// reachability: <reason>' marker exempts a symbol; an unexplained marker does not; both executeRun and superviseRun invoke the check.

SELF-TEST, do this before you claim: run your own findOrphans against this repo's current mcp/delegation/goal.ts and confirm it reports transitionGoal. If it does not, the check does not work — say so plainly rather than claiming success.

CONSTRAINTS: this project compiles to CommonJS — no 'import.meta' (TS1470), no top-level await. Do not touch mcp/delegation/adapters/index.ts or mcp/delegation/brief.ts; another run is editing them right now. Do not run kage_refresh, kage_learn or kage_pr_check; those are the operator's job. Run 'npm test --prefix mcp' (636 tests green before your change) and 'npm run build --prefix mcp' (no 'error TS'), and state honestly in your claim if any command was denied to you.
Verified by: diff-size, npx tsc --noEmit -p mcp/tsconfig.json

## Verification

diff-size, npx tsc --noEmit -p mcp/tsconfig.json

# Citations

[1] explicit_capture (2026-08-18T11:20:43.303Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:build-a-reachability-check-stop-runs-fro-260818-b1d4:decision:the-reachability-check-briefs-flagship-self-test-example-transitiongoal-its-only","title":"The reachability-check brief's flagship self-test example (transitionGoal, 'its ONLY ca...","summary":"The reachability check brief's flagship self test example transitionGoal, 'its ONLY caller is abandonGoal' no longer reproduces at HEAD: abandonGoal is now wired to a live route daemon.ts:817 delegation/api.ts:1002's POS","body":"The reachability-check brief's flagship self-test example (transitionGoal, 'its ONLY caller is abandonGoal') no longer reproduces at HEAD: abandonGoal is now wired to a live route (daemon.ts:817 -> delegation/api.ts:1002's POST /goals/:id/abandon -> abandonGoal -> transitionGoal), so a correct reachability check must NOT flag it -- it's really called from a real entry point. The brief's other three orphan examples (reapRun, looksLikeGitRepo, resolveWorkspaceKind) have likewise already been given real production callers (delegation/ratify.ts + delegation/steer.ts; delegation/worktree.ts; delegation/supervisor.ts + delegation/dispatch.ts). The residual transitionGoal defect -- nothing in production ever passes 'executing' or 'done' as its `to` argument, only 'abandoned' ever fires -- is a value/argument-flow issue one level below symbol/field reachability, a fundamentally different and more false-positive-prone analysis than a reference graph.\n\nLearned while delivering: Build a reachability check: stop runs from landing code that exists, compiles, passes tests, and is never reached from anything real.\n\nWHY — this is the single most repeated defect class in this repo. Seven instances found, all shipped green:\n- transitionGoal (mcp/delegation/goal.ts): its ONLY caller is abandonGoal, so a goal can never reach 'executing' or 'done'.\n- supervisor_pid (mcp/delegation/contract.ts): READ by liveState, WRITTEN only by supervisor.ts — never on the CLI dispatch path, so the fix that reads it is inert where it matters most.\n- goal attachment: existed, but ran AFTER the run finished, so goalForRun was null for the run's whole life.\n- reapRun, looksLikeGitRepo, resolveWorkspaceKind: all shipped with zero callers.\n- LEGAL_TRANSITIONS allows failed -> running with a comment explaining why, but no command anywhere performs it.\nIn every case tests passed, because the test called the new symbol DIRECTLY or patched the field by hand (the liveState test literally does patchRun(project, id, { supervisor_pid: ... })). Nothing ever asked whether the code was reachable from a real entry point. That is the hole to close.\n\nWHAT TO BUILD — a new module mcp/delegation/reachability.ts:\n\n1. Export findOrphans(projectDir: string, changedFiles: string[]): OrphanFinding[].\n   An OrphanFinding names the symbol or field, its file and line, and which of the two rules it broke.\n\n2. RULE A — orphan export. For each symbol EXPORTED by a changed file, resolve every reference to it across mcp/**/*.ts. If its only references are its own definition and *.test.ts files, it is an orphan: nothing in production reaches it. Report it.\n\n3. RULE B — write-only or read-only field. For each field added to an interface in a changed file (TaskRecord, GoalRecord and friends), find its readers and its writers across non-test sources. A field with readers but no non-test writer (the supervisor_pid case) or with writers but no reader is reported. This rule is what would have caught the inert liveness fix.\n\n4. Reachability must be judged from REAL entry points, not from 'something imports it'. Treat these as roots: mcp/cli.ts, mcp/index.ts (the MCP tool surface), mcp/daemon.ts and mcp/delegation/api.ts (HTTP routes), and any bin entry in mcp/package.json. A symbol only reachable from another orphan is still an orphan, so compute this transitively from the roots rather than checking one hop.\n\n5. ESCAPE HATCH, mandatory: a symbol is exempt when the line above it carries the comment marker '// reachability: <reason>'. Some exports are deliberate public API. The marker must require a written reason — an unexplained exemption is how this rot returns. Honour it, and cover it with a test.\n\n6. WIRE IT AS A KERNEL-EXECUTED CHECK on every run, exactly the way the static checks that just landed in mcp/delegation/static-checks.ts are wired — read that file first and follow its pattern, including how it writes evidence through verify.ts's writeEvidence and how it appears in the receipt as a check run by Kage rather than by the agent. IMPORTANT: static-checks.ts had to be wired into BOTH dispatch.ts's executeRun (foreground runs) and supervisor.ts's superviseRun (detached runs) because those two call sites hand-roll claim assembly independently and have drifted before. Wire yours into both, and add a test asserting both call sites run it — otherwise this task ships its own orphan, which would be a genuinely embarrassing outcome for this particular feature.\n\n7. A finding must NOT hard-fail the run. Report orphans as a distinct, visible check result with the symbol names and the rule each broke, so the operator sees them in the receipt and decides. Getting the signal in front of a human is the goal; a false positive that blocks a merge would get this check disabled within a week.\n\nTESTS — put them in a NEW file mcp/reachability.test.ts, NOT in mcp/delegation.test.ts. That file is a merge-conflict hotspot because every run appends to its end; three merges in a row have collided there today. Cover: an export referenced only by a test is reported; an export reachable from cli.ts is not; an export reachable only via another orphan IS reported (the transitive case); a field with a reader and no non-test writer is reported; the '// reachability: <reason>' marker exempts a symbol; an unexplained marker does not; both executeRun and superviseRun invoke the check.\n\nSELF-TEST, do this before you claim: run your own findOrphans against this repo's current mcp/delegation/goal.ts and confirm it reports transitionGoal. If it does not, the check does not work — say so plainly rather than claiming success.\n\nCONSTRAINTS: this project compiles to CommonJS — no 'import.meta' (TS1470), no top-level await. Do not touch mcp/delegation/adapters/index.ts or mcp/delegation/brief.ts; another run is editing them right now. Do not run kage_refresh, kage_learn or kage_pr_check; those are the operator's job. Run 'npm test --prefix mcp' (636 tests green before your change) and 'npm run build --prefix mcp' (no 'error TS'), and state honestly in your claim if any command was denied to you.\nVerified by: diff-size, npx tsc --noEmit -p mcp/tsconfig.json","type":"decision","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["delegated-run","kage-run:build-a-reachability-check-stop-runs-fro-260818-b1d4"],"paths":["mcp/delegation/dispatch.ts","mcp/delegation/reachability.ts","mcp/delegation/supervisor.ts","mcp/reachability.test.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-18T11:20:43.303Z"}],"context":{"fact":"The reachability-check brief's flagship self-test example (transitionGoal, 'its ONLY caller is abandonGoal') no longer reproduces at HEAD: abandonGoal is now wired to a live route (daemon.ts:817 -> delegation/api.ts:1002's POST /goals/:id/abandon -> abandonGoal -> transitionGoal), so a correct reachability check must NOT flag it -- it's really called from a real entry point. The brief's other three orphan examples (reapRun, looksLikeGitRepo, resolveWorkspaceKind) have likewise already been given real production callers (delegation/ratify.ts + delegation/steer.ts; delegation/worktree.ts; delegation/supervisor.ts + delegation/dispatch.ts). The residual transitionGoal defect -- nothing in production ever passes 'executing' or 'done' as its `to` argument, only 'abandoned' ever fires -- is a value/argument-flow issue one level below symbol/field reachability, a fundamentally different and more false-positive-prone analysis than a reference graph.","verification":"diff-size, npx tsc --noEmit -p mcp/tsconfig.json"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-20T12:41:47.829Z","path_fingerprints":[{"path":"mcp/delegation/dispatch.ts","sha256":"8ffa9bcf9d6458a52aaf472946ca73b1a6408ecbda47ddd09124bceffa861e05","size":23982,"symbols":[{"name":"index","kind":"constant","sha256":"4f9ac98937fd4bda11e3c960113c786f03852f8e8edc53cf53ebea1852e4254d"},{"name":"result","kind":"constant","sha256":"0581aeea4384d52610a5a3a6b2250467bbbb34a444c13d6351948929b699fc47"},{"name":"executerun","kind":"function","sha256":"09a1d2b39a7ce153ff5c4e1cfcf5dccc62b1e9b07205dff1286a5a7a856b4e73"},{"name":"outcome","kind":"constant","sha256":"5546e65c49dd416a69ab06c4c53d1387d9474fdf93715a9a99290cfe3fa02db2"},{"name":"claim","kind":"constant","sha256":"503d03bbad627f38bd9f2f7b2dd08561cca0c09fbdcba09345502c785d52a736"},{"name":"entry","kind":"constant","sha256":"4973122f31ff8193a04bbb42d2824e61d29b0421b8ba721799d82b4b3ca0be0a"},{"name":"worktree","kind":"constant","sha256":"0d337a9939de05272f55ff66c5ee11f4ef58ab4589d71335c0518092a3f9dabf"}]},{"path":"mcp/delegation/reachability.ts","sha256":"3c303da6a78f70fe3b8ec9a47f14506a30bb7801eb9492f29c8ec9a99442a183","size":33652,"symbols":[{"name":"reason","kind":"constant","sha256":"daf3eea49fe1d7b1a09294425736f171298b8352a274f0c26781c6450b371fe9"},{"name":"path","kind":"constant","sha256":"669bf062f485af35bfcc9e280a38b5fb3b4b3150d27bc9e596216a77c0cf48f2"},{"name":"roots","kind":"constant","sha256":"8de4f10642b93d3f2871bf0ee921eeebe5a1d95e2e0de8e8e878c3c38426a3da"},{"name":"file","kind":"constant","sha256":"ab09a2b5e7c5d43c625803fa7fca81fd4c962ffa5d5f157cd2d17e6680d983e8"},{"name":"reachable","kind":"constant","sha256":"6a4986d20fbb12f66da52174f4980bdfcc2f5163a5845a59081a933d5fd5f229"},{"name":"example","kind":"constant","sha256":"46d2bbed4f067fc181dce09fce6a4dcbf676082d261b3bb19fc900ed0df7210c"},{"name":"added","kind":"constant","sha256":"34eb4e82b68be36685a69b39ffbbe43523593b733b0091dfc8fbd6e04791e3d4"},{"name":"result","kind":"constant","sha256":"19b5696fca3f169cb20e7be07df927d56a9009f5b351096aa751e3da8e60fc29"},{"name":"readers","kind":"constant","sha256":"68bc230c1c09dc99ab97310dfd78a1c5475b80e2f59ee361519f6efcc197c947"},{"name":"writers","kind":"constant","sha256":"ca8b8fd70368ba7f6fd484bbed198937c7d170e2ac74e08b5f511ee80362a95e"},{"name":"findorphans","kind":"function","sha256":"40b8e53c004ee2beef0430a2fe9d2c4f2259f074d2694f886e55326a18488df7"},{"name":"analysis","kind":"constant","sha256":"ec1e7046e4e47614e7817294fdb901920bfa28bee9505e4f6d6a666d5818771a"},{"name":"changed","kind":"constant","sha256":"91f39b8f00999cb886df482297653a3fe3f6ea642a2e81f3a44203fd0dad16f0"},{"name":"evidence","kind":"constant","sha256":"f2380b02c6701ead7491fb5e90a46edd03dbdf66a0d6303f6702d32f8aec80d6"},{"name":"names","kind":"constant","sha256":"a1595ebfa92a349f489523dc052dcc3ec24c9f4f21a8b34f5bffdfabe6c52d9f"},{"name":"check","kind":"constant","sha256":"725e6cedb1c932ff2d21b6c28a7320dba8fa37093178e248f3cbbe590e1b10c8"}]},{"path":"mcp/delegation/supervisor.ts","sha256":"d8ed31b23d0078e3e8e44e233ec819b4b203ed5e0b1a47f1a4858c834f5bfd4f","size":43021,"symbols":[{"name":"result","kind":"constant","sha256":"7ddfa5fb8fbc03e08b55b77c544e31123755ea081ec606bb93fa1204b146911c"},{"name":"superviserun","kind":"function","sha256":"97e6a6a3bcf1aed316113cd585eb865f5bc448c4c9cbcbedeadf145e4870b8ab"},{"name":"task","kind":"constant","sha256":"85f8a3124cd54be2d855713df25f16201af7f8bd479bd55ac37f1f399bd11753"},{"name":"brief","kind":"constant","sha256":"d6f0c21082d08ce5ca0501e184cb30dff7dc321ae8ae3f48a93ed013d30ef12f"},{"name":"state","kind":"constant","sha256":"74ff1664f384b9305eedf989c0d85f9eb9f5142fd6d024b00630476f177a9c6e"},{"name":"signal","kind":"constant","sha256":"58cf9dbc0b422f6562f67838df8679182ff3f26f0e871a7bd2368007ac08130b"},{"name":"check","kind":"constant","sha256":"c66ffea80735db586a3cb711454818ae97c9c05b55b9a22863f1be5bdcd90ff2"},{"name":"outcome","kind":"constant","sha256":"7ef48c19ebba9b6f4e57c950cc802900bc47219d2439d400e72df1d454e806a7"},{"name":"claim","kind":"constant","sha256":"503d03bbad627f38bd9f2f7b2dd08561cca0c09fbdcba09345502c785d52a736"}]},{"path":"mcp/reachability.test.ts","sha256":"7a06cb34312326a22eefa4f22813e0b2879c7f1521032c7695a995cee4af6fad","size":29444,"symbols":[{"name":"write","kind":"function","sha256":"6c106f3e40489091d37318d93f6bb7330c3d72defc72ac08052c9a8c0059c013"},{"name":"entry","kind":"constant","sha256":"0af98548860ec2cb16b9219b4b7138ef4873746f26e21d791d1049d4f78cd0f5"},{"name":"check","kind":"constant","sha256":"202596502ec3077fc1d4df9f5c8ba282fa5b675452a9947e381c9e909d721cec"},{"name":"task","kind":"constant","sha256":"7cd7f537502307fd17875cc4910a527697cbccf28a1ffd56b91eab8a53e651c0"},{"name":"claim","kind":"constant","sha256":"71219e415e121136903afbe4364deaf35ceabbdba360471de232fa0f77c5fe49"},{"name":"marker","kind":"constant","sha256":"16e8f9abff43f5cff6863fbef698a2a90a285f3187777853a3bde548f2ebd141"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":4000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":1540,"unresolved_symbols":["transitionGoal","abandonGoal","reapRun","looksLikeGitRepo","LEGAL_TRANSITIONS","GoalRecord","kage_refresh","kage_learn"],"reverified_at":"2026-08-20T12:41:47.829Z"},"created_at":"2026-08-18T11:20:43.303Z","updated_at":"2026-08-20T12:41:47.829Z","author_branch":"kage/build-a-reachability-check-stop-runs-fro-260818-b1d4"}
```

