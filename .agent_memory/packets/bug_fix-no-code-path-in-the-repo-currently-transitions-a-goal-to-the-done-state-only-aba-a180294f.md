---
type: "Bug Fix"
title: "No code path in the repo currently transitions a goal to the 'done' state (only 'abando..."
description: "No code path in the repo currently transitions a goal to the 'done' state only 'abandoned' is reachable via the API — transitionGoal 'done' is only ever exercised directly in tests today. The active goal clearing logic b"
tags: ["delegated-run", "kage-run:make-goal-attachment-implicit-so-the-orc-260818-e307"]
timestamp: "2026-08-18T07:28:07.816Z"
x-kage-id: "repo:make-goal-attachment-implicit-so-the-orc-260818-e307:bug_fix:no-code-path-in-the-repo-currently-transitions-a-goal-to-the-done-state-only-aba"
x-kage-type: "bug_fix"
x-kage-status: "pending"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
---

# No code path in the repo currently transitions a goal to the 'done' state (only 'abando...

> No code path in the repo currently transitions a goal to the 'done' state only 'abandoned' is reachable via the API —…

No code path in the repo currently transitions a goal to the 'done' state (only 'abandoned' is reachable via the API) — transitionGoal('done') is only ever exercised directly in tests today. The active-goal-clearing logic belongs inside transitionGoal itself (not in the API route) precisely so a future 'done' transition inherits the clearing behavior automatically rather than needing every future call site to remember it.

Learned while delivering: Make goal attachment IMPLICIT so the orchestrator loop cannot silently open. Observed live: the Room manager planned a goal correctly and said it would pass goal_id on kage_dispatch, then dispatched without it — attachRunToGoal never ran, the goal record kept zero waves, goalForRun returned null, and the event bridge (which only wakes the manager for goal-owned runs) could never fire. Capability and instruction were both present (dist had goal_id, built before the supervisor started), so the fix is structural, not a stronger instruction: the manager must not be ABLE to forget. BUILD: (1) Room sessions carry an active goal. In mcp/delegation/room-sessions.ts (read it first — sessions are keyed thread records) add an optional active_goal_id to the session record plus setActiveGoal(projectDir, sessionKey, goalId|null) and readActiveGoal(projectDir, sessionKey), atomic writes like the rest. (2) POST /goals in mcp/delegation/api.ts accepts an optional session (default the DEFAULT_SESSION key) and calls setActiveGoal for it, so a goal created from a room thread is owned by that thread; add POST /goals/:id/activate {session} and clear the active goal when a goal is abandoned or reaches done. (3) The MCP dispatch handler in mcp/index.ts (kage_dispatch, which already takes optional goal_id) runs inside the room MCP server with env KAGE_ROOM=1 and KAGE_PROJECT_DIR: when goal_id is ABSENT and KAGE_ROOM is set, resolve the active goal for the room session (default thread unless a session env/arg is available) and attach the run to it automatically; an explicit goal_id always wins; when neither exists, behave exactly as today. Say so in the tool description: runs dispatched from a room thread with an active goal attach to it automatically. (4) manager-prompt.ts: replace the pass goal_id on every dispatch instruction with the truth — runs you dispatch while a goal is active attach automatically; pass goal_id only to attach to a DIFFERENT goal. (5) Tests in mcp/delegation.test.ts (and api tests where routes changed): setActiveGoal/readActiveGoal round-trip; a dispatch with no goal_id under KAGE_ROOM attaches to the session active goal (drive the handler directly, do not spawn anything); an explicit goal_id overrides the active one; no active goal and no goal_id leaves the run unattached; abandoning a goal clears the active pointer. Keep the diff tight. Success = npm test fully green.
Verified by: npm test --prefix mcp, diff-size, citations

## Verification

npm test --prefix mcp, diff-size, citations

# Citations

[1] explicit_capture (2026-08-18T07:28:07.816Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:make-goal-attachment-implicit-so-the-orc-260818-e307:bug_fix:no-code-path-in-the-repo-currently-transitions-a-goal-to-the-done-state-only-aba","title":"No code path in the repo currently transitions a goal to the 'done' state (only 'abando...","summary":"No code path in the repo currently transitions a goal to the 'done' state only 'abandoned' is reachable via the API — transitionGoal 'done' is only ever exercised directly in tests today. The active goal clearing logic b","body":"No code path in the repo currently transitions a goal to the 'done' state (only 'abandoned' is reachable via the API) — transitionGoal('done') is only ever exercised directly in tests today. The active-goal-clearing logic belongs inside transitionGoal itself (not in the API route) precisely so a future 'done' transition inherits the clearing behavior automatically rather than needing every future call site to remember it.\n\nLearned while delivering: Make goal attachment IMPLICIT so the orchestrator loop cannot silently open. Observed live: the Room manager planned a goal correctly and said it would pass goal_id on kage_dispatch, then dispatched without it — attachRunToGoal never ran, the goal record kept zero waves, goalForRun returned null, and the event bridge (which only wakes the manager for goal-owned runs) could never fire. Capability and instruction were both present (dist had goal_id, built before the supervisor started), so the fix is structural, not a stronger instruction: the manager must not be ABLE to forget. BUILD: (1) Room sessions carry an active goal. In mcp/delegation/room-sessions.ts (read it first — sessions are keyed thread records) add an optional active_goal_id to the session record plus setActiveGoal(projectDir, sessionKey, goalId|null) and readActiveGoal(projectDir, sessionKey), atomic writes like the rest. (2) POST /goals in mcp/delegation/api.ts accepts an optional session (default the DEFAULT_SESSION key) and calls setActiveGoal for it, so a goal created from a room thread is owned by that thread; add POST /goals/:id/activate {session} and clear the active goal when a goal is abandoned or reaches done. (3) The MCP dispatch handler in mcp/index.ts (kage_dispatch, which already takes optional goal_id) runs inside the room MCP server with env KAGE_ROOM=1 and KAGE_PROJECT_DIR: when goal_id is ABSENT and KAGE_ROOM is set, resolve the active goal for the room session (default thread unless a session env/arg is available) and attach the run to it automatically; an explicit goal_id always wins; when neither exists, behave exactly as today. Say so in the tool description: runs dispatched from a room thread with an active goal attach to it automatically. (4) manager-prompt.ts: replace the pass goal_id on every dispatch instruction with the truth — runs you dispatch while a goal is active attach automatically; pass goal_id only to attach to a DIFFERENT goal. (5) Tests in mcp/delegation.test.ts (and api tests where routes changed): setActiveGoal/readActiveGoal round-trip; a dispatch with no goal_id under KAGE_ROOM attaches to the session active goal (drive the handler directly, do not spawn anything); an explicit goal_id overrides the active one; no active goal and no goal_id leaves the run unattached; abandoning a goal clears the active pointer. Keep the diff tight. Success = npm test fully green.\nVerified by: npm test --prefix mcp, diff-size, citations","type":"bug_fix","scope":"repo","visibility":"team","sensitivity":"internal","status":"pending","confidence":0.7,"tags":["delegated-run","kage-run:make-goal-attachment-implicit-so-the-orc-260818-e307"],"paths":[],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-18T07:28:07.816Z"}],"context":{"fact":"No code path in the repo currently transitions a goal to the 'done' state (only 'abandoned' is reachable via the API) — transitionGoal('done') is only ever exercised directly in tests today. The active-goal-clearing logic belongs inside transitionGoal itself (not in the API route) precisely so a future 'done' transition inherits the clearing behavior automatically rather than needing every future call site to remember it.","verification":"npm test --prefix mcp, diff-size, citations"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-18T07:28:07.816Z","path_fingerprints":[],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":74,"reasons":["high-value memory type","has source evidence","tagged","actionable rationale or verification"],"risks":["not grounded to paths"],"duplicate_candidates":[],"stale_reasons":[],"estimated_tokens_saved":731},"created_at":"2026-08-18T07:28:07.816Z","updated_at":"2026-08-18T07:28:08.017Z","author_branch":"kage/make-goal-attachment-implicit-so-the-orc-260818-e307"}
```

