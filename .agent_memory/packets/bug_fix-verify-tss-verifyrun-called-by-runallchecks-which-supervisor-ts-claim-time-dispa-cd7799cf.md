---
type: "Bug Fix"
title: "verify.ts's verifyRun (called by runAllChecks, which supervisor.ts claim-time, dispatch..."
description: "verify.ts's verifyRun called by runAllChecks, which supervisor.ts claim time, dispatch.ts claim time, ratify.ts's reverifyRun, and recovery.ts's adoptOrphanedRun all funnel through is the single choke point for diff meas"
resource: "mcp/delegation/contract.ts"
tags: ["delegated-run", "kage-run:three-meter-and-guard-integrity-defects-260820-593c"]
timestamp: "2026-08-20T07:08:36.215Z"
x-kage-id: "repo:three-meter-and-guard-integrity-defects-260820-593c:bug_fix:verify-tss-verifyrun-called-by-runallchecks-which-supervisor-ts-claim-time-dispa"
x-kage-type: "bug_fix"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/delegation/contract.ts", "mcp/delegation/supervisor.ts", "mcp/delegation/verify.ts", "mcp/delegation/worktree-guard.ts", "mcp/meter-integrity.test.ts", "mcp/worktree-guard.test.ts"]
---

# verify.ts's verifyRun (called by runAllChecks, which supervisor.ts claim-time, dispatch...

> verify.ts's verifyRun called by runAllChecks, which supervisor.ts claim time, dispatch.ts claim time, ratify.ts's rev…

verify.ts's verifyRun (called by runAllChecks, which supervisor.ts claim-time, dispatch.ts claim-time, ratify.ts's reverifyRun, and recovery.ts's adoptOrphanedRun all funnel through) is the single choke point for diff measurement — fixing measureDiff there fixes claim-time, reverify, AND adopt in one place, no need to touch any of those four call sites.

Learned while delivering: Three meter-and-guard integrity defects, each observed live today with evidence on disk. They share one theme: a check or meter that reports in the reassuring direction when its measurement window is wrong. Fix all three.

1. REVERIFY'S DIFF-SIZE CHECK PASSES VACUOUSLY ON COMMITTED WORKTREES. Runs commit their work at claim time (commitWorktree), so by the time `kage reverify` re-runs the diff-size check the working tree is clean and the check records literally '0 file(s), 0 changed line(s), budget 800' - see .agent_memory/runs/budgets-become-a-circuit-breaker-not-a-t-260820-e70a/evidence/diff-size.log, written while that run's own claim admitted ~1,400 changed lines. Fix: the diff measurement must be merge-base..HEAD PLUS uncommitted changes, wherever the diff check computes its numbers (find the single place; verify.ts's runDiffCheck or its data source) - so a committed diff still counts and an uncommitted one still counts, on every path that runs checks (claim-time, reverify, adopt). Regression test: a worktree whose change is fully committed must report its real line count under reverify, not zero.

2. SPEND RESETS ON RESUME. recordSpend overwrites spend with the agent CLI's latest usage report, and a resumed run starts a FRESH agent session whose cumulative usage starts at zero - so a run that had spent $30.96 before its stop showed $2.95 after resuming (both figures observed on budgets-become-...-e70a today). The meter under-counts exactly the expensive runs. Fix: accumulate across sessions - keep a per-session ledger keyed by agent_session_id (the run record already carries the current one, and it now lands mid-stream) or a sealed-spend baseline that each new session's usage adds onto; state in your claim which design you chose and why it cannot double-count a session that reports usage multiple times (the CLI reports cumulative-per-session on every tick - the last figure per session is the truth for that session). Regression test: stop, resume with a new session id, report smaller usage - total spend must be prior + new, never just new.

3. THE WORKTREE-BOUNDARY GUARD ATTRIBUTES BY DRIFT, NOT BY CONTENT. It fired today on run m1-... because mcp/dist in the MAIN checkout changed during the run's lifetime - but that change was the OPERATOR's own legitimate post-merge rebuild, not the run's escape (evidence: .agent_memory/runs/m1-of-the-memory-store-build-the-storeba-260820-3ff8/evidence/worktree-boundary.log lists dist files from a merge the run never touched). A guard that cries wolf on every operator rebuild will get ignored, which is worse than no guard. Fix: attribute by CONTENT - flag only when the main checkout's changed build output contains code traceable to THIS run's worktree (e.g. identifiers/strings present in the run's new or modified source but absent from the main checkout's committed source - the exact two-marker method that caught the real leak on 2026-08-19). Both cases need tests: the operator-rebuild scenario must NOT flag; a planted leak (a marker string from the worktree's source appearing in main dist) MUST flag.

TESTS - new file mcp/meter-integrity.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). The three regression tests above plus: the diff fix counts committed+uncommitted together when both exist. Name in your claim which test fails if the change is reverted.

CONSTRAINTS: CommonJS - no import.meta, no top-level await. Your files: mcp/delegation/verify.ts, mcp/delegation/ratify.ts, mcp/delegation/contract.ts, mcp/delegation/worktree-guard.ts, mcp/delegation/supervisor.ts if spend recording lives there, plus your new test file. Do NOT touch mcp/kernel.ts or anything under mcp/store/ (another run owns those right now), nor api.ts/app-client.ts. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage.
VERIFY: npm run test --prefix mcp (935/936 green on main before your change - the one red is a doc-guard another run is fixing; YOUR worktree's bar is zero NEW failures and all three regressions green) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence.
Verified by: diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability

## Verification

.agent_memory/runs/m1-of-the-memory-store-build-the-storeba-260820-3ff8/evidence/worktree-boundary.log lists dist files from a merge the run never touched). A guard that cries wolf on every operator rebuild will get ignored, which is worse than no guard. Fix: attribute by CONTENT - flag only when the main checkout's changed build output contains code traceable to THIS run's worktree (e.g. identifiers/strings present in the run's new or modified source but absent from the main checkout's committed source - the exact two-marker method that caught the real leak on 2026-08-19). Both cases need tests: the operator-rebuild scenario must NOT flag; a planted leak (a marker string from the worktree's source appearing in main dist) MUST flag.

TESTS - new file mcp/meter-integrity.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). The three regression tests above plus: the diff fix counts committed+uncommitted together when both exist. Name in your claim which test fails if the change is reverted.

CONSTRAINTS: CommonJS - no import.meta, no top-level await. Your files: mcp/delegation/verify.ts, mcp/delegation/ratify.ts, mcp/delegation/contract.ts, mcp/delegation/worktree-guard.ts, mcp/delegation/supervisor.ts if spend recording lives there, plus your new test file. Do NOT touch mcp/kernel.ts or anything under mcp/store/ (another run owns those right now), nor api.ts/app-client.ts. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage.
VERIFY: npm run test --prefix mcp (935/936 green on main before your change - the one red is a doc-guard another run is fixing; YOUR worktree's bar is zero NEW failures and all three regressions green) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence.

# Citations

[1] explicit_capture (2026-08-20T07:08:36.215Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:three-meter-and-guard-integrity-defects-260820-593c:bug_fix:verify-tss-verifyrun-called-by-runallchecks-which-supervisor-ts-claim-time-dispa","title":"verify.ts's verifyRun (called by runAllChecks, which supervisor.ts claim-time, dispatch...","summary":"verify.ts's verifyRun called by runAllChecks, which supervisor.ts claim time, dispatch.ts claim time, ratify.ts's reverifyRun, and recovery.ts's adoptOrphanedRun all funnel through is the single choke point for diff meas","body":"verify.ts's verifyRun (called by runAllChecks, which supervisor.ts claim-time, dispatch.ts claim-time, ratify.ts's reverifyRun, and recovery.ts's adoptOrphanedRun all funnel through) is the single choke point for diff measurement — fixing measureDiff there fixes claim-time, reverify, AND adopt in one place, no need to touch any of those four call sites.\n\nLearned while delivering: Three meter-and-guard integrity defects, each observed live today with evidence on disk. They share one theme: a check or meter that reports in the reassuring direction when its measurement window is wrong. Fix all three.\n\n1. REVERIFY'S DIFF-SIZE CHECK PASSES VACUOUSLY ON COMMITTED WORKTREES. Runs commit their work at claim time (commitWorktree), so by the time `kage reverify` re-runs the diff-size check the working tree is clean and the check records literally '0 file(s), 0 changed line(s), budget 800' - see .agent_memory/runs/budgets-become-a-circuit-breaker-not-a-t-260820-e70a/evidence/diff-size.log, written while that run's own claim admitted ~1,400 changed lines. Fix: the diff measurement must be merge-base..HEAD PLUS uncommitted changes, wherever the diff check computes its numbers (find the single place; verify.ts's runDiffCheck or its data source) - so a committed diff still counts and an uncommitted one still counts, on every path that runs checks (claim-time, reverify, adopt). Regression test: a worktree whose change is fully committed must report its real line count under reverify, not zero.\n\n2. SPEND RESETS ON RESUME. recordSpend overwrites spend with the agent CLI's latest usage report, and a resumed run starts a FRESH agent session whose cumulative usage starts at zero - so a run that had spent $30.96 before its stop showed $2.95 after resuming (both figures observed on budgets-become-...-e70a today). The meter under-counts exactly the expensive runs. Fix: accumulate across sessions - keep a per-session ledger keyed by agent_session_id (the run record already carries the current one, and it now lands mid-stream) or a sealed-spend baseline that each new session's usage adds onto; state in your claim which design you chose and why it cannot double-count a session that reports usage multiple times (the CLI reports cumulative-per-session on every tick - the last figure per session is the truth for that session). Regression test: stop, resume with a new session id, report smaller usage - total spend must be prior + new, never just new.\n\n3. THE WORKTREE-BOUNDARY GUARD ATTRIBUTES BY DRIFT, NOT BY CONTENT. It fired today on run m1-... because mcp/dist in the MAIN checkout changed during the run's lifetime - but that change was the OPERATOR's own legitimate post-merge rebuild, not the run's escape (evidence: .agent_memory/runs/m1-of-the-memory-store-build-the-storeba-260820-3ff8/evidence/worktree-boundary.log lists dist files from a merge the run never touched). A guard that cries wolf on every operator rebuild will get ignored, which is worse than no guard. Fix: attribute by CONTENT - flag only when the main checkout's changed build output contains code traceable to THIS run's worktree (e.g. identifiers/strings present in the run's new or modified source but absent from the main checkout's committed source - the exact two-marker method that caught the real leak on 2026-08-19). Both cases need tests: the operator-rebuild scenario must NOT flag; a planted leak (a marker string from the worktree's source appearing in main dist) MUST flag.\n\nTESTS - new file mcp/meter-integrity.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). The three regression tests above plus: the diff fix counts committed+uncommitted together when both exist. Name in your claim which test fails if the change is reverted.\n\nCONSTRAINTS: CommonJS - no import.meta, no top-level await. Your files: mcp/delegation/verify.ts, mcp/delegation/ratify.ts, mcp/delegation/contract.ts, mcp/delegation/worktree-guard.ts, mcp/delegation/supervisor.ts if spend recording lives there, plus your new test file. Do NOT touch mcp/kernel.ts or anything under mcp/store/ (another run owns those right now), nor api.ts/app-client.ts. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage.\nVERIFY: npm run test --prefix mcp (935/936 green on main before your change - the one red is a doc-guard another run is fixing; YOUR worktree's bar is zero NEW failures and all three regressions green) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence.\nVerified by: diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, reachability","type":"bug_fix","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["delegated-run","kage-run:three-meter-and-guard-integrity-defects-260820-593c"],"paths":["mcp/delegation/contract.ts","mcp/delegation/supervisor.ts","mcp/delegation/verify.ts","mcp/delegation/worktree-guard.ts","mcp/meter-integrity.test.ts","mcp/worktree-guard.test.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-20T07:08:36.215Z"}],"context":{"fact":"verify.ts's verifyRun (called by runAllChecks, which supervisor.ts claim-time, dispatch.ts claim-time, ratify.ts's reverifyRun, and recovery.ts's adoptOrphanedRun all funnel through) is the single choke point for diff measurement — fixing measureDiff there fixes claim-time, reverify, AND adopt in one place, no need to touch any of those four call sites.","verification":".agent_memory/runs/m1-of-the-memory-store-build-the-storeba-260820-3ff8/evidence/worktree-boundary.log lists dist files from a merge the run never touched). A guard that cries wolf on every operator rebuild will get ignored, which is worse than no guard. Fix: attribute by CONTENT - flag only when the main checkout's changed build output contains code traceable to THIS run's worktree (e.g. identifiers/strings present in the run's new or modified source but absent from the main checkout's committed source - the exact two-marker method that caught the real leak on 2026-08-19). Both cases need tests: the operator-rebuild scenario must NOT flag; a planted leak (a marker string from the worktree's source appearing in main dist) MUST flag.\n\nTESTS - new file mcp/meter-integrity.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). The three regression tests above plus: the diff fix counts committed+uncommitted together when both exist. Name in your claim which test fails if the change is reverted.\n\nCONSTRAINTS: CommonJS - no import.meta, no top-level await. Your files: mcp/delegation/verify.ts, mcp/delegation/ratify.ts, mcp/delegation/contract.ts, mcp/delegation/worktree-guard.ts, mcp/delegation/supervisor.ts if spend recording lives there, plus your new test file. Do NOT touch mcp/kernel.ts or anything under mcp/store/ (another run owns those right now), nor api.ts/app-client.ts. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage.\nVERIFY: npm run test --prefix mcp (935/936 green on main before your change - the one red is a doc-guard another run is fixing; YOUR worktree's bar is zero NEW failures and all three regressions green) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence."},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-20T07:08:36.215Z","path_fingerprints":[{"path":"mcp/delegation/contract.ts","sha256":"6f0879ab432845cf0be37c4ee49be9e63888403d5b63a836c19fa0444007a8c1","size":47166,"symbols":[{"name":"need","kind":"constant","sha256":"9e0478e88af1da758b9cee4a70be31c11e1871dec82bc2a5a1f8580ec5fb25ee"},{"name":"prefix","kind":"constant","sha256":"d0c83c86f4e94d7a484b3325b54d1c90e1c6fc37b342a4cf7d3c33dd33dfb85a"},{"name":"budget","kind":"constant","sha256":"384d19c5e4dcc60e64c4cba1aba25131d3930a3de3c394f0ee612f3d3a68873e"},{"name":"runs","kind":"constant","sha256":"aeed515a96dc123ddceac928842184b148f9bbd85f63bccbb0918926221c95e2"},{"name":"recordspend","kind":"function","sha256":"306469d47eb860299b819b8f7360e3c446e08d151fda705d84a9900bd946a708"},{"name":"sealed","kind":"constant","sha256":"25d0939278915aacddbac8f27dad565e8b3879692c2b6c72afa40b72b9a22590"},{"name":"change","kind":"constant","sha256":"e499aa50ffbb9c260caec4bb421746f795f0062475fe49fbee7f283705f385e8"},{"name":"from","kind":"constant","sha256":"bd2200c07292af9f1ac297cdf2d079dea9c17bd2c3ee4d9522b7a3bb5c07a013"}]},{"path":"mcp/delegation/supervisor.ts","sha256":"d8ed31b23d0078e3e8e44e233ec819b4b203ed5e0b1a47f1a4858c834f5bfd4f","size":43021,"symbols":[{"name":"state","kind":"constant","sha256":"74ff1664f384b9305eedf989c0d85f9eb9f5142fd6d024b00630476f177a9c6e"},{"name":"record","kind":"constant","sha256":"42b464544b1bbb3bece105cafedc2bb1d102343181ac99a5f20ce3461a94ce34"},{"name":"lines","kind":"constant","sha256":"5f197d0245e4b15b6dc5b3e32bf249b2b95b701c002512a7ca7e015e8f461b6e"},{"name":"check","kind":"constant","sha256":"c66ffea80735db586a3cb711454818ae97c9c05b55b9a22863f1be5bdcd90ff2"},{"name":"claim","kind":"constant","sha256":"503d03bbad627f38bd9f2f7b2dd08561cca0c09fbdcba09345502c785d52a736