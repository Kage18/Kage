---
type: "Bug Fix"
title: "This delegated-agent sandbox denies all non-trivial Bash commands (npm, npx, tsc, piped..."
description: "This delegated agent sandbox denies all non trivial Bash commands npm, npx, tsc, piped commands even though the task brief states 'you may be running without permission to execute commands' — only single simple read only"
resource: "mcp/delegation.test.ts"
tags: ["delegated-run", "kage-run:make-goal-events-durable-so-the-orchestr-260818-86df"]
timestamp: "2026-08-18T09:43:19.969Z"
x-kage-id: "repo:make-goal-events-durable-so-the-orchestr-260818-86df:bug_fix:this-delegated-agent-sandbox-denies-all-non-trivial-bash-commands-npm-npx-tsc-pi"
x-kage-type: "bug_fix"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/delegation.test.ts", "mcp/delegation/goal.ts", "mcp/delegation/room-supervisor.ts"]
---

# This delegated-agent sandbox denies all non-trivial Bash commands (npm, npx, tsc, piped...

> This delegated agent sandbox denies all non trivial Bash commands npm, npx, tsc, piped commands even though the task …

This delegated-agent sandbox denies all non-trivial Bash commands (npm, npx, tsc, piped commands) even though the task brief states 'you may be running without permission to execute commands' — only single simple read-only commands like `pwd`, `node --version`, and `grep` succeed here, confirming the same restriction an earlier session on this exact task hit.

Learned while delivering: Make goal events DURABLE so the orchestrator wake-loop cannot drop the events that matter. Observed live and verified: notifyManagerOfRunEvent in mcp/delegation/room-supervisor.ts delivers through askRoomSupervisor op:ask, which requires busy===false, and a busy manager is treated as an accepted drop — so during a real orchestration ZERO [kage event] frames ever reached the manager (no such turns in .agent_memory/room/history.json). A wave changes state immediately after the very turn that dispatched it, while the manager is still busy, so the events that matter most are exactly the ones discarded. FIX: (1) PENDING LOG — in mcp/delegation/goal.ts add a per-goal pending-events log (JSONL under the goal dir) with appendGoalEvent, readPendingGoalEvents, markGoalEventsDelivered, using the same atomic temp+rename rewrite the steer log uses. notifyManagerOfRunEvent must ALWAYS append first, then attempt immediate delivery; success marks those events delivered, a busy or dead manager simply leaves them pending — never a silent drop. (2) DRAIN ON IDLE — in room-supervisor.ts, at the point where a turn completes and the session goes idle, drain pending events for goals whose active session is this thread (room-sessions.ts now stores the active goal per thread — use it): coalesce into ONE compact frame (group by run, keep only the latest state per run, cap the list and state how many were coalesced), deliver it as the next user frame into the held session, then mark delivered. Keep the existing rate limit so a storm cannot flood the manager, and keep the rule that only goal-owned runs generate events — never wake the manager for a human-only run. TESTS in mcp/delegation.test.ts, in a clearly separated block at the END of the file (this file has had append-collision conflicts): an event fired while the manager is busy is PENDING not lost; going idle drains it as exactly one coalesced frame and marks the events delivered; two state changes for the same run coalesce to the latest; a run with no goal produces no event and no frame; a dead manager leaves events pending for the next live one. Use the existing room-supervisor seams so no real process is spawned. Success = npm test fully green.
Verified by: diff-size, citations

## Verification

diff-size, citations

# Citations

[1] explicit_capture (2026-08-18T09:43:19.969Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:make-goal-events-durable-so-the-orchestr-260818-86df:bug_fix:this-delegated-agent-sandbox-denies-all-non-trivial-bash-commands-npm-npx-tsc-pi","title":"This delegated-agent sandbox denies all non-trivial Bash commands (npm, npx, tsc, piped...","summary":"This delegated agent sandbox denies all non trivial Bash commands npm, npx, tsc, piped commands even though the task brief states 'you may be running without permission to execute commands' — only single simple read only","body":"This delegated-agent sandbox denies all non-trivial Bash commands (npm, npx, tsc, piped commands) even though the task brief states 'you may be running without permission to execute commands' — only single simple read-only commands like `pwd`, `node --version`, and `grep` succeed here, confirming the same restriction an earlier session on this exact task hit.\n\nLearned while delivering: Make goal events DURABLE so the orchestrator wake-loop cannot drop the events that matter. Observed live and verified: notifyManagerOfRunEvent in mcp/delegation/room-supervisor.ts delivers through askRoomSupervisor op:ask, which requires busy===false, and a busy manager is treated as an accepted drop — so during a real orchestration ZERO [kage event] frames ever reached the manager (no such turns in .agent_memory/room/history.json). A wave changes state immediately after the very turn that dispatched it, while the manager is still busy, so the events that matter most are exactly the ones discarded. FIX: (1) PENDING LOG — in mcp/delegation/goal.ts add a per-goal pending-events log (JSONL under the goal dir) with appendGoalEvent, readPendingGoalEvents, markGoalEventsDelivered, using the same atomic temp+rename rewrite the steer log uses. notifyManagerOfRunEvent must ALWAYS append first, then attempt immediate delivery; success marks those events delivered, a busy or dead manager simply leaves them pending — never a silent drop. (2) DRAIN ON IDLE — in room-supervisor.ts, at the point where a turn completes and the session goes idle, drain pending events for goals whose active session is this thread (room-sessions.ts now stores the active goal per thread — use it): coalesce into ONE compact frame (group by run, keep only the latest state per run, cap the list and state how many were coalesced), deliver it as the next user frame into the held session, then mark delivered. Keep the existing rate limit so a storm cannot flood the manager, and keep the rule that only goal-owned runs generate events — never wake the manager for a human-only run. TESTS in mcp/delegation.test.ts, in a clearly separated block at the END of the file (this file has had append-collision conflicts): an event fired while the manager is busy is PENDING not lost; going idle drains it as exactly one coalesced frame and marks the events delivered; two state changes for the same run coalesce to the latest; a run with no goal produces no event and no frame; a dead manager leaves events pending for the next live one. Use the existing room-supervisor seams so no real process is spawned. Success = npm test fully green.\nVerified by: diff-size, citations","type":"bug_fix","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["delegated-run","kage-run:make-goal-events-durable-so-the-orchestr-260818-86df"],"paths":["mcp/delegation.test.ts","mcp/delegation/goal.ts","mcp/delegation/room-supervisor.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-18T09:43:19.969Z"}],"context":{"fact":"This delegated-agent sandbox denies all non-trivial Bash commands (npm, npx, tsc, piped commands) even though the task brief states 'you may be running without permission to execute commands' — only single simple read-only commands like `pwd`, `node --version`, and `grep` succeed here, confirming the same restriction an earlier session on this exact task hit.","verification":"diff-size, citations"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-18T09:43:19.969Z","path_fingerprints":[{"path":"mcp/delegation.test.ts","sha256":"4a5f610365e01c3b958a6b32752e17f66014462471e1b023e46078bf283c4749","size":101975,"symbols":[{"name":"learned","kind":"constant","sha256":"86a9cef845cb3f653282806e5a05f474fd9c20f4ad4af48ec5d031f762c896d5"},{"name":