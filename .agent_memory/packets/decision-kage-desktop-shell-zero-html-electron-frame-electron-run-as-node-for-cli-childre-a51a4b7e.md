---
type: "Decision"
title: "Kage desktop shell: zero-HTML Electron frame; ELECTRON_RUN_AS_NODE for CLI children; dock badge rides document.title"
description: "The desktop app is shell/main.js — a ~140 line Electron main process containing zero HTML and zero renderer logic. It reuses kage app no open to find/heal/start the daemon never re implements daemon management and loads"
resource: "shell/main.js"
tags: ["session-learning", "electron", "shell", "desktop", "delegation", "dock-badge"]
timestamp: "2026-08-16T11:06:39.927Z"
x-kage-id: "repo:https-github-com-kage-core-kage:decision:kage-desktop-shell-zero-html-electron-frame-electron-run-as-node-for-cli-childre"
x-kage-type: "decision"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["shell/main.js", "shell/package.json", "mcp/delegation/app-html.ts", "mcp/cli.ts"]
---

# Kage desktop shell: zero-HTML Electron frame; ELECTRON_RUN_AS_NODE for CLI children; dock badge rides document.title

> The desktop app is shell/main.js — a ~140 line Electron main process containing zero HTML and zero renderer logic. It…

The desktop app is shell/main.js — a ~140-line Electron main process containing zero HTML and zero renderer logic. It reuses `kage app --no-open` to find/heal/start the daemon (never re-implements daemon management) and loads the daemon's /app page in a hiddenInset BrowserWindow (sandbox on, contextIsolation on, nodeIntegration off). Two non-obvious mechanics: (1) in the Electron main process, process.execPath is the Electron binary, not Node — spawning the Kage CLI with it launches a second app instance that hangs; child processes must set ELECTRON_RUN_AS_NODE=1. (2) The dock badge needs no IPC: the renderer sets document.title to "Kage · N" for N open decisions, and the shell parses page-title-updated — the title is the one channel a sandboxed page and its shell already share. ⌥K is a global summon/hide shortcut; window.open targets go to the system browser; KAGE_SHELL_SHOT=<path.png> makes the window capture itself after load so "the desktop app runs" is a checkable claim. Launch: cd shell && npx electron . --project <repo>. Electron is a devDependency of the private shell/ package only — the npm kage package keeps its 4 runtime deps.
Evidence: First launch failed with spawnSync Electron ETIMEDOUT (execPath trap), fixed by ELECTRON_RUN_AS_NODE; the relaunched app wrote its own window capture (kage-shell.png) showing the redesigned inbox over the Kage repo with the badge title "Kage · 2".
Verified by: Self-capture PNG from the running window; npm test --prefix mcp → 470 unit + 12 dogfood pass

## Verification

First launch failed with spawnSync Electron ETIMEDOUT (execPath trap), fixed by ELECTRON_RUN_AS_NODE; the relaunched app wrote its own window capture (kage-shell.png) showing the redesigned inbox over the Kage repo with the badge title "Kage · 2".

# Citations

[1] explicit_capture (2026-08-15T08:43:59.552Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:https-github-com-kage-core-kage:decision:kage-desktop-shell-zero-html-electron-frame-electron-run-as-node-for-cli-childre","title":"Kage desktop shell: zero-HTML Electron frame; ELECTRON_RUN_AS_NODE for CLI children; dock badge rides document.title","summary":"The desktop app is shell/main.js — a ~140 line Electron main process containing zero HTML and zero renderer logic. It reuses kage app no open to find/heal/start the daemon never re implements daemon management and loads","body":"The desktop app is shell/main.js — a ~140-line Electron main process containing zero HTML and zero renderer logic. It reuses `kage app --no-open` to find/heal/start the daemon (never re-implements daemon management) and loads the daemon's /app page in a hiddenInset BrowserWindow (sandbox on, contextIsolation on, nodeIntegration off). Two non-obvious mechanics: (1) in the Electron main process, process.execPath is the Electron binary, not Node — spawning the Kage CLI with it launches a second app instance that hangs; child processes must set ELECTRON_RUN_AS_NODE=1. (2) The dock badge needs no IPC: the renderer sets document.title to \"Kage · N\" for N open decisions, and the shell parses page-title-updated — the title is the one channel a sandboxed page and its shell already share. ⌥K is a global summon/hide shortcut; window.open targets go to the system browser; KAGE_SHELL_SHOT=<path.png> makes the window capture itself after load so \"the desktop app runs\" is a checkable claim. Launch: cd shell && npx electron . --project <repo>. Electron is a devDependency of the private shell/ package only — the npm kage package keeps its 4 runtime deps.\nEvidence: First launch failed with spawnSync Electron ETIMEDOUT (execPath trap), fixed by ELECTRON_RUN_AS_NODE; the relaunched app wrote its own window capture (kage-shell.png) showing the redesigned inbox over the Kage repo with the badge title \"Kage · 2\".\nVerified by: Self-capture PNG from the running window; npm test --prefix mcp → 470 unit + 12 dogfood pass","type":"decision","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["session-learning","electron","shell","desktop","delegation","dock-badge"],"paths":["shell/main.js","shell/package.json","mcp/delegation/app-html.ts","mcp/cli.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-15T08:43:59.552Z"}],"context":{"fact":"The desktop app is shell/main.js — a ~140-line Electron main process containing zero HTML and zero renderer logic. It reuses `kage app --no-open` to find/heal/start the daemon (never re-implements daemon management) and loads the daemon's /app page in a hiddenInset BrowserWindow (sandbox on, contextIsolation on, nodeIntegration off). Two non-obvious mechanics: (1) in the Electron main process, process.execPath is the Electron binary, not Node — spawning the Kage CLI with it launches a second app instance that hangs; child processes must set ELECTRON_RUN_AS_NODE=1. (2) The dock badge needs no IPC: the renderer sets document.title to \"Kage · N\" for N open decisions, and the shell parses page-title-updated — the title is the one channel a sandboxed page and its shell already share. ⌥K is a global summon/hide shortcut; window.open targets go to the system browser; KAGE_SHELL_SHOT=<path.png> makes the window capture itself after load so \"the desktop app runs\" is a checkable claim. Launch: cd shell && npx electron . --project <repo>. Electron is a devDependency of the private shell/ package only — the npm kage package keeps its 4 runtime deps.\nEvidence: First launch failed with spawnSync Electron ETIMEDOUT (execPath trap), fixed by ELECTRON_RUN_AS_NODE; the relaunched app wrote its own window capture (kage-shell.png) showing the redesigned inbox over the Kage repo with the badge title \"Kage · 2\".\nVerified by: Self-capture PNG from the running window; npm test --prefix mcp → 470 unit + 12 dogfood pass","verification":"First launch failed with spawnSync Electron ETIMEDOUT (execPath trap), fixed by ELECTRON_RUN_AS_NODE; the relaunched app wrote its own window capture (kage-shell.png) showing the redesigned inbox over the Kage repo with the badge title \"Kage · 2\"."},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-16T11:06:39.927Z","path_fingerprints":[{"path":"shell/main.js","sha256":"c7cf3b75f11c2557184464a8a19798f6af2bcfc20ad1e294eb673f914db960be","size":6795,"symbols":[{"name":"shell","kind":"constant","sha256":"f263d75d34fcf1b386c10b5dceb8caa4ad7ef8da72d83078480234e9286a5471"}]},{"path":"shell/package.json","sha256":"fb4c3b027bba6a8b4c2e8b6f5cc448553f83fa5ad1d9d60301ecf109bfb5afbb","size":497},{"path":"mcp/delegation/app-html.ts","sha256":"e3231b98e635861ba106d9a2211b0654a0e49623349ded109bcb55a0bfb1fd29","size":125340},{"path":"mcp/cli.ts","sha256":"828e76eea9838af0e35e1140d24cb3f98ab8b89dc6fdf7bbf78cef0a5d223952","size":133134,"symbols":[{"name":"main","kind":"function","sha256":"4ef4ae93483a2f0a36dacafb0e536072cf48d86c46b10f209398cd2966e7075d"},{"name":"from","kind":"constant","sha256":"b386d829bcacbd9e216c726f6dff71fc7d8a53aade383fa45b850387414b780c"},{"name":"deps","kind":"constant","sha256":"92952d9b9113155d73fb943b0068685f0ce3e0a26102709e0234ce18896fc3c7"},{"name":"title","kind":"constant","sha256":"6d7ba14d87bd02f948e59b7dbc7774d2913c386857b877e1317147364fb94769"},{"name":"runs","kind":"constant","sha256":"f7d055af62b25c4cb13983f9c81fa5b35c09b64c06279e258694f6f7b180485a"},{"name":"test","kind":"constant","sha256":"c67f3770bf3c3e9b9aff73d99406c7664a900a970c68cfe75a1efefbd4447185"},{"name":"claim","kind":"constant","sha256":"e5d1149d80d0165ec38a10139389c0b1c8db959ed6db25e995ffbb90d7e48fa2"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":4000,"discovery_tokens_estimated":true,"score":100,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","concise but substantive","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":380,"unresolved_symbols":["devDependency","spawnSync"],"reverified_at":"2026-08-16T11:06:39.927Z"},"created_at":"2026-08-15T08:43:59.552Z","updated_at":"2026-08-16T11:06:39.927Z","author_branch":"master"}
```

