---
type: "Decision"
title: "RoomHistoryTurn.manager ('pty'|'headless', room-history.ts) is exactly 'the API already..."
description: "RoomHistoryTurn.manager 'pty'|'headless', room history.ts is exactly 'the API already reports which manager answered ' referenced by the W2 brief for the Chat register's quiet headless label — it lives on GET /room's own"
resource: "mcp/delegation-api.test.ts"
tags: ["delegated-run", "kage-run:w2-of-the-sessions-surface-the-renderer-260820-3482"]
timestamp: "2026-08-20T12:41:17.224Z"
x-kage-id: "repo:w2-of-the-sessions-surface-the-renderer-260820-3482:decision:roomhistoryturn-manager-pty-headless-room-history-ts-is-exactly-the-api-already-"
x-kage-type: "decision"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/delegation-api.test.ts", "mcp/delegation/app-client.ts", "mcp/delegation/app-html.ts", "mcp/delegation/app-styles.ts", "mcp/sessions-ui.test.ts"]
---

# RoomHistoryTurn.manager ('pty'|'headless', room-history.ts) is exactly 'the API already...

> RoomHistoryTurn.manager 'pty'|'headless', room history.ts is exactly 'the API already reports which manager answered …

RoomHistoryTurn.manager ('pty'|'headless', room-history.ts) is exactly 'the API already reports which [manager answered]' referenced by the W2 brief for the Chat register's quiet headless label — it lives on GET /room's own turns, not on /room/transcript.

Learned while delivering: W2 of the sessions surface: the renderer pass. Make what the user SEES match docs/design/SESSIONS_SURFACE.md - the AO-grade sessions UI on Kage's verification substrate. This is the largest renderer change of the project; the design doc (merged, guard-tested) is the contract, and the W1 backend (merged just before you) supplies every piece of data you need. Read both before writing a line: the doc end to end, then W1's additions (display_name on runs, suggested_next on run API responses from delegation/suggest.ts, the per-file diff tree on the run API, orchestrator presence on the room/status surface, /room/transcript from the unified-session run).

THE SEVEN PIECES, in the order a user meets them:
1. SIDEBAR FLEET. Under the active project in the sidebar: the Orchestrator entry first with a live activity dot (presence data from W1), then every non-terminal run by its display_name with its own state dot. Clicking a worker opens its run detail; clicking the Orchestrator opens the Room. Terminal-state runs do not appear here - the board is their home.
2. CHAT BECOMES THE SESSION. The Room's Chat tab renders the REAL orchestrator session from GET /room/transcript (the unified-session backend): the user's message as a prompt block, the manager's words verbatim, tool calls collapsed into group lines ('Ran 2 tool calls' - expandable), timestamps. When the pty manager answered, say nothing special; when the headless fallback answered, show its label quietly (the API already reports which). Delete the old chat rendering path it replaces - two chat renderings would be the two-sources bug in new clothes. Keep the Terminal tab exactly as is: same session, raw view.
3. THE SESSION TRANSCRIPT REGISTER for workers. The run detail's Follow tab graduates to the same register: the brief's intent as the opening prompt block, activity lines grouped ('reading delegation/api.ts - 12 actions' stays), commands with their exit codes, Write/Edit tool calls showing the file name and +/- counts inline (data from the W1 files tree where the stream lacks it), the agent's prose verbatim, duration lines between turns. No invented content: only what the transcript and run record actually carry.
4. THE RIGHT PANEL on run detail, in the design's order: RECEIPT (the existing receipt card, now always visible for ready/merged/failed runs - not behind a tab), SESSION CONTROLS (the deliver-now/queue toggle relocated here, with its two positions relabelled in ONE grammar - both state words: 'Delivers now' / 'Queues' - fixing the standing mode-pill defect), ACTIVITY (state_history as a compact timeline with the by-actor and note text), FILES (the W1 per-file tree with +/- counts; clicking a file scrolls the Diff tab to it). The tabs Follow/Queue/Receipt/Diff/Brief/Raw stay for deep inspection; the panel is the at-a-glance layer.
5. GHOST SUGGESTION. The run composer AND the Room composer pre-fill suggested_next from the API as GHOST TEXT: rendered dim inside the empty input, Tab or click accepts it into the input, any typed character replaces it, Escape dismisses. Never auto-sends. When suggested_next is null the input is simply empty - absent means absent.
6. CARDS AND BOARD. Trim run cards to exactly: display_name, branch chip, state word with dot, token count, age - plus the VERIFIED n/n chip on finished runs (claimVerdict's label, colored by verdict). Remove everything else from cards. The board columns stay as they are. The Memory dashboard's numbers get the standing fix: active = total minus stale (never total), the two different 'stale' quantities get distinct labels naming their windows, and the three access cards get labels that say they do not partition (or make them partition - state which in your claim).
7. PRESENCE AND THE NEW RUN MODAL. When no orchestrator session is live: a one-line banner in the Room - 'No orchestrator is running' - with one Start button wired to the existing start path. And the standing dispatch-modal defect: reserve the preflight forecast's vertical space when the modal opens so the Dispatch button NEVER moves when the forecast resolves; also clear the previous dispatch's transient status text when the modal reopens.

HARD CONSTRAINTS - these files are TS TEMPLATE LITERALS: mcp/delegation/app-client.ts, app-styles.ts, app-html.ts. Client-side escapes must be DOUBLED, no backticks anywhere, no ${ }, never innerHTML - build DOM with the existing h() helper. Match the existing design language exactly: same tokens, same mono/sans split, same surfaces - read app-styles.ts before styling anything, extend its variables, invent no new visual system. The narrow-width (390px) layout that merged yesterday must keep working: every new element needs a sane narrow behaviour, and the composed-page parse gates plus narrow-layout tests must stay green. CommonJS - no import.meta, no top-level await.
DO NOT touch: mcp/kernel.ts, mcp/cli.ts, mcp/store/**, mcp/delegation/suggest.ts, contract.ts, api.ts, git.ts, dispatch.ts, goal.ts (W1 just landed those - consume their API, do not edit them; if an API response lacks something you need, STOP and name it in your claim rather than patching the backend), and no test file that exists today except updating snapshots/gates your renderer change legitimately shifts - name each such edit in your claim.
TESTS - new file mcp/sessions-ui.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). The page is a string: assert the fleet list markup renders per project with display_name and dots; the chat view consumes /room/transcript (its fetch path present, old chat path absent); ghost-text wiring present (dim class + accept-on-Tab handler); cards carry exactly the five elements plus verdict chip; the mode toggle's two labels share one grammar; the preflight space is reserved (the rule that pins the action row); the memory numbers derive active as total-minus-stale; composed page still parses; narrow-layout gates still pass. Name in your claim which test fails if the change is reverted.
VERIFY: npm run test --prefix mcp (fully green on main right now - zero failures is your bar too) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage. If your base shows a red that main has since fixed, rebase onto release-prep first - the stale-base lesson is in repo memory.
Verified by: npm test --prefix mcp, diff-size, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability

## Verification

npm test --prefix mcp, diff-size, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability

# Citations

[1] explicit_capture (2026-08-20T09:02:17.421Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:w2-of-the-sessions-surface-the-renderer-260820-3482:decision:roomhistoryturn-manager-pty-headless-room-history-ts-is-exactly-the-api-already-","title":"RoomHistoryTurn.manager ('pty'|'headless', room-history.ts) is exactly 'the API already...","summary":"RoomHistoryTurn.manager 'pty'|'headless', room history.ts is exactly 'the API already reports which manager answered ' referenced by the W2 brief for the Chat register's quiet headless label — it lives on GET /room's own","body":"RoomHistoryTurn.manager ('pty'|'headless', room-history.ts) is exactly 'the API already reports which [manager answered]' referenced by the W2 brief for the Chat register's quiet headless label — it lives on GET /room's own turns, not on /room/transcript.\n\nLearned while delivering: W2 of the sessions surface: the renderer pass. Make what the user SEES match docs/design/SESSIONS_SURFACE.md - the AO-grade sessions UI on Kage's verification substrate. This is the largest renderer change of the project; the design doc (merged, guard-tested) is the contract, and the W1 backend (merged just before you) supplies every piece of data you need. Read both before writing a line: the doc end to end, then W1's additions (display_name on runs, suggested_next on run API responses from delegation/suggest.ts, the per-file diff tree on the run API, orchestrator presence on the room/status surface, /room/transcript from the unified-session run).\n\nTHE SEVEN PIECES, in the order a user meets them:\n1. SIDEBAR FLEET. Under the active project in the sidebar: the Orchestrator entry first with a live activity dot (presence data from W1), then every non-terminal run by its display_name with its own state dot. Clicking a worker opens its run detail; clicking the Orchestrator opens the Room. Terminal-state runs do not appear here - the board is their home.\n2. CHAT BECOMES THE SESSION. The Room's Chat tab renders the REAL orchestrator session from GET /room/transcript (the unified-session backend): the user's message as a prompt block, the manager's words verbatim, tool calls collapsed into group lines ('Ran 2 tool calls' - expandable), timestamps. When the pty manager answered, say nothing special; when the headless fallback answered, show its label quietly (the API already reports which). Delete the old chat rendering path it replaces - two chat renderings would be the two-sources bug in new clothes. Keep the Terminal tab exactly as is: same session, raw view.\n3. THE SESSION TRANSCRIPT REGISTER for workers. The run detail's Follow tab graduates to the same register: the brief's intent as the opening prompt block, activity lines grouped ('reading delegation/api.ts - 12 actions' stays), commands with their exit codes, Write/Edit tool calls showing the file name and +/- counts inline (data from the W1 files tree where the stream lacks it), the agent's prose verbatim, duration lines between turns. No invented content: only what the transcript and run record actually carry.\n4. THE RIGHT PANEL on run detail, in the design's order: RECEIPT (the existing receipt card, now always visible for ready/merged/failed runs - not behind a tab), SESSION CONTROLS (the deliver-now/queue toggle relocated here, with its two positions relabelled in ONE grammar - both state words: 'Delivers now' / 'Queues' - fixing the standing mode-pill defect), ACTIVITY (state_history as a compact timeline with the by-actor and note text), FILES (the W1 per-file tree with +/- counts; clicking a file scrolls the Diff tab to it). The tabs Follow/Queue/Receipt/Diff/Brief/Raw stay for deep inspection; the panel is the at-a-glance layer.\n5. GHOST SUGGESTION. The run composer AND the Room composer pre-fill suggested_next from the API as GHOST TEXT: rendered dim inside the empty input, Tab or click accepts it into the input, any typed character replaces it, Escape dismisses. Never auto-sends. When suggested_next is null the input is simply empty - absent means absent.\n6. CARDS AND BOARD. Trim run cards to exactly: display_name, branch chip, state word with dot, token count, age - plus the VERIFIED n/n chip on finished runs (claimVerdict's label, colored by verdict). Remove everything else from cards. The board columns stay as they are. The Memory dashboard's numbers get the standing fix: active = total minus stale (never total), the two different 'stale' quantities get distinct labels naming their windows, and the three access cards get labels that say they do not partition (or make them partition - state which in your claim).\n7. PRESENCE AND THE NEW RUN MODAL. When no orchestrator session is live: a one-line banner in the Room - 'No orchestrator is running' - with one Start button wired to the existing start path. And the standing dispatch-modal defect: reserve the preflight forecast's vertical space when the modal opens so the Dispatch button NEVER moves when the forecast resolves; also clear the previous dispatch's transient status text when the modal reopens.\n\nHARD CONSTRAINTS - these files are TS TEMPLATE LITERALS: mcp/delegation/app-client.ts, app-styles.ts, app-html.ts. Client-side escapes must be DOUBLED, no backticks anywhere, no ${ }, never innerHTML - build DOM with the existing h() helper. Match the existing design language exactly: same tokens, same mono/sans split, same surfaces - read app-styles.ts before styling anything, extend its variables, invent no new visual system. The narrow-width (390px) layout that merged yesterday must keep working: every new element needs a sane narrow behaviour, and the composed-page parse gates plus narrow-layout tests must stay green. CommonJS - no import.meta, no top-level await.\nDO NOT touch: mcp/kernel.ts, mcp/cli.ts, mcp/store/**, mcp/delegation/suggest.ts, contract.ts, api.ts, git.ts, dispatch.ts, goal.ts (W1 just landed those - consume their API, do not edit them; if an API response lacks something you need, STOP and name it in your claim rather than patching the backend), and no test file that exists today except updating snapshots/gates your renderer change legitimately shifts - name each such edit in your claim.\nTESTS - new file mcp/sessions-ui.test.ts (repo rule: new behaviour gets its own file; mcp/delegation.test.ts is off-limits). The page is a string: assert the fleet list markup renders per project with display_name and dots; the chat view consumes /room/transcript (its fetch path present, old chat path absent); ghost-text wiring present (dim class + accept-on-Tab handler); cards carry exactly the five elements plus verdict chip; the mode toggle's two labels share one grammar; the preflight space is reserved (the rule that pins the action row); the memory numbers derive active as total-minus-stale; composed page still parses; narrow-layout gates still pass. Name in your claim which test fails if the change is reverted.\nVERIFY: npm run test --prefix mcp (fully green on main right now - zero failures is your bar too) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage. If your base shows a red that main has since fixed, rebase onto release-prep first - the stale-base lesson is in repo memory.\nVerified by: npm test --prefix mcp, diff-size, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability","type":"decision","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["delegated-run","kage-run:w2-of-the-sessions-surface-the-renderer-260820-3482"],"paths":["mcp/delegation-api.test.ts","mcp/delegation/app-client.ts","mcp/delegation/app-html.ts","mcp/delegation/app-styles.ts","mcp/sessions-ui.test.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-20T09:02:17.421Z"}],"context":{"fact":"RoomHistoryTurn.manager ('pty'|'headless', room-history.ts) is exactly 'the API already reports which [manager answered]' referenced by the W2 brief for the Chat register's quiet headless label — it lives on GET /room's own turns, not on /room/transcript.","verification":"npm test --prefix mcp, diff-size, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-20T12:41:17.224Z","path_fingerprints":[{"path":"mcp/delegation-api.test.ts","sha256":"5664b212d3adef17954a296672ff7ce2be4feb285f82989abbcdae276b854adb","size":75803,"symbols":[{"name":"token","kind":"constant","sha256":"9834ba45163697ce4cd9fd57ea9862eb97e3007759508c14c382a70025222c71"},{"name":"room","kind":"constant","sha256":"daca1174495725bcfe924b660b84e822be76aea4fbb7dc3ebb244be8b8a9aa47"},{"name":"verdict","kind":"constant","sha256":"3ed70e56e1a6dfda82106ab5dfc6323d91018e6671f0b7890d0417d1bc307f03"},{"name":"live","kind":"constant","sha256":"9629e8f225f196c0276f8d705b263d0e12479e1dc0bc0c7179246d13e6beb3ef"},{"name":"check","kind":"function","sha256":"7b30564574a597040e8dba4b655835572d5ee55cf88928c264f07931c49d79d5"},{"name":"learned","kind":"constant","sha256":"86a9cef845cb3f653282806e5a05f474fd9c20f4ad4af48ec5d031f762c896d5"},{"name":"quiet","kind":"constant","sha256":"9fe8c45796d5d535459afb04f36cbc84fe2ea172586f10743b0458bd573b9803"},{"name":"diff","kind":"constant","sha256":"4755f26124dc87f6c211031d5c8f7b97491ca68a2f89a4a806acbacee62110a6"},{"name":"first","kind":"constant","sha256":"cc3317ae29ab364733fbfa206f85ac441e0100f2033087b0c3c2d81126a7593a"},{"name":"calls","kind":"constant","sha256":"75e9b50cf46652b7740e78542d25175ee80d8f52094521553c80138965325eb9"},{"name":"still","kind":"constant","sha256":"1459f1107cb523e46b301881ef7497e4d14d252efc606679ea358fc01a3949d4"},{"name":"list","kind":"constant","sha256":"385ad89762910c2d07a3657a28d80425db452ebb7c9db280f45ac7b213534128"},{"name":"intent","kind":"constant","sha256":"d9c22c4887edd9f50a4001dcba18e1adf969e59e65f21db00f8d123e1e86fbf5"}]},{"path":"mcp/delegation/app-client.ts","sha256":"ded26e7b2ea02db2b4eade9056358d995b0517b8f9cf1181245a867beb33bafb","size":206711},{"path":"mcp/delegation/app-html.ts","sha256":"ce8d9a5a3bc47b68e8aef4dd6277c2b22a08bdbb6a50e0e583d0024a2e9728e7","size":14159},{"path":"mcp/delegation/app-styles.ts","sha256":"c8d1bb16e765a5659d5a425df9d3e21f50cc10456d0095463a03618f21efbb77","size":76146},{"path":"mcp/sessions-ui.test.ts","sha256":"cbd5be8e6e773a665a9c99ade3cc72decd0293f7ddab507d606041db83021a4a","size":32037,"symbols":[{"name":"match","kind":"constant","sha256":"1e6d8cbbec9cec96837b2132b17c7b947ef5368686eb7cdeeafa42ee034b1795"},{"name":"card","kind":"constant","sha256":"77df2dee61fb634f903ed9370e34442b488c6b467b372786d35f5e6341d4d35e"},{"name":"file","kind":"constant","sha256":"04eabbf3c8129428a1072ddf2dcf0409551dc725b45835fedb84e2e978633398"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":4000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":1725,"unresolved_symbols":["noEmit"],"reverified_at":"2026-08-20T12:41:17.224Z"},"created_at":"2026-08-20T09:02:17.421Z","updated_at":"2026-08-20T12:41:17.224Z","author_branch":"kage/w2-of-the-sessions-surface-the-renderer-260820-3482"}
```

