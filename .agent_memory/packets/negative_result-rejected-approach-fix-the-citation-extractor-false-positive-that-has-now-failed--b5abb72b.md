---
type: "Negative Result"
title: "Rejected approach: Fix the citation extractor false positive that has now failed three oth"
description: "A delegated attempt at \"Fix the citation extractor false positive that has now failed three otherwise green runs. citedPaths in mcp/delegation/verify.ts scans a claim statement for cited file paths and the citation check"
tags: ["delegated-run", "rejected", "kage-run:fix-the-citation-extractor-false-positiv-260818-06a1"]
timestamp: "2026-08-18T08:45:23.035Z"
x-kage-id: "repo:https-github-com-kage-core-kage:negative_result:rejected-approach-fix-the-citation-extractor-false-positive-that-has-now-failed-"
x-kage-type: "negative_result"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
---

# Rejected approach: Fix the citation extractor false positive that has now failed three oth

> A delegated attempt at "Fix the citation extractor false positive that has now failed three otherwise green runs. cit…

A delegated attempt at "Fix the citation extractor false positive that has now failed three otherwise-green runs. citedPaths in mcp/delegation/verify.ts scans a claim statement for cited file paths and the citation check fails the run when one does not exist in the worktree. It treats ANY slash-joined token as a path, so ordinary prose fails verification: state.room/state.pty (an identifier pair) and .agent_memory/runs/room-mcp.json (a runtime artifact that legitimately does not exist in a worktree) both failed runs whose code and tests were perfect. FIX: tighten the heuristic so it only claims a token is a cited path when it is genuinely path-like AND checkable — require a real file extension on the final segment or a known repo top-level prefix (mcp/, docs/, shell/, evals/, alias/), and EXCLUDE runtime/generated locations that are never part of a worktree diff (anything under .agent_memory/ — those are Kage's own artifacts, not the agent's cited sources). Keep the check strict for real source citations: a claim naming mcp/delegation/verify.ts that does not exist must still fail. Prefer explicit string handling over regex where it is clearer, and keep the existing exported signature. TESTS in mcp/delegation.test.ts, in a clearly separated block near the end: prose identifiers with slashes are not treated as citations; a .agent_memory path in prose is ignored; a real missing source path still fails; a real existing source path still passes; extension-less prose words are ignored. Success = npm test fully green." was rejected.

Reason: Failed on a FLAKY pre-existing test, not on its own change: a room-message timing assertion (actual 2, expected 1) that has nothing to do with the citation extractor, tripped while five heavy runs plus operator merges ran concurrently on one machine. Operator lowered max_concurrent to 3; this work is still wanted and will be redispatched serially. Finding worth keeping: this repo has timing-sensitive room/pty tests that flake under parallel load, so a verification failure under a wide wave deserves a second look before blaming the diff.

Claimed: work delivered — see diff (agent skipped the kage-claim-v1 fence)
Branch kept for inspection: kage/fix-the-citation-extractor-false-positiv-260818-06a1

## Verification

state.room/state.pty (an identifier pair) and .agent_memory/runs/room-mcp.json (a runtime artifact that legitimately does not exist in a worktree) both failed runs whose code and tests were perfect. FIX: tighten the heuristic so it only claims a token is a cited path when it is genuinely path-like AND checkable — require a real file extension on the final segment or a known repo top-level prefix (mcp/, docs/, shell/, evals/, alias/), and EXCLUDE runtime/generated locations that are never part of a worktree diff (anything under .agent_memory/ — those are Kage's own artifacts, not the agent's cited sources). Keep the check strict for real source citations: a claim naming mcp/delegation/verify.ts that does not exist must still fail. Prefer explicit string handling over regex where it is clearer, and keep the existing exported signature. TESTS in mcp/delegation.test.ts, in a clearly separated block near the end: prose identifiers with slashes are not treated as citations; a .agent_memory path in prose is ignored; a real missing source path still fails; a real existing source path still passes; extension-less prose words are ignored. Success = npm test fully green." was rejected.

Reason: Failed on a FLAKY pre-existing test, not on its own change: a room-message timing assertion (actual 2, expected 1) that has nothing to do with the citation extractor, tripped while five heavy runs plus operator merges ran concurrently on one machine. Operator lowered max_concurrent to 3; this work is still wanted and will be redispatched serially. Finding worth keeping: this repo has timing-sensitive room/pty tests that flake under parallel load, so a verification failure under a wide wave deserves a second look before blaming the diff.

Claimed: work delivered — see diff (agent skipped the kage-claim-v1 fence)
Branch kept for inspection: kage/fix-the-citation-extractor-false-positiv-260818-06a1

# Citations

[1] explicit_capture (2026-08-18T08:45:23.035Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:https-github-com-kage-core-kage:negative_result:rejected-approach-fix-the-citation-extractor-false-positive-that-has-now-failed-","title":"Rejected approach: Fix the citation extractor false positive that has now failed three oth","summary":"A delegated attempt at \"Fix the citation extractor false positive that has now failed three otherwise green runs. citedPaths in mcp/delegation/verify.ts scans a claim statement for cited file paths and the citation check","body":"A delegated attempt at \"Fix the citation extractor false positive that has now failed three otherwise-green runs. citedPaths in mcp/delegation/verify.ts scans a claim statement for cited file paths and the citation check fails the run when one does not exist in the worktree. It treats ANY slash-joined token as a path, so ordinary prose fails verification: state.room/state.pty (an identifier pair) and .agent_memory/runs/room-mcp.json (a runtime artifact that legitimately does not exist in a worktree) both failed runs whose code and tests were perfect. FIX: tighten the heuristic so it only claims a token is a cited path when it is genuinely path-like AND checkable — require a real file extension on the final segment or a known repo top-level prefix (mcp/, docs/, shell/, evals/, alias/), and EXCLUDE runtime/generated locations that are never part of a worktree diff (anything under .agent_memory/ — those are Kage's own artifacts, not the agent's cited sources). Keep the check strict for real source citations: a claim naming mcp/delegation/verify.ts that does not exist must still fail. Prefer explicit string handling over regex where it is clearer, and keep the existing exported signature. TESTS in mcp/delegation.test.ts, in a clearly separated block near the end: prose identifiers with slashes are not treated as citations; a .agent_memory path in prose is ignored; a real missing source path still fails; a real existing source path still passes; extension-less prose words are ignored. Success = npm test fully green.\" was rejected.\n\nReason: Failed on a FLAKY pre-existing test, not on its own change: a room-message timing assertion (actual 2, expected 1) that has nothing to do with the citation extractor, tripped while five heavy runs plus operator merges ran concurrently on one machine. Operator lowered max_concurrent to 3; this work is still wanted and will be redispatched serially. Finding worth keeping: this repo has timing-sensitive room/pty tests that flake under parallel load, so a verification failure under a wide wave deserves a second look before blaming the diff.\n\nClaimed: work delivered — see diff (agent skipped the kage-claim-v1 fence)\nBranch kept for inspection: kage/fix-the-citation-extractor-false-positiv-260818-06a1","type":"negative_result","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["delegated-run","rejected","kage-run:fix-the-citation-extractor-false-positiv-260818-06a1"],"paths":[],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-18T08:45:23.035Z"}],"context":{"fact":"A delegated attempt at \"Fix the citation extractor false positive that has now failed three otherwise-green runs. citedPaths in mcp/delegation/verify.ts scans a claim statement for cited file paths and the citation check fails the run when one does not exist in the worktree. It treats ANY slash-joined token as a path, so ordinary prose fails verification: state.room/state.pty (an identifier pair) and .agent_memory/runs/room-mcp.json (a runtime artifact that legitimately does not exist in a worktree) both failed runs whose code and tests were perfect. FIX: tighten the heuristic so it only claims a token is a cited path when it is genuinely path-like AND checkable — require a real file extension on the final segment or a known repo top-level prefix (mcp/, docs/, shell/, evals/, alias/), and EXCLUDE runtime/generated locations that are never part of a worktree diff (anything under .agent_memory/ — those are Kage's own artifacts, not the agent's cited sources). Keep the check strict for real source citations: a claim naming mcp/delegation/verify.ts that does not exist must still fail. Prefer explicit string handling over regex where it is clearer, and keep the existing exported signature. TESTS in mcp/delegation.test.ts, in a clearly separated block near the end: prose identifiers with slashes are not treated as citations; a .agent_memory path in prose is ignored; a real missing source path still fails; a real existing source path still passes; extension-less prose words are ignored. Success = npm test fully green.\" was rejected.","verification":"state.room/state.pty (an identifier pair) and .agent_memory/runs/room-mcp.json (a runtime artifact that legitimately does not exist in a worktree) both failed runs whose code and tests were perfect. FIX: tighten the heuristic so it only claims a token is a cited path when it is genuinely path-like AND checkable — require a real file extension on the final segment or a known repo top-level prefix (mcp/, docs/, shell/, evals/, alias/), and EXCLUDE runtime/generated locations that are never part of a worktree diff (anything under .agent_memory/ — those are Kage's own artifacts, not the agent's cited sources). Keep the check strict for real source citations: a claim naming mcp/delegation/verify.ts that does not exist must still fail. Prefer explicit string handling over regex where it is clearer, and keep the existing exported signature. TESTS in mcp/delegation.test.ts, in a clearly separated block near the end: prose identifiers with slashes are not treated as citations; a .agent_memory path in prose is ignored; a real missing source path still fails; a real existing source path still passes; extension-less prose words are ignored. Success = npm test fully green.\" was rejected.\n\nReason: Failed on a FLAKY pre-existing test, not on its own change: a room-message timing assertion (actual 2, expected 1) that has nothing to do with the citation extractor, tripped while five heavy runs plus operator merges ran concurrently on one machine. Operator lowered max_concurrent to 3; this work is still wanted and will be redispatched serially. Finding worth keeping: this repo has timing-sensitive room/pty tests that flake under parallel load, so a verification failure under a wide wave deserves a second look before blaming the diff.\n\nClaimed: work delivered — see diff (agent skipped the kage-claim-v1 fence)\nBranch kept for inspection: kage/fix-the-citation-extractor-false-positiv-260818-06a1"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-18T08:45:23.035Z","path_fingerprints":[],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":2000,"discovery_tokens_estimated":true,"score":74,"reasons":["high-value memory type","has source evidence","tagged","actionable rationale or verification"],"risks":["not grounded to paths"],"duplicate_candidates":[],"stale_reasons":[],"estimated_tokens_saved":567},"created_at":"2026-08-18T08:45:23.035Z","updated_at":"2026-08-18T08:45:23.035Z","author_branch":"release-prep"}
```

