---
type: "Bug Fix"
title: "readActiveGoal in room-sessions.ts takes a required `sessionKey: string`, not an option..."
description: "readActiveGoal in room sessions.ts takes a required sessionKey: string , not an optional one, even though it normalizes internally via normalizeSessionKey — callers with an optional session must normalize before calling "
resource: "mcp/delegation/room-supervisor.ts"
tags: ["delegated-run", "kage-run:make-goal-events-durable-so-the-orchestr-260818-86df"]
timestamp: "2026-08-20T12:42:32.160Z"
x-kage-id: "repo:make-goal-events-durable-so-the-orchestr-260818-86df:bug_fix:readactivegoal-in-room-sessions-ts-takes-a-required-sessionkey-string-not-an-opt"
x-kage-type: "bug_fix"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/delegation/room-supervisor.ts"]
---

# readActiveGoal in room-sessions.ts takes a required `sessionKey: string`, not an option...

> readActiveGoal in room sessions.ts takes a required sessionKey: string , not an optional one, even though it normaliz…

readActiveGoal in room-sessions.ts takes a required `sessionKey: string`, not an optional one, even though it normalizes internally via normalizeSessionKey — callers with an optional session must normalize before calling it, they can't rely on the function accepting undefined at the type level.

Learned while delivering: Make goal events DURABLE so the orchestrator wake-loop cannot drop the events that matter. Observed live and verified: notifyManagerOfRunEvent in mcp/delegation/room-supervisor.ts delivers through askRoomSupervisor op:ask, which requires busy===false, and a busy manager is treated as an accepted drop — so during a real orchestration ZERO [kage event] frames ever reached the manager (no such turns in .agent_memory/room/history.json). A wave changes state immediately after the very turn that dispatched it, while the manager is still busy, so the events that matter most are exactly the ones discarded. FIX: (1) PENDING LOG — in mcp/delegation/goal.ts add a per-goal pending-events log (JSONL under the goal dir) with appendGoalEvent, readPendingGoalEvents, markGoalEventsDelivered, using the same atomic temp+rename rewrite the steer log uses. notifyManagerOfRunEvent must ALWAYS append first, then attempt immediate delivery; success marks those events delivered, a busy or dead manager simply leaves them pending — never a silent drop. (2) DRAIN ON IDLE — in room-supervisor.ts, at the point where a turn completes and the session goes idle, drain pending events for goals whose active session is this thread (room-sessions.ts now stores the active goal per thread — use it): coalesce into ONE compact frame (group by run, keep only the latest state per run, cap the list and state how many were coalesced), deliver it as the next user frame into the held session, then mark delivered. Keep the existing rate limit so a storm cannot flood the manager, and keep the rule that only goal-owned runs generate events — never wake the manager for a human-only run. TESTS in mcp/delegation.test.ts, in a clearly separated block at the END of the file (this file has had append-collision conflicts): an event fired while the manager is busy is PENDING not lost; going idle drains it as exactly one coalesced frame and marks the events delivered; two state changes for the same run coalesce to the latest; a run with no goal produces no event and no frame; a dead manager leaves events pending for the next live one. Use the existing room-supervisor seams so no real process is spawned. Success = npm test fully green.
Verified by: npm test --prefix mcp, diff-size, citations

## Verification

npm test --prefix mcp, diff-size, citations

# Citations

[1] explicit_capture (2026-08-18T09:46:25.245Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:make-goal-events-durable-so-the-orchestr-260818-86df:bug_fix:readactivegoal-in-room-sessions-ts-takes-a-required-sessionkey-string-not-an-opt","title":"readActiveGoal in room-sessions.ts takes a required `sessionKey: string`, not an option...","summary":"readActiveGoal in room sessions.ts takes a required sessionKey: string , not an optional one, even though it normalizes internally via normalizeSessionKey — callers with an optional session must normalize before calling ","body":"readActiveGoal in room-sessions.ts takes a required `sessionKey: string`, not an optional one, even though it normalizes internally via normalizeSessionKey — callers with an optional session must normalize before calling it, they can't rely on the function accepting undefined at the type level.\n\nLearned while delivering: Make goal events DURABLE so the orchestrator wake-loop cannot drop the events that matter. Observed live and verified: notifyManagerOfRunEvent in mcp/delegation/room-supervisor.ts delivers through askRoomSupervisor op:ask, which requires busy===false, and a busy manager is treated as an accepted drop — so during a real orchestration ZERO [kage event] frames ever reached the manager (no such turns in .agent_memory/room/history.json). A wave changes state immediately after the very turn that dispatched it, while the manager is still busy, so the events that matter most are exactly the ones discarded. FIX: (1) PENDING LOG — in mcp/delegation/goal.ts add a per-goal pending-events log (JSONL under the goal dir) with appendGoalEvent, readPendingGoalEvents, markGoalEventsDelivered, using the same atomic temp+rename rewrite the steer log uses. notifyManagerOfRunEvent must ALWAYS append first, then attempt immediate delivery; success marks those events delivered, a busy or dead manager simply leaves them pending — never a silent drop. (2) DRAIN ON IDLE — in room-supervisor.ts, at the point where a turn completes and the session goes idle, drain pending events for goals whose active session is this thread (room-sessions.ts now stores the active goal per thread — use it): coalesce into ONE compact frame (group by run, keep only the latest state per run, cap the list and state how many were coalesced), deliver it as the next user frame into the held session, then mark delivered. Keep the existing rate limit so a storm cannot flood the manager, and keep the rule that only goal-owned runs generate events — never wake the manager for a human-only run. TESTS in mcp/delegation.test.ts, in a clearly separated block at the END of the file (this file has had append-collision conflicts): an event fired while the manager is busy is PENDING not lost; going idle drains it as exactly one coalesced frame and marks the events delivered; two state changes for the same run coalesce to the latest; a run with no goal produces no event and no frame; a dead manager leaves events pending for the next live one. Use the existing room-supervisor seams so no real process is spawned. Success = npm test fully green.\nVerified by: npm test --prefix mcp, diff-size, citations","type":"bug_fix","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["delegated-run","kage-run:make-goal-events-durable-so-the-orchestr-260818-86df"],"paths":["mcp/delegation/room-supervisor.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-18T09:46:25.245Z"}],"context":{"fact":"readActiveGoal in room-sessions.ts takes a required `sessionKey: string`, not an optional one, even though it normalizes internally via normalizeSessionKey — callers with an optional session must normalize before calling it, they can't rely on the function accepting undefined at the type level.","verification":"npm test --prefix mcp, diff-size, citations"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-20T12:42:32.160Z","path_fingerprints":[{"path":"mcp/delegation/room-supervisor.ts","sha256":"78d92419a70d835de3a0df55acbd64adc15521ec16783f1005b4eb01d9b90fc2","size":37422,"symbols":[{"name":"next","kind":"constant","sha256":"bef68a356033cf375840d2ffe854a864ed5587cd294d8c00ed3dfbb02266d8a2"},{"name":"busy","kind":"constant","sha256":"ef761ea4a3e91148c2a195d09c5895ae0e825f10b9895f7d2ba2a3d7c4412670"},{"name":"turn","kind":"constant","sha256":"f95ea88785537c90b76b020c9eeecb0eb3d2adb3b31e0eae954500d34118ee24"},{"name":"askroomsupervisor","kind":"function","sha256":"4d5b0795888d3b767f529303e6bcc55a8568f7be283790bd369d583140795d83"},{"name":"wave","kind":"constant","sha256":"17015e9b08fafac9eb86e65044032049dddf235a215adc54686bad3857f270f6"},{"name":"state","kind":"constant","sha256":"2b4cf4b0bd4c3d1fd3a85eca9e7d7251ba45f82c571671c197e6087f30672a86"},{"name":"notifymanagerofrunevent","kind":"function","sha256":"b8ace886f989f84dd26381663f4c1e7011584427efe5c7cccafcddcc3d3c98f6"},{"name":"append","kind":"constant","sha256":"494c63631f33e185a528e90cc3d27968a154e7636940bb563e74dd967a8a6cae"},{"name":"runs","kind":"constant","sha256":"b3d76c26b0084e0f4d3ccb5e51c8cca0860499dcbbbc5a38796cad53753a4401"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":649,"unresolved_symbols":["agent_memory"],"reverified_at":"2026-08-20T12:42:32.160Z","stale":true,"stale_reasons":["linked path changed since memory was verified: mcp/delegation/room-supervisor.ts"],"suggested_action":"update"},"created_at":"2026-08-18T09:46:25.245Z","updated_at":"2026-08-21T08:54:05.683Z","author_branch":"kage/make-goal-events-durable-so-the-orchestr-260818-86df"}
```

