---
type: "Gotcha"
title: "Sandboxed agents cannot self-check the renderer's parse gate — the supervisor should run cheap static checks before accepting a claim"
description: "Two consecutive delegated runs on mcp/delegation/app client.ts failed verification on defects the agent could not possibly catch itself: run 1 tripped the innerHTML containment gate with its own comment prose \"No innerHT"
resource: "mcp/delegation/app-client.ts"
tags: ["session-learning", "template-literal", "escaping", "parse-gate", "sandbox", "supervisor", "pre-claim-checks"]
timestamp: "2026-08-17T20:52:26.156Z"
x-kage-id: "repo:https-github-com-kage-core-kage:gotcha:sandboxed-agents-cannot-self-check-the-renderers-parse-gate-the-supervisor-shoul"
x-kage-type: "gotcha"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/delegation/app-client.ts", "mcp/delegation/supervisor.ts", "mcp/delegation-api.test.ts"]
---

# Sandboxed agents cannot self-check the renderer's parse gate — the supervisor should run cheap static checks before accepting a claim

> Two consecutive delegated runs on mcp/delegation/app client.ts failed verification on defects the agent could not pos…

Two consecutive delegated runs on mcp/delegation/app-client.ts failed verification on defects the agent could not possibly catch itself: run 1 tripped the innerHTML containment gate with its own comment prose ("No innerHTML, no regex" — second occurrence of the gate-vs-prose collision; the orphan-class gate hit the same shape earlier and answers it by stripping comments), and run 2 shipped a single under-escaped split("\n\n") whose single backslashes compile to a REAL newline inside the composed client string, killing the script at parse time — the exact gotcha the file header documents. Root cause of the failure LOOP, not just the failures: run agents have no execution rights in their sandbox, so they cannot run node --check on the composed page — the one check this file's conventions demand — and each miss costs a full dispatch-verify-reject-redispatch cycle (~$1.5 and ~6 minutes each). Operator recipe that breaks the loop: build the failed worktree's dist, compose the page via delegationAppHtml, write the script to a file, node --check it for the exact line, then redispatch quoting the defective line and its exact correction. Product affordance worth dispatching: the supervisor (which CAN execute) should run cheap static pre-claim checks (node --check on the composed client when app-client/app-html/app-styles changed) and feed a failure back into the still-live agent session for one self-repair round before verification — converting a full failed-run cycle into an in-run retry.
Evidence: Run fix-three-room-chat-rendering-defects-260817-7831: innerHTML gate failed on the agent's own comment, zero real usages. Run land-the-room-chat-rendering-fix-260817-13cb: parse gate failed; node --check on the composed script pinpointed line 448 split with single backslashes. Both rejected with reasons; third dispatch carries the quoted correction.
Verified by: Kernel evidence files under .agent_memory/runs/ for both runs; operator node --check reproduction.

## Verification

Run fix-three-room-chat-rendering-defects-260817-7831: innerHTML gate failed on the agent's own comment, zero real usages. Run land-the-room-chat-rendering-fix-260817-13cb: parse gate failed; node --check on the composed script pinpointed line 448 split with single backslashes. Both rejected with reasons; third dispatch carries the quoted correction.

# Citations

[1] explicit_capture (2026-08-17T19:36:39.870Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:https-github-com-kage-core-kage:gotcha:sandboxed-agents-cannot-self-check-the-renderers-parse-gate-the-supervisor-shoul","title":"Sandboxed agents cannot self-check the renderer's parse gate — the supervisor should run cheap static checks before accepting a claim","summary":"Two consecutive delegated runs on mcp/delegation/app client.ts failed verification on defects the agent could not possibly catch itself: run 1 tripped the innerHTML containment gate with its own comment prose \"No innerHT","body":"Two consecutive delegated runs on mcp/delegation/app-client.ts failed verification on defects the agent could not possibly catch itself: run 1 tripped the innerHTML containment gate with its own comment prose (\"No innerHTML, no regex\" — second occurrence of the gate-vs-prose collision; the orphan-class gate hit the same shape earlier and answers it by stripping comments), and run 2 shipped a single under-escaped split(\"\\n\\n\") whose single backslashes compile to a REAL newline inside the composed client string, killing the script at parse time — the exact gotcha the file header documents. Root cause of the failure LOOP, not just the failures: run agents have no execution rights in their sandbox, so they cannot run node --check on the composed page — the one check this file's conventions demand — and each miss costs a full dispatch-verify-reject-redispatch cycle (~$1.5 and ~6 minutes each). Operator recipe that breaks the loop: build the failed worktree's dist, compose the page via delegationAppHtml, write the script to a file, node --check it for the exact line, then redispatch quoting the defective line and its exact correction. Product affordance worth dispatching: the supervisor (which CAN execute) should run cheap static pre-claim checks (node --check on the composed client when app-client/app-html/app-styles changed) and feed a failure back into the still-live agent session for one self-repair round before verification — converting a full failed-run cycle into an in-run retry.\nEvidence: Run fix-three-room-chat-rendering-defects-260817-7831: innerHTML gate failed on the agent's own comment, zero real usages. Run land-the-room-chat-rendering-fix-260817-13cb: parse gate failed; node --check on the composed script pinpointed line 448 split with single backslashes. Both rejected with reasons; third dispatch carries the quoted correction.\nVerified by: Kernel evidence files under .agent_memory/runs/ for both runs; operator node --check reproduction.","type":"gotcha","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["session-learning","template-literal","escaping","parse-gate","sandbox","supervisor","pre-claim-checks"],"paths":["mcp/delegation/app-client.ts","mcp/delegation/supervisor.ts","mcp/delegation-api.test.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-17T19:36:39.870Z"}],"context":{"fact":"Two consecutive delegated runs on mcp/delegation/app-client.ts failed verification on defects the agent could not possibly catch itself: run 1 tripped the innerHTML containment gate with its own comment prose (\"No innerHTML, no regex\" — second occurrence of the gate-vs-prose collision; the orphan-class gate hit the same shape earlier and answers it by stripping comments), and run 2 shipped a single under-escaped split(\"\\n\\n\") whose single backslashes compile to a REAL newline inside the composed client string, killing the script at parse time — the exact gotcha the file header documents. Root cause of the failure LOOP, not just the failures: run agents have no execution rights in their sandbox, so they cannot run node --check on the composed page — the one check this file's conventions demand — and each miss costs a full dispatch-verify-reject-redispatch cycle (~$1.5 and ~6 minutes each). Operator recipe that breaks the loop: build the failed worktree's dist, compose the page via delegationAppHtml, write the script to a file, node --check it for the exact line, then redispatch quoting the defective line and its exact correction. Product affordance worth dispatching: the supervisor (which CAN execute) should run cheap static pre-claim checks (node --check on the composed client when app-client/app-html/app-styles changed) and feed a failure back into the still-live agent session for one self-repair round before verification — converting a full failed-run cycle into an in-run retry.\nEvidence: Run fix-three-room-chat-rendering-defects-260817-7831: innerHTML gate failed on the agent's own comment, zero real usages. Run land-the-room-chat-rendering-fix-260817-13cb: parse gate failed; node --check on the composed script pinpointed line 448 split with single backslashes. Both rejected with reasons; third dispatch carries the quoted correction.\nVerified by: Kernel evidence files under .agent_memory/runs/ for both runs; operator node --check reproduction.","verification":"Run fix-three-room-chat-rendering-defects-260817-7831: innerHTML gate failed on the agent's own comment, zero real usages. Run land-the-room-chat-rendering-fix-260817-13cb: parse gate failed; node --check on the composed script pinpointed line 448 split with single backslashes. Both rejected with reasons; third dispatch carries the quoted correction."},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-17T20:52:26.156Z","path_fingerprints":[{"path":"mcp/delegation/app-client.ts","sha256":"fc32a40985a41fd801263e1fd471a4cf59e555de4317bee67a7060d98c384bff","size":114374},{"path":"mcp/delegation/supervisor.ts","sha256":"b12f7b9ac588c293faedafb881f2fa6d43eeeea923f00f5f394804767753571c","size":21103,"symbols":[{"name":"verification","kind":"constant","sha256":"0e8ef41a682662a3f4b76b32d9df952232c5f1493a9fb8e53946e94cbe8489d1"},{"name":"claim","kind":"constant","sha256":"e94b9085c778497525d8daef323c7725188bde4197da98cc6eb46190ae5d7506"}]},{"path":"mcp/delegation-api.test.ts","sha256":"8e82d5d85af655870d13afee445a5c44e4b305bdd0f5c0ae7e3bf99936f05303","size":49722,"symbols":[{"name":"feed","kind":"constant","sha256":"38003f546da73239ba957c86a2cf7d173f8179a7240599f252d2e3027cbd6a72"},{"name":"room","kind":"constant","sha256":"daca1174495725bcfe924b660b84e822be76aea4fbb7dc3ebb244be8b8a9aa47"},{"name":"check","kind":"function","sha256":"7b30564574a597040e8dba4b655835572d5ee55cf88928c264f07931c49d79d5"},{"name":"still","kind":"constant","sha256":"1459f1107cb523e46b301881ef7497e4d14d252efc606679ea358fc01a3949d4"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":495,"unresolved_symbols":["innerHTML"],"reverified_at":"2026-08-17T20:52:26.156Z"},"created_at":"2026-08-17T19:36:39.870Z","updated_at":"2026-08-17T20:52:26.156Z","author_branch":"release-prep"}
```

