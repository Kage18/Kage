---
type: "Gotcha"
title: "kage reverify re-checks a CLAIM, not a worktree — its help text promises the worktree"
description: "kage reverify <run id cannot recover a run that never produced a claim. It refuses with \"Run <id has no claim to reverify.\" even when the run's worktree holds complete, correct work. The CLI help line for the command rea"
resource: "mcp/cli.ts"
tags: ["session-learning", "reverify", "recovery", "orphaned-run", "cli-help"]
timestamp: "2026-08-20T12:42:01.938Z"
x-kage-id: "repo:https-github-com-kage-core-kage:gotcha:kage-reverify-re-checks-a-claim-not-a-worktree-its-help-text-promises-the-worktr"
x-kage-type: "gotcha"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/cli.ts", "mcp/delegation/verify.ts", "mcp/delegation/contract.ts"]
---

# kage reverify re-checks a CLAIM, not a worktree — its help text promises the worktree

> kage reverify <run id cannot recover a run that never produced a claim. It refuses with "Run <id has no claim to reve…

`kage reverify <run-id>` cannot recover a run that never produced a claim. It refuses with "Run <id> has no claim to reverify." even when the run's worktree holds complete, correct work. The CLI help line for the command reads "re-check a failed/ready run's current worktree — no agent re-run", which promises worktree-based recovery the code does not implement. This matters most in exactly the case where recovery is needed: when a supervisor dies before its agent finishes, no claim is ever captured (the supervisor is what parses the agent's claim fence), so the one command that looks like the recovery path is the one that cannot help. Do not plan an orphan-recovery flow around reverify without first checking whether a claim exists.
Evidence: Run the-predicted-touch-set-is-uncorrelated-260819-ecbd: supervisor died seconds after dispatch, agent worked ~20 minutes and produced 196 insertions across mcp/delegation/brief.ts, preflight.ts, verify.ts plus a new mcp/touch-set.test.ts. Run state failed, worktree intact. `node mcp/dist/cli.js reverify the-predicted-touch-set-is-uncorrelated-260819-ecbd --project .` printed "Run the-predicted-touch-set-is-uncorrelated-260819-ecbd has no claim to reverify." and exited 0 without re-checking anything.
Verified by: kage reverify invoked against a failed run with an intact worktree and no claim; refusal reproduced

## Verification

Run the-predicted-touch-set-is-uncorrelated-260819-ecbd: supervisor died seconds after dispatch, agent worked ~20 minutes and produced 196 insertions across mcp/delegation/brief.ts, preflight.ts, verify.ts plus a new mcp/touch-set.test.ts. Run state failed, worktree intact. `node mcp/dist/cli.js reverify the-predicted-touch-set-is-uncorrelated-260819-ecbd --project .` printed "Run the-predicted-touch-set-is-uncorrelated-260819-ecbd has no claim to reverify." and exited 0 without re-checking anything.

# Citations

[1] explicit_capture (2026-08-19T14:52:27.450Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:https-github-com-kage-core-kage:gotcha:kage-reverify-re-checks-a-claim-not-a-worktree-its-help-text-promises-the-worktr","title":"kage reverify re-checks a CLAIM, not a worktree — its help text promises the worktree","summary":"kage reverify <run id cannot recover a run that never produced a claim. It refuses with \"Run <id has no claim to reverify.\" even when the run's worktree holds complete, correct work. The CLI help line for the command rea","body":"`kage reverify <run-id>` cannot recover a run that never produced a claim. It refuses with \"Run <id> has no claim to reverify.\" even when the run's worktree holds complete, correct work. The CLI help line for the command reads \"re-check a failed/ready run's current worktree — no agent re-run\", which promises worktree-based recovery the code does not implement. This matters most in exactly the case where recovery is needed: when a supervisor dies before its agent finishes, no claim is ever captured (the supervisor is what parses the agent's claim fence), so the one command that looks like the recovery path is the one that cannot help. Do not plan an orphan-recovery flow around reverify without first checking whether a claim exists.\nEvidence: Run the-predicted-touch-set-is-uncorrelated-260819-ecbd: supervisor died seconds after dispatch, agent worked ~20 minutes and produced 196 insertions across mcp/delegation/brief.ts, preflight.ts, verify.ts plus a new mcp/touch-set.test.ts. Run state failed, worktree intact. `node mcp/dist/cli.js reverify the-predicted-touch-set-is-uncorrelated-260819-ecbd --project .` printed \"Run the-predicted-touch-set-is-uncorrelated-260819-ecbd has no claim to reverify.\" and exited 0 without re-checking anything.\nVerified by: kage reverify invoked against a failed run with an intact worktree and no claim; refusal reproduced","type":"gotcha","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["session-learning","reverify","recovery","orphaned-run","cli-help"],"paths":["mcp/cli.ts","mcp/delegation/verify.ts","mcp/delegation/contract.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-19T14:52:27.450Z"}],"context":{"fact":"`kage reverify <run-id>` cannot recover a run that never produced a claim. It refuses with \"Run <id> has no claim to reverify.\" even when the run's worktree holds complete, correct work. The CLI help line for the command reads \"re-check a failed/ready run's current worktree — no agent re-run\", which promises worktree-based recovery the code does not implement. This matters most in exactly the case where recovery is needed: when a supervisor dies before its agent finishes, no claim is ever captured (the supervisor is what parses the agent's claim fence), so the one command that looks like the recovery path is the one that cannot help. Do not plan an orphan-recovery flow around reverify without first checking whether a claim exists.\nEvidence: Run the-predicted-touch-set-is-uncorrelated-260819-ecbd: supervisor died seconds after dispatch, agent worked ~20 minutes and produced 196 insertions across mcp/delegation/brief.ts, preflight.ts, verify.ts plus a new mcp/touch-set.test.ts. Run state failed, worktree intact. `node mcp/dist/cli.js reverify the-predicted-touch-set-is-uncorrelated-260819-ecbd --project .` printed \"Run the-predicted-touch-set-is-uncorrelated-260819-ecbd has no claim to reverify.\" and exited 0 without re-checking anything.\nVerified by: kage reverify invoked against a failed run with an intact worktree and no claim; refusal reproduced","verification":"Run the-predicted-touch-set-is-uncorrelated-260819-ecbd: supervisor died seconds after dispatch, agent worked ~20 minutes and produced 196 insertions across mcp/delegation/brief.ts, preflight.ts, verify.ts plus a new mcp/touch-set.test.ts. Run state failed, worktree intact. `node mcp/dist/cli.js reverify the-predicted-touch-set-is-uncorrelated-260819-ecbd --project .` printed \"Run the-predicted-touch-set-is-uncorrelated-260819-ecbd has no claim to reverify.\" and exited 0 without re-checking anything."},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-20T12:42:01.938Z","path_fingerprints":[{"path":"mcp/cli.ts","sha256":"f49dea5e35b66b1309c62c71f90a8b0a6986061d7956f3417bcb330139cd14e3","size":150054,"symbols":[{"name":"command","kind":"constant","sha256":"9e594169e1f8559f50d7e73b407f1aa3a9a0f14bf43a676fedefa72734badb98"},{"name":"current","kind":"constant","sha256":"471f3cfbffa04d6ecddb2f3d4013027b71237de766afb69ec418f1f1b8308937"},{"name":"agent","kind":"constant","sha256":"5b9caa614311fe691d6af171b9a8985b0d49464b9df178ad28ff5b9883eb4cf2"},{"name":"plan","kind":"constant","sha256":"4ac354c6ab0916ca294173e8647a9aa608ac812ea36e8129f05721e0822902f0"},{"name":"test","kind":"constant","sha256":"c67f3770bf3c3e9b9aff73d99406c7664a900a970c68cfe75a1efefbd4447185"}]},{"path":"mcp/delegation/verify.ts","sha256":"862abe6b14f036221a86c82b71d9e83234404c827e2eed3d7515a6f7e71d6482","size":19211,"symbols":[{"name":"path","kind":"constant","sha256":"83c2238b8ba558b62d6620118338401da219426c2a75ca1481aa2f18ec551d1e"},{"name":"minutes","kind":"constant","sha256":"296ee74989bd5a622c676fdf25bf8ab6a792a4b857b2fbf199262b26945d00ee"}]},{"path":"mcp/delegation/contract.ts","sha256":"b00fed2868c1a4c64be39b8d82e4b49a7414c7f2eb1c9e6b5cd561c954f03891","size":50356,"symbols":[{"name":"plan","kind":"constant","sha256":"f7aaf2aa2689f78f53ef378b158cfe7101f2dd4ede7e0acf10f6e55274d89469"},{"name":"needed","kind":"constant","sha256":"a3b91d52a820aa557b45d115cceaa93a3d3db344c5fbe63006f01df0cca0a3db"},{"name":"minutes","kind":"constant","sha256":"39c314b988d0b3d91761a0676f0210c72e20ddf208003edb41782a203e48aeb7"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":100,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","concise but substantive","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":343,"reverified_at":"2026-08-20T12:42:01.938Z"},"created_at":"2026-08-19T14:52:27.450Z","updated_at":"2026-08-20T12:42:01.938Z","author_branch":"release-prep"}
```

