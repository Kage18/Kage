---
type: "Bug Fix"
title: "Every verifying run falsely displays as dropped: liveState checks the agent pid, but the agent is gone during verification by design"
description: "Confirmed live 2026 08 18 : a run in state verifying shows display state dropped while its supervisor is demonstrably alive and running the checks. Mechanism: verification happens AFTER the hired agent exits — the superv"
resource: "mcp/delegation/contract.ts"
tags: ["session-learning", "liveState", "verifying", "reaper", "false-dropped", "honesty", "dogfood"]
timestamp: "2026-08-18T10:01:50.840Z"
x-kage-id: "repo:https-github-com-kage-core-kage:bug_fix:every-verifying-run-falsely-displays-as-dropped-livestate-checks-the-agent-pid-b"
x-kage-type: "bug_fix"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/delegation/contract.ts", "mcp/delegation/supervisor.ts"]
---

# Every verifying run falsely displays as dropped: liveState checks the agent pid, but the agent is gone during verification by design

> Confirmed live 2026 08 18 : a run in state verifying shows display state dropped while its supervisor is demonstrably…

Confirmed live (2026-08-18): a run in state verifying shows display_state dropped while its supervisor is demonstrably alive and running the checks. Mechanism: verification happens AFTER the hired agent exits — the supervisor runs verifyRun itself — but task.agent_pid tracks the AGENT, so during the whole verification window (npm test + build, ~90s here, longer under load) isProcessAlive(agent_pid) is false and liveState marks the run stale. Two consequences, one cosmetic and one dangerous: surfaces show a healthy run as dropped, and sweepDeadRuns reaps dropped runs after a 5-minute grace, so a slow-but-healthy verification can be killed by the reaper. Evidence: run mcp-delegation-verify-ts-citedpaths-trea-260818-2d31 displayed dropped while pgrep showed its supervisor (cli.js supervise <runId>) alive and working. Operator consequence worth remembering: I rejected an earlier run partly on this false signal, so the bug also costs real work. Fix direction: record the SUPERVISOR's pid on the task (supervisor_pid) alongside agent_pid and have liveState treat verifying as in-flight when the supervisor is alive; only when neither the agent nor the supervisor exists is a verifying run genuinely stale. Same family as the day's other honesty bugs — a check that conflates "the thing I happened to track is gone" with "the work is dead".
Evidence: pgrep showed pid 66032 running cli.js supervise mcp-delegation-verify-ts-citedpaths-trea-260818-2d31 while the API reported display_state dropped and task.json had agent_pid null with state verifying.
Verified by: Live observation on the merged Goals build; process table cross-checked against the run record and API response.

## Verification

run mcp-delegation-verify-ts-citedpaths-trea-260818-2d31 displayed dropped while pgrep showed its supervisor (cli.js supervise <runId>) alive and working. Operator consequence worth remembering: I rejected an earlier run partly on this false signal, so the bug also costs real work. Fix direction: record the SUPERVISOR's pid on the task (supervisor_pid) alongside agent_pid and have liveState treat verifying as in-flight when the supervisor is alive; only when neither the agent nor the supervisor exists is a verifying run genuinely stale. Same family as the day's other honesty bugs — a check that conflates "the thing I happened to track is gone" with "the work is dead".

# Citations

[1] explicit_capture (2026-08-18T10:01:50.840Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:https-github-com-kage-core-kage:bug_fix:every-verifying-run-falsely-displays-as-dropped-livestate-checks-the-agent-pid-b","title":"Every verifying run falsely displays as dropped: liveState checks the agent pid, but the agent is gone during verification by design","summary":"Confirmed live 2026 08 18 : a run in state verifying shows display state dropped while its supervisor is demonstrably alive and running the checks. Mechanism: verification happens AFTER the hired agent exits — the superv","body":"Confirmed live (2026-08-18): a run in state verifying shows display_state dropped while its supervisor is demonstrably alive and running the checks. Mechanism: verification happens AFTER the hired agent exits — the supervisor runs verifyRun itself — but task.agent_pid tracks the AGENT, so during the whole verification window (npm test + build, ~90s here, longer under load) isProcessAlive(agent_pid) is false and liveState marks the run stale. Two consequences, one cosmetic and one dangerous: surfaces show a healthy run as dropped, and sweepDeadRuns reaps dropped runs after a 5-minute grace, so a slow-but-healthy verification can be killed by the reaper. Evidence: run mcp-delegation-verify-ts-citedpaths-trea-260818-2d31 displayed dropped while pgrep showed its supervisor (cli.js supervise <runId>) alive and working. Operator consequence worth remembering: I rejected an earlier run partly on this false signal, so the bug also costs real work. Fix direction: record the SUPERVISOR's pid on the task (supervisor_pid) alongside agent_pid and have liveState treat verifying as in-flight when the supervisor is alive; only when neither the agent nor the supervisor exists is a verifying run genuinely stale. Same family as the day's other honesty bugs — a check that conflates \"the thing I happened to track is gone\" with \"the work is dead\".\nEvidence: pgrep showed pid 66032 running cli.js supervise mcp-delegation-verify-ts-citedpaths-trea-260818-2d31 while the API reported display_state dropped and task.json had agent_pid null with state verifying.\nVerified by: Live observation on the merged Goals build; process table cross-checked against the run record and API response.","type":"bug_fix","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["session-learning","liveState","verifying","reaper","false-dropped","honesty","dogfood"],"paths":["mcp/delegation/contract.ts","mcp/delegation/supervisor.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-18T10:01:50.840Z"}],"context":{"fact":"Confirmed live (2026-08-18): a run in state verifying shows display_state dropped while its supervisor is demonstrably alive and running the checks. Mechanism: verification happens AFTER the hired agent exits — the supervisor runs verifyRun itself — but task.agent_pid tracks the AGENT, so during the whole verification window (npm test + build, ~90s here, longer under load) isProcessAlive(agent_pid) is false and liveState marks the run stale. Two consequences, one cosmetic and one dangerous: surfaces show a healthy run as dropped, and sweepDeadRuns reaps dropped runs after a 5-minute grace, so a slow-but-healthy verification can be killed by the reaper. Evidence: run mcp-delegation-verify-ts-citedpaths-trea-260818-2d31 displayed dropped while pgrep showed its supervisor (cli.js supervise <runId>) alive and working. Operator consequence worth remembering: I rejected an earlier run partly on this false signal, so the bug also costs real work. Fix direction: record the SUPERVISOR's pid on the task (supervisor_pid) alongside agent_pid and have liveState treat verifying as in-flight when the supervisor is alive; only when neither the agent nor the supervisor exists is a verifying run genuinely stale. Same family as the day's other honesty bugs — a check that conflates \"the thing I happened to track is gone\" with \"the work is dead\".\nEvidence: pgrep showed pid 66032 running cli.js supervise mcp-delegation-verify-ts-citedpaths-trea-260818-2d31 while the API reported display_state dropped and task.json had agent_pid null with state verifying.\nVerified by: Live observation on the merged Goals build; process table cross-checked against the run record and API response.","verification":"run mcp-delegation-verify-ts-citedpaths-trea-260818-2d31 displayed dropped while pgrep showed its supervisor (cli.js supervise <runId>) alive and working. Operator consequence worth remembering: I rejected an earlier run partly on this false signal, so the bug also costs real work. Fix direction: record the SUPERVISOR's pid on the task (supervisor_pid) alongside agent_pid and have liveState treat verifying as in-flight when the supervisor is alive; only when neither the agent nor the supervisor exists is a verifying run genuinely stale. Same family as the day's other honesty bugs — a check that conflates \"the thing I happened to track is gone\" with \"the work is dead\"."},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-18T10:01:50.840Z","path_fingerprints":[{"path":"mcp/delegation/contract.ts","sha256":"0a196580f07d622a1e353ca2146c2ee09da7d1b8bc75d8ab8aa9cc6101f49f91","size":28618,"symbols":[{"name":"runs","kind":"constant","sha256":"aeed515a96dc123ddceac928842184b148f9bbd85f63bccbb0918926221c95e2"},{"name":"isprocessalive","kind":"function","sha256":"2ef971e601770357e0b32745a45e7bfa9e01aa138f0045a32c95a4c7e1f999e3"},{"name":"livestate","kind":"function","sha256":"315259448a509981f85b90699a4a1c3f37206fc0502de480246aeacc9ece13fa"},{"name":"sweepdeadruns","kind":"function","sha256":"9ffa0ba0afe62c88bf4448c5fb8cc6311204f31828de829806b84a5a6ccc45ac"}]},{"path":"mcp/delegation/supervisor.ts","sha256":"b8bb782d44d5791f7bdd30ecabca551d579e5d64cec7bb85e03c46c4470cb4d7","size":24200,"symbols":[{"name":"task","kind":"constant","sha256":"85f8a3124cd54be2d855713df25f16201af7f8bd479bd55ac37f1f399bd11753"},{"name":"state","kind":"constant","sha256":"74ff1664f384b9305eedf989c0d85f9eb9f5142fd6d024b00630476f177a9c6e"},{"name":"record","kind":"constant","sha256":"42b464544b1bbb3bece105cafedc2bb1d102343181ac99a5f20ce3461a94ce34"},{"name":"signal","kind":"constant","sha256":"58cf9dbc0b422f6562f67838df8679182ff3f26f0e871a7bd2368007ac08130b"},{"name":"verification","kind":"constant","sha256":"0e8ef41a682662a3f4b76b32d9df952232c5f1493a9fb8e53946e94cbe8489d1"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"stale_reasons":[],"estimated_tokens_saved":421},"created_at":"2026-08-18T10:01:50.840Z","updated_at":"2026-08-18T10:01:50.840Z","author_branch":"release-prep"}
```

