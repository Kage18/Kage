---
type: "Negative Result"
title: "Rejected approach: In mcp/delegation/supervisor.ts, run cheap static checks (tsc --noEmit;"
description: "A delegated attempt at \"In mcp/delegation/supervisor.ts, run cheap static checks tsc noEmit; node check on the composed page when app client.ts/app html.ts/app styles.ts changed BEFORE accepting a claim fence from the he"
tags: ["delegated-run", "rejected", "kage-run:in-mcp-delegation-supervisor-ts-run-chea-260818-d778"]
timestamp: "2026-08-18T09:35:00.315Z"
x-kage-id: "repo:https-github-com-kage-core-kage:negative_result:rejected-approach-in-mcp-delegation-supervisor-ts-run-cheap-static-checks-tsc-no"
x-kage-type: "negative_result"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
---

# Rejected approach: In mcp/delegation/supervisor.ts, run cheap static checks (tsc --noEmit;

> A delegated attempt at "In mcp/delegation/supervisor.ts, run cheap static checks tsc noEmit; node check on the compos…

A delegated attempt at "In mcp/delegation/supervisor.ts, run cheap static checks (tsc --noEmit; node --check on the composed page when app-client.ts/app-html.ts/app-styles.ts changed) BEFORE accepting a claim fence from the held agent session, and feed a failure back into the held session for one self-repair round before falling through to the normal verify-reject cycle. Goal: agents cannot execute code in their sandbox, so today every type or parse error costs a full verify-reject-redispatch cycle. Scope your edits to mcp/delegation/supervisor.ts; add your tests to mcp/delegation.test.ts in a clearly separated block near the END of the file (a companion run is adding unrelated tests to the same file in parallel — minimize hunk overlap so both can merge cleanly)." was rejected.

Reason: Leftover from the first orchestrated wave, which the manager dispatched twice per intent; both duplicates died under the five-way parallel load (git lock contention and flaky timing tests). The underlying work — citation-extractor tightening and supervisor pre-claim static checks — is still wanted and is being redispatched serially now that concurrency is back to 3 and the git-retry fix has merged.

Claimed: superviseRun in mcp/delegation/supervisor.ts now runs cheap static checks (tsc --noEmit against mcp/tsconfig.json when the worktree has node_modules installed; node --check against the re-composed <script> when app-client.ts/app-html.ts/app-styles.ts changed) at the exact moment a live-child claim fence would otherwise close stdin, and feeds one failure message back into the held session for exactly one self-repair turn before falling through unchanged to the normal verify-reject cycle; both checks are exported (runTypecheck, composedAppScript, checkComposedPage, extractTrailingConst, runPreClaimStaticChecks) and unit-tested plus exercised end-to-end via a new REGRESSION test in mcp/delegation.test.ts appended in a clearly separated block at the end of the file.
Branch kept for inspection: kage/in-mcp-delegation-supervisor-ts-run-chea-260818-d778

# Citations

[1] explicit_capture (2026-08-18T09:35:00.315Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:https-github-com-kage-core-kage:negative_result:rejected-approach-in-mcp-delegation-supervisor-ts-run-cheap-static-checks-tsc-no","title":"Rejected approach: In mcp/delegation/supervisor.ts, run cheap static checks (tsc --noEmit;","summary":"A delegated attempt at \"In mcp/delegation/supervisor.ts, run cheap static checks tsc noEmit; node check on the composed page when app client.ts/app html.ts/app styles.ts changed BEFORE accepting a claim fence from the he","body":"A delegated attempt at \"In mcp/delegation/supervisor.ts, run cheap static checks (tsc --noEmit; node --check on the composed page when app-client.ts/app-html.ts/app-styles.ts changed) BEFORE accepting a claim fence from the held agent session, and feed a failure back into the held session for one self-repair round before falling through to the normal verify-reject cycle. Goal: agents cannot execute code in their sandbox, so today every type or parse error costs a full verify-reject-redispatch cycle. Scope your edits to mcp/delegation/supervisor.ts; add your tests to mcp/delegation.test.ts in a clearly separated block near the END of the file (a companion run is adding unrelated tests to the same file in parallel — minimize hunk overlap so both can merge cleanly).\" was rejected.\n\nReason: Leftover from the first orchestrated wave, which the manager dispatched twice per intent; both duplicates died under the five-way parallel load (git lock contention and flaky timing tests). The underlying work — citation-extractor tightening and supervisor pre-claim static checks — is still wanted and is being redispatched serially now that concurrency is back to 3 and the git-retry fix has merged.\n\nClaimed: superviseRun in mcp/delegation/supervisor.ts now runs cheap static checks (tsc --noEmit against mcp/tsconfig.json when the worktree has node_modules installed; node --check against the re-composed <script> when app-client.ts/app-html.ts/app-styles.ts changed) at the exact moment a live-child claim fence would otherwise close stdin, and feeds one failure message back into the held session for exactly one self-repair turn before falling through unchanged to the normal verify-reject cycle; both checks are exported (runTypecheck, composedAppScript, checkComposedPage, extractTrailingConst, runPreClaimStaticChecks) and unit-tested plus exercised end-to-end via a new REGRESSION test in mcp/delegation.test.ts appended in a clearly separated block at the end of the file.\nBranch kept for inspection: kage/in-mcp-delegation-supervisor-ts-run-chea-260818-d778","type":"negative_result","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["delegated-run","rejected","kage-run:in-mcp-delegation-supervisor-ts-run-chea-260818-d778"],"paths":[],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-18T09:35:00.315Z"}],"context":{"fact":"A delegated attempt at \"In mcp/delegation/supervisor.ts, run cheap static checks (tsc --noEmit; node --check on the composed page when app-client.ts/app-html.ts/app-styles.ts changed) BEFORE accepting a claim fence from the held agent session, and feed a failure back into the held session for one self-repair round before falling through to the normal verify-reject cycle. Goal: agents cannot execute code in their sandbox, so today every type or parse error costs a full verify-reject-redispatch cycle. Scope your edits to mcp/delegation/supervisor.ts; add your tests to mcp/delegation.test.ts in a clearly separated block near the END of the file (a companion run is adding unrelated tests to the same file in parallel — minimize hunk overlap so both can merge cleanly).\" was rejected."},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-18T09:35:00.315Z","path_fingerprints":[],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":2000,"discovery_tokens_estimated":true,"score":74,"reasons":["high-value memory type","has source evidence","tagged","actionable rationale or verification"],"risks":["not grounded to paths"],"duplicate_candidates":[],"estimated_tokens_saved":517},"created_at":"2026-08-18T09:35:00.315Z","updated_at":"2026-08-20T20:13:09.752Z","author_branch":"release-prep"}
```

