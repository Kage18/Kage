---
type: "Bug Fix"
title: "cardCoreAtoms(run) returns [branchAtom, stateAtom, tokensAtom?] in that fixed order; Bo..."
description: "cardCoreAtoms run returns branchAtom, stateAtom, tokensAtom? in that fixed order; Board's fillBoardCard now reuses it via .slice 1 to get state+tokens for row 3 while building its own middle truncated branch label for ro"
resource: "mcp/delegation/app-client.ts"
tags: ["delegated-run", "kage-run:board-cards-crush-the-run-s-name-into-co-260820-3d0f"]
timestamp: "2026-08-20T12:02:11.492Z"
x-kage-id: "repo:board-cards-crush-the-run-s-name-into-co-260820-3d0f:bug_fix:cardcoreatoms-run-returns-branchatom-stateatom-tokensatom-in-that-fixed-order-bo"
x-kage-type: "bug_fix"
x-kage-status: "pending"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-confidence: 0.7
x-kage-verified: "verified"
x-kage-paths: ["mcp/delegation/app-client.ts", "mcp/delegation/app-styles.ts", "mcp/sessions-ui.test.ts"]
---

# cardCoreAtoms(run) returns [branchAtom, stateAtom, tokensAtom?] in that fixed order; Bo...

> cardCoreAtoms run returns branchAtom, stateAtom, tokensAtom? in that fixed order; Board's fillBoardCard now reuses it…

cardCoreAtoms(run) returns [branchAtom, stateAtom, tokensAtom?] in that fixed order; Board's fillBoardCard now reuses it via .slice(1) to get state+tokens for row 3 while building its own middle-truncated branch label for row 2, so the branch-building logic (shortBranch) still has exactly one call site's worth of behavior shared between List and Board without changing cardCoreAtoms's shared signature.

Learned while delivering: Board cards crush the run's name into confetti - fix the card layout. Owner screenshot from the live app just now: in Board view every card renders its display_name in a column roughly five characters wide, so titles shatter into 'Writ / th...', 'Kill / th...', 'M4 / of...', 'Ever / d...' - while the raw run-id slug underneath ('write-if-changed-finish-the-render-calm-260820-3502') wraps across the card's FULL width and visually dominates. The sparse-card trim put the right content on the card but the title shares a flex row with the meta cluster (state dot, token count, verdict chip), which squeezes it to nothing at board-column widths; List view survives only because its rows are wider.

THE FIX, in mcp/delegation/app-client.ts and app-styles.ts:
1. Card layout becomes three stacked rows, each full card width: ROW 1 - display_name, up to two lines, overflow ellipsis on the second line (-webkit-line-clamp 2 with the existing fallback pattern if one exists in the stylesheet; otherwise single-line ellipsis is acceptable - state which you shipped). ROW 2 - the branch/slug chip, single line, TRUNCATED IN THE MIDDLE so both the leading words and the trailing -260820-xxxx serial stay readable (CSS direction tricks are fragile here - do it in JS when building the label: keep first ~18 and last ~12 chars with a single ellipsis between; state your exact split). ROW 3 - the meta cluster: state dot + word, token count, age, verdict chip right-aligned. Never let rows 1 and 3 share horizontal space.
2. The same card component serves List and Board - verify the fix does not regress List rows (they may keep a wider single-line title; if List uses a different renderer, fix Board's only and say so).
3. Check 390px narrow width: cards must keep the three-row shape without horizontal overflow; the board's own horizontal scroll for multiple columns stays as-is.

TESTS - extend mcp/sessions-ui.test.ts (the card assertions live there - editing it for the card layout it already covers is in scope): the card markup renders name, slug and meta as three sibling rows; the middle-truncation helper keeps prefix and suffix with one ellipsis and never exceeds its cap; composed page parses; narrow-layout gates stay green.

CONSTRAINTS: app-client.ts and app-styles.ts are TS TEMPLATE LITERALS - escapes DOUBLED, no backticks, no ${ }, never innerHTML, DOM via h(). Your files: those two plus mcp/sessions-ui.test.ts only. CommonJS. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage. Rebase onto release-prep first if your base carries an inherited red. End with the kage-claim-v1 fence, citing files individually - never slash-joined.
VERIFY: npm run test --prefix mcp (1038 unit + 12 dogfood green on main) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence.
Verified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability

## Verification

npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability

# Citations

[1] explicit_capture (2026-08-20T12:02:11.492Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:board-cards-crush-the-run-s-name-into-co-260820-3d0f:bug_fix:cardcoreatoms-run-returns-branchatom-stateatom-tokensatom-in-that-fixed-order-bo","title":"cardCoreAtoms(run) returns [branchAtom, stateAtom, tokensAtom?] in that fixed order; Bo...","summary":"cardCoreAtoms run returns branchAtom, stateAtom, tokensAtom? in that fixed order; Board's fillBoardCard now reuses it via .slice 1 to get state+tokens for row 3 while building its own middle truncated branch label for ro","body":"cardCoreAtoms(run) returns [branchAtom, stateAtom, tokensAtom?] in that fixed order; Board's fillBoardCard now reuses it via .slice(1) to get state+tokens for row 3 while building its own middle-truncated branch label for row 2, so the branch-building logic (shortBranch) still has exactly one call site's worth of behavior shared between List and Board without changing cardCoreAtoms's shared signature.\n\nLearned while delivering: Board cards crush the run's name into confetti - fix the card layout. Owner screenshot from the live app just now: in Board view every card renders its display_name in a column roughly five characters wide, so titles shatter into 'Writ / th...', 'Kill / th...', 'M4 / of...', 'Ever / d...' - while the raw run-id slug underneath ('write-if-changed-finish-the-render-calm-260820-3502') wraps across the card's FULL width and visually dominates. The sparse-card trim put the right content on the card but the title shares a flex row with the meta cluster (state dot, token count, verdict chip), which squeezes it to nothing at board-column widths; List view survives only because its rows are wider.\n\nTHE FIX, in mcp/delegation/app-client.ts and app-styles.ts:\n1. Card layout becomes three stacked rows, each full card width: ROW 1 - display_name, up to two lines, overflow ellipsis on the second line (-webkit-line-clamp 2 with the existing fallback pattern if one exists in the stylesheet; otherwise single-line ellipsis is acceptable - state which you shipped). ROW 2 - the branch/slug chip, single line, TRUNCATED IN THE MIDDLE so both the leading words and the trailing -260820-xxxx serial stay readable (CSS direction tricks are fragile here - do it in JS when building the label: keep first ~18 and last ~12 chars with a single ellipsis between; state your exact split). ROW 3 - the meta cluster: state dot + word, token count, age, verdict chip right-aligned. Never let rows 1 and 3 share horizontal space.\n2. The same card component serves List and Board - verify the fix does not regress List rows (they may keep a wider single-line title; if List uses a different renderer, fix Board's only and say so).\n3. Check 390px narrow width: cards must keep the three-row shape without horizontal overflow; the board's own horizontal scroll for multiple columns stays as-is.\n\nTESTS - extend mcp/sessions-ui.test.ts (the card assertions live there - editing it for the card layout it already covers is in scope): the card markup renders name, slug and meta as three sibling rows; the middle-truncation helper keeps prefix and suffix with one ellipsis and never exceeds its cap; composed page parses; narrow-layout gates stay green.\n\nCONSTRAINTS: app-client.ts and app-styles.ts are TS TEMPLATE LITERALS - escapes DOUBLED, no backticks, no ${ }, never innerHTML, DOM via h(). Your files: those two plus mcp/sessions-ui.test.ts only. CommonJS. Build and test ONLY inside your own worktree - never run npm, tsc or node against /Users/kushaljain/code/Kage. Rebase onto release-prep first if your base carries an inherited red. End with the kage-claim-v1 fence, citing files individually - never slash-joined.\nVERIFY: npm run test --prefix mcp (1038 unit + 12 dogfood green on main) and npm run build --prefix mcp with no 'error TS'. Run them for real and fill in the claim fence.\nVerified by: npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability","type":"bug_fix","scope":"repo","visibility":"team","sensitivity":"internal","status":"pending","confidence":0.7,"tags":["delegated-run","kage-run:board-cards-crush-the-run-s-name-into-co-260820-3d0f"],"paths":["mcp/delegation/app-client.ts","mcp/delegation/app-styles.ts","mcp/sessions-ui.test.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-08-20T12:02:11.492Z"}],"context":{"fact":"cardCoreAtoms(run) returns [branchAtom, stateAtom, tokensAtom?] in that fixed order; Board's fillBoardCard now reuses it via .slice(1) to get state+tokens for row 3 while building its own middle-truncated branch label for row 2, so the branch-building logic (shortBranch) still has exactly one call site's worth of behavior shared between List and Board without changing cardCoreAtoms's shared signature.","verification":"npm test --prefix mcp, diff-size, citations, npx tsc --noEmit -p mcp/tsconfig.json, node --check <composed inline script>, reachability"},"freshness":{"ttl_days":365,"last_verified_at":"2026-08-20T12:02:11.492Z","path_fingerprints":[{"path":"mcp/delegation/app-client.ts","sha256":"e1bab470474c3fd1f7a8f9d5d645e6cf83c1ef17048ded3fb27703a53a91c78b","size":202601},{"path":"mcp/delegation/app-styles.ts","sha256":"4d00b49da2cfb0fab1f3e64a655a1a6e7e7dc948fcbc5bc64f90c6b4d48121f6","size":74796},{"path":"mcp/sessions-ui.test.ts","sha256":"6d6dc34e5ee2c94af8ae1f3529bcc7df43ebbd54e0f2ebdef968c1c6e4fd92f9","size":17333,"symbols":[{"name":"fillboardcard","kind":"constant","sha256":"b1d77f9771d5dd4d761ac56207020ab0eb22c55d659dbb84a75c13513f8bfdbb"},{"name":"card","kind":"constant","sha256":"77df2dee61fb634f903ed9370e34442b488c6b467b372786d35f5e6341d4d35e"},{"name":"rows","kind":"constant","sha256":"47160f4cea4a1d46971c11cb91aefc94092275d9eb21db2f71316bb90de08226"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"stale_reasons":[],"estimated_tokens_saved":864,"unresolved_symbols":["branchAtom","tokensAtom","noEmit"]},"created_at":"2026-08-20T12:02:11.492Z","updated_at":"2026-08-20T12:02:11.686Z","author_branch":"kage/board-cards-crush-the-run-s-name-into-co-260820-3d0f"}
```

