---
name: kage-context-is-the-single-session-start-tool-replacing-4-se
description: kage context project dir, query replaces the old 4 step validate + recall + code graph + graph sequence. It runs all four in one call and returns a combined context block. Added in mcp/index.ts callTool handler and liste
---

# kage_context is the single session-start tool replacing 4 separate calls

kage context project dir, query replaces the old 4 step validate + recall + code graph + graph sequence. It runs all four in one call and returns a combined context block. Added in mcp/index.ts callTool handler and liste

## Detail

kage_context(project_dir, query) replaces the old 4-step validate + recall + code_graph + graph sequence. It runs all four in one call and returns a combined context block. Added in mcp/index.ts callTool() handler and listed first in the MCP tool list. AGENTS.md and AGENTS.md updated to reference kage_context. Requires Codex v2.1.121+ with alwaysLoad: true in ~/.Codex.json for tools to be immediately available (not deferred). On older versions alwaysLoad silently does nothing and tools end up deferred requiring ToolSearch.
Verified by: 64/64 tests passing, global MCP install updated

## Grounded in

- `mcp/index.ts`
- `mcp/kernel.ts`
- `AGENTS.md`
- `AGENTS.md`

---
_Generated by `kage skills` from verified repo memory (packet `repo:https-github-com-kage-core-kage:runbook:kage-context-is-the-single-session-start-tool-replacing-4-separate-calls-1777792`). Checked against the code it cites; regenerate with `kage skills`._
