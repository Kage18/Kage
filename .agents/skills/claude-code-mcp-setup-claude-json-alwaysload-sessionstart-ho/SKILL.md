---
name: Codex-mcp-setup-Codex-json-alwaysload-sessionstart-ho
description: Codex reads MCP servers from ~/.Codex.json not ~/.Codex/settings.json . The server entry requires type: "stdio" and alwaysLoad: true to make tools immediately visible without ToolSearch. A SessionStart hook at ~/
---

# Codex MCP setup: ~/.Codex.json + alwaysLoad + SessionStart hook

Codex reads MCP servers from ~/.Codex.json not ~/.Codex/settings.json . The server entry requires type: "stdio" and alwaysLoad: true to make tools immediately visible without ToolSearch. A SessionStart hook at ~/

## Detail

Codex reads MCP servers from ~/.Codex.json (not ~/.Codex/settings.json). The server entry requires type: "stdio" and alwaysLoad: true to make tools immediately visible without ToolSearch. A SessionStart hook at ~/.Codex/kage/hooks/session-start.sh injects the full AGENTS.md policy as a system message — this is the only way to get mandatory Codex-equivalent enforcement in Codex, since AGENTS.md is advisory context not a system-level protocol.
Verified by: Manually verified: Codex mcp add --scope user --transport stdio wrote correct ~/.Codex.json; alwaysLoad confirmed by sub-agent listing 31 kage tools without ToolSearch; hook tested with echo pipe

## Grounded in

- `mcp/kernel.ts`
- `AGENTS.md`
- `AGENTS.md`

---
_Generated by `kage skills` from verified repo memory (packet `repo:https-github-com-kage-core-kage:runbook:Codex-mcp-setup-Codex-json-alwaysload-sessionstart-hook-1777737306925`). Checked against the code it cites; regenerate with `kage skills`._
