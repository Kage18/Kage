---
name: run-kage-mcp-tests
description: Fact: Run the Kage MCP package test suite from the mcp directory with npm test, or from the repo root with npm test prefix mcp. Why: The test script builds TypeScript first and then runs node test over dist/ / .test.js, Use when: Recall when asked how to run tests, verify recall/ranking changes, or check MCP package behavior.
---

# Run Kage MCP tests

Fact: Run the Kage MCP package test suite from the mcp directory with npm test, or from the repo root with npm test prefix mcp. Why: The test script builds TypeScript first and then runs node test over dist/ / .test.js,

Run the Kage MCP package test suite from the mcp directory with npm test, or from the repo root with npm test --prefix mcp.

**Why it matters:** The test script builds TypeScript first and then runs node --test over dist/**/*.test.js, so it verifies both compilation and behavior.

**Use this when:** Recall when asked how to run tests, verify recall/ranking changes, or check MCP package behavior.

## What to do

Use npm test in mcp for the full package suite; use node --check mcp/viewer/app.js for a quick browser viewer syntax check after viewer-only edits.

## Verify

mcp/package.json defines test as npm run build && node --test dist/**/*.test.js.

**If you skip this:** Agents may cite old release proof memories that mention tests without giving the actual command.

## Grounded in

- `mcp/package.json`
- `mcp/kernel.test.ts`
- `mcp/mcp.test.ts`
- `mcp/registry.test.ts`

---
_Generated by `kage skills` from verified repo memory (packet `repo:https-github-com-kage-core-kage:runbook:run-kage-mcp-tests-1778039925946`). Checked against the code it cites; regenerate with `kage skills`._
