{
  "name": "@kage-core/kage-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Kage — the desktop app. An orchestrator of memory and agents that runs while you work.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:core": "npm --prefix ../../mcp run build",
    "build:portal": "npm --prefix ../web run build && npm --prefix ../../mcp run bundle:portal",
    "build:all": "npm run build:core && npm run build:portal && npm run build",
    "start": "npm run build:all && electron dist/main.js",
    "start:fast": "npm run build && electron dist/main.js",
    "assets": "node scripts/make-assets.mjs",
    "dist:mac": "npm run build:all && npm run assets && electron-builder --mac --publish never"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.6.0"
  },
  "build": {
    "appId": "com.kagecore.kage",
    "productName": "Kage",
    "files": ["dist/**/*", "assets/**/*"],
    "extraResources": [
      { "from": "../../mcp/dist", "to": "kage/dist" },
      { "from": "../../mcp/viewer", "to": "kage/viewer" }
    ],
    "mac": {
      "category": "public.app-category.developer-tools",
      "target": ["dmg", "zip"]
    }
  }
}
